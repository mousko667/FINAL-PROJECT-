# AUDIT_MASTER — Registre unique de l'audit exhaustif

> **Registre persistant sur les 7 phases (P0→P6).** C'est la source de vérité unique des findings.
> Méthodologie : `docs/superpowers/specs/2026-07-22-audit-exhaustif-systeme-design.md`.
> Modèle du système : `docs/AUDIT_SYSTEM_MODEL.md`. Couverture : `docs/AUDIT_COVERAGE.md`.
>
> Ouvert le **2026-07-22** sur la branche `audit/exhaustif-p0-comprehension` (depuis `main` = `c4f5e11`).

---

## Règles du registre — à ne jamais enfreindre

1. **Un ID n'est jamais réutilisé.** Numérotation continue `AUDIT-001`, `AUDIT-002`, … Prochain ID
   libre : **AUDIT-047**.
2. **Un finding n'est jamais réévalué deux fois.** S'il ressort dans une phase ultérieure, on
   complète la ligne existante (preuve runtime, dépendances) — on n'en crée pas une nouvelle.
3. **Preuve runtime obligatoire** pour tout finding visuel ou fonctionnel (capture Playwright, trace
   réseau, log console). Un finding sans preuve reste « à confirmer » et ne peut pas passer en P6.
4. **Aucun finding n'est émis en P0.** P0 = compréhension + mise en place. Les findings commencent
   en P1. AUDIT-001 est la seule exception : il était pré-identifié avant le lancement de l'audit.
5. **Statuts autorisés** : `OUVERT` · `EN COURS` · `CORRIGÉ` · `HORS-SCOPE`.
   `HORS-SCOPE` exige une validation explicite de l'utilisateur, consignée dans la ligne.
6. **Sévérités** : `P0` bloquant (le système est faux ou dangereux) · `P1` majeur (fonction cassée
   ou faille SoD) · `P2` mineur (gêne réelle, contournable) · `P3` cosmétique / dette / suggestion.
7. **Fin d'audit (P6)** : plus aucune ligne en `OUVERT` ou `EN COURS`.

---

## Registre

| ID | Phase | Module | Sévérité | Titre | Localisation | Cause probable | Preuve runtime | Solution proposée | Dépendances | Statut |
|---|---|---|---|---|---|---|---|---|---|---|
| AUDIT-001 | P0 | supplier-portal | À confirmer (P2/P3 proposé) | Portail fournisseur : aucun endpoint de lecture des PO — le fournisseur recopie le numéro de commande à la main | `src/main/java/com/oct/invoicesystem/domain/invoice/model/Invoice.java:138` (`purchaseOrderId`) · `src/main/java/com/oct/invoicesystem/domain/supplier/controller/SupplierPortalController.java:273` (`toInvoice`) | `purchaseOrderId` est un champ **plat déclaratif** (UUID repris tel quel de la requête), pas une relation « facturer contre une commande ». `SupplierPortalController` (classe `@PreAuthorize("hasRole('SUPPLIER')")`, base `/api/v1/supplier`) n'expose que factures / documents / profil / dashboard — **aucun `GET` de PO ouverts**. Le fournisseur doit donc connaître le numéro de commande hors système (email, appel, contrat) et le saisir manuellement ; seul le three-way matching côté OCT le vérifie a posteriori. | **PROUVÉE (P3, 2026-07-22) — et le constat est PLUS GRAVE que l'hypothèse P0.** Parcours joué en runtime avec le compte `supplier` sur `/supplier/invoices/new` (capture `docs/audit/p3-captures/AUDIT-001-portail-formulaire-sans-champ-PO.png`) : après « Ignorer — saisir manuellement », le formulaire ne comporte que **7 champs** — `departmentId`, `invoiceNumber`, `amount`, `currency`, `issueDate`, `dueDate`, `description`. **Il n'existe AUCUN champ de bon de commande** : ni liste déroulante, ni saisie libre. Le fournisseur ne peut donc pas « recopier le numéro à la main » comme supposé en P0 — il ne peut **pas du tout** rattacher sa facture à un PO depuis l'interface. Le champ `purchaseOrderId` n'est atteignable que par appel direct de l'API (vérifié : `POST /api/v1/supplier/invoices` l'accepte, facture `FAC-2026-00043` créée avec le PO d'un tiers). **Conséquence fonctionnelle : aucune facture soumise via le portail ne peut jamais déclencher le three-way matching** — le contrôle financier central est structurellement contourné pour toute la voie fournisseur. | Deux volets désormais distincts. (a) **Interface** : ajouter au formulaire une sélection de PO alimentée par un nouvel endpoint `GET /api/v1/supplier/purchase-orders` scopé au fournisseur connecté. (b) **Décision de cadrage en P5** : si le rattachement à un PO n'est pas voulu côté fournisseur, il faut l'assumer explicitement au cahier des charges et documenter que le matching ne couvre que la voie interne (AA). En l'état, le système promet un contrôle qu'il n'applique pas sur cette voie. | Recoupe AUDIT-002 (même champ, volet propriété) et **AUDIT-031** (les `lineItems` du portail sont ignorés — second verrou sur le même parcours) | **CORRIGÉ (P6, lot V1-B, 2026-07-23, après AUDIT-031)** — (a) nouvel endpoint `GET /api/v1/supplier/purchase-orders` **scopé au fournisseur authentifié** (`PurchaseOrderRepository.findOpenBySupplierId` : `supplier.id` + `deletedAt IS NULL` + statut `OPEN` seul), avec un DTO dédié `SupplierPurchaseOrderDTO` **omettant volontairement** `createdBy` et `supplierId` (données internes OCT) ; (b) sélecteur de BC ajouté à `SupplierInvoiceSubmitPage.tsx` : la sélection préremplit le montant **et** envoie les lignes du PO comme `lineItems` de la facture, de sorte que le rapprochement dispose de données ligne à ligne des deux côtés. **Preuve** : `purchaseOrders_returnsOnlyOwnOpenOrders` (un PO OPEN du fournisseur retourné ; PO `CLOSED` **et** PO d'un autre fournisseur exclus ; `createdBy`/`supplierId` absents de la réponse), `staff_CannotListSupplierPurchaseOrders` (compte AA → **403**), et le parcours complet `portalInvoiceAgainstOwnPo_reachesMatchedOnSubmit` → **MATCHED**. Backend **655/0/0**, frontend **237/237**, `tsc` 0. Journalisé **PROB-132**. |
| AUDIT-002 | P1 | supplier-portal / purchasing | **P1** (majeur — SoD / fuite inter-fournisseurs) | Aucun contrôle de propriété du `purchaseOrderId` saisi par le fournisseur : un fournisseur peut référencer le PO d'un **autre** fournisseur | `src/main/java/com/oct/invoicesystem/domain/supplier/controller/SupplierPortalController.java:260-281` (`toInvoice`) · `src/main/java/com/oct/invoicesystem/domain/invoice/service/InvoiceStateMachineServiceImpl.java:170-201` (`performMatchingCheck`) · `src/main/java/com/oct/invoicesystem/domain/purchasing/model/PurchaseOrder.java:52-53` | `toInvoice` recopie `request.purchaseOrderId()` sans validation. `performMatchingCheck` charge ensuite le PO par `findByIdActive(...)` **sans jamais comparer `po.getSupplier().getId()` au fournisseur de la facture**, alors que `PurchaseOrder` porte bien un `supplier_id` non nul. Le rapprochement 3-way peut donc s'exécuter — et réussir — contre le bon de commande d'un tiers, ce qui vaut à la fois divulgation indirecte (montants/quantités d'un concurrent via le résultat du matching) et contournement du contrôle financier. Aucun test ne couvre ce cas (`SupplierPortalController` n'a pas de test d'intégration dédié). | **PROUVÉE (P3, 2026-07-22) — l'absence de contrôle de propriété est confirmée, avec fuite de données d'un tiers.** Le compte `supplier` = fournisseur `40433645`. PO `PO-2026-101` = fournisseur `cf24d2c9` (**un tiers**). (a) *Voie portail* : `POST /api/v1/supplier/invoices` avec `purchaseOrderId` du tiers → **201 Created**, facture `FAC-2026-00043` acceptée, aucun refus. (b) *Voie interne* (AA, facture au nom du fournisseur `40433645` mais PO du tiers `cf24d2c9`, facture `FAC-2026-00045`) : le matching **s'exécute réellement** contre le PO du tiers et la réponse d'erreur **divulgue ses données** — message HTTP 400 littéral : « *Invoice total: 28905000.00 XAF; **PO total: 28905000.00 XAF; GRN: GRN-2026-101 (received on 2026-06-20)**; Applied tolerance: 2.00% or 0.00 XAF* ». Le montant du bon de commande, le numéro et la date du bon de réception d'un **autre fournisseur** sont renvoyés à l'appelant. Le rapprochement n'a jamais comparé `po.getSupplier().getId()` au fournisseur de la facture. | Dans `performMatchingCheck`, rejeter si `po.getSupplier().getId()` ≠ fournisseur de la facture, **avant** d'exécuter le rapprochement, avec un message i18n générique ne divulguant aucune donnée du PO. Revoir aussi `generateDiscrepancyNotes` : les notes d'écart ne doivent jamais remonter telles quelles dans un message d'erreur d'API. Ajouter le test d'intégration « PO d'un autre fournisseur → refus ». | Recoupe AUDIT-001 · **AUDIT-034** (le message d'écart divulgue les données du PO/GRN) | **CORRIGÉ (P6, lot V2-B, 2026-07-23)** — verrou posé aux **deux** portes d'entrée prouvées en P3 : (a) `performMatchingCheck` compare `po.getSupplier()` au fournisseur de la facture **avant** d'exécuter le rapprochement — le GRN n'est même plus chargé ; (b) le portail refuse dès la création en restreignant à `listOpenBySupplier`, c'est-à-dire exactement l'ensemble que propose le sélecteur de PO, de sorte que l'API et le formulaire ne peuvent plus diverger. Le refus est une clé i18n générique (`matching.po.not_owned`) qui ne confirme même pas l'existence du PO. **Preuve** : `InvoiceStateMachineServiceTest` — « PO d'un tiers → refus, message **strictement égal** à la clé générique, GRN jamais chargé » **et** la contre-preuve « même montage, même fournisseur → la garde laisse passer » (sans laquelle un refus systématique satisferait le test). |
| AUDIT-003 | P1 | invoice / frontend | **P2** (mineur — homogénéité d'UX ; révisé P1→P2 en P5, décision D7) | `InvoiceDetailPage` n'a aucun `PageRoleGuard` : toute la donnée financière d'une facture est lisible par URL directe | `frontend/src/pages/InvoiceDetailPage.tsx` (aucune occurrence de `PageRoleGuard` dans tout le fichier) · à comparer à `frontend/src/pages/InvoiceListPage.tsx:313` (gardé par `ALLOWED_ROLES`) · route `frontend/src/AppRoutes.tsx:92` | Les routes ne portent aucune restriction de rôle (`ProtectedRoute` ne vérifie que l'authentification) : la protection est portée par la page. `InvoiceDetailPage` est la seule page financière sans garde. Seuls certains boutons sont conditionnés (`canOverride`, `canClassify`), **pas la lecture**. Le backend refuse bien ADMIN sur `GET /api/v1/invoices/{id}` (`InvoiceController.java:99`, `!hasRole('ADMIN')`), donc l'impact réel dépend du rendu en cas de 403 — d'où la nécessité d'une preuve runtime. | **PROUVÉE (P2, 2026-07-22)** — capture `docs/audit/p2-captures/AUDIT-003-admin-invoice-detail-desktop-light.png` : connecté en `admin`, `/invoices/:id` affiche un écran générique **« Une erreur est survenue » + bouton Retour**. Trace réseau : `GET /api/v1/invoices/{id}` → **403** (deux fois). **Conclusion : aucune fuite de donnée financière** — le backend tient. Le défaut réel est l'**incohérence d'UX** : les 18 autres pages financières affichent « Accès non autorisé — Vous n'avez pas les droits nécessaires » (écran `PageRoleGuard`), tandis que `/invoices/:id`, seule page sans garde, tombe sur une erreur générique qui ressemble à une panne. Sévérité à réviser de P1 vers **P2** en P5 : c'est de l'homogénéité, pas une faille. | Ajouter `<PageRoleGuard allowedRoles={ALLOWED_ROLES}>` en réutilisant la constante de `InvoiceListPage.tsx:17`, pour un refus homogène et explicite côté UI. | Voir AUDIT-018 : la vraie fuite ADMIN n'est pas ici mais sur `/documents`, `/workflow/steps` et `/checklist` | **CORRIGÉ (P6, lot V3-D, 2026-07-24)** — *sévérité révisée en P5 (D7) : P1 → P2.* — `PageRoleGuard` ajouté sur `InvoiceDetailPage`, seule page financière sans garde. La liste de rôles est **extraite** dans `constants/invoiceRoles.ts` (`INVOICE_VIEW_ROLES`) et partagée avec `InvoiceListPage` plutôt que dupliquée — deux copies auraient dérivé. Conforme à la révision **P1 → P2** (décision D7) : il n'y avait **aucune fuite** (le backend renvoie bien 403), le défaut était l'écran de refus incohérent. **Preuve runtime** : `admin` sur `/invoices/:id` → « Accès non autorisé » (écran homogène aux 18 autres pages financières), plus « Une erreur est survenue », et **aucune donnée financière rendue** (0 occurrence de `XAF` dans le DOM). **Tests** : `PageRoleGuardsCoverage.test.tsx` — refus ADMIN **et** passage DAF/AA. |
| AUDIT-004 | P1 | workflow / frontend | **P2** (mineur — garde absente, backend couvrant) | `MyDelegationsPage` sans `PageRoleGuard` alors que la Sidebar réserve l'entrée aux validateurs + DAF | `frontend/src/pages/MyDelegationsPage.tsx` (aucun `PageRoleGuard`) · `frontend/src/components/layout/Sidebar.tsx:141-152` (entrée sous garde DAF + 11 validateurs) | Écart nav ⊂ page : un `ROLE_ADMIN` ou `ROLE_ASSISTANT_COMPTABLE` qui saisit `/my-delegations` atteint la page. Le backend protège les appels (`DelegationController`, constante `APPROVER_ROLES` = DAF + 11 validateurs, **sans ADMIN** — vérifié lignes 64-70), donc la page s'affichera vide/en erreur plutôt que de fuiter des données. Défaut d'homogénéité plutôt que faille. | Vide (P1 statique) — à confirmer en P3 | Ajouter un `PageRoleGuard` reprenant exactement la liste de `Sidebar.tsx:141-149`. | — | **CORRIGÉ (P6, lot V3-D, 2026-07-24)** — `PageRoleGuard` ajouté sur `MyDelegationsPage`, avec une liste `DELEGATION_ROLES` reprenant **exactement** celle de la sidebar et celle du backend (`DelegationController:64-70`, `APPROVER_ROLES`) : DAF + 11 validateurs, **ADMIN exclu** (SoD). **Preuve runtime** : `admin` sur `/my-delegations` → écran « Accès non autorisé », formulaire de délégation inatteignable. **Tests** : `PageRoleGuardsCoverage.test.tsx` — refus ADMIN, refus ASSISTANT_COMPTABLE, **et** passage DAF/validateur (les deux sens). |
| AUDIT-005 | P1 | workflow / frontend | **P2** (mineur — fonction inaccessible) | Entrée de navigation « Escalades » invisible pour le DAF : son `RoleGuard` est imbriqué dans le bloc ADMIN | `frontend/src/components/layout/Sidebar.tsx:206-208` (garde interne `['ROLE_ADMIN','ROLE_DAF']`) imbriqué dans le bloc ouvert ligne 188 (`['ROLE_ADMIN']`) et fermé ligne 213 | Le garde externe ADMIN filtre avant le garde interne : la condition `ROLE_DAF` est **morte**. Or `frontend/src/pages/admin/EscalationRulesPage.tsx:57` autorise bien `['ROLE_ADMIN','ROLE_DAF']` et le backend aussi (`EscalationRuleController`, `hasAnyRole('ADMIN','DAF')`). Le DAF dispose donc du droit mais n'a aucun chemin de navigation — fonction atteignable uniquement par URL directe. | **PROUVÉE (P2, 2026-07-22)** — capture `docs/audit/p2-captures/AUDIT-005-daf-sidebar-sans-escalades-desktop-light.png` : la sidebar du `daf` contient **15 liens**, `escalation-rules` **absent** (relevé DOM : `hasEscalation: false`). Capture `AUDIT-005-daf-escalation-rules-accessible-par-url.png` : en saisissant `/admin/escalation-rules`, le DAF atteint la page **pleinement fonctionnelle** (« Règles d'escalade », formulaire « Nouvelle règle », tableau) — aucun refus. La branche `ROLE_DAF` est donc bien **morte** et la fonction n'est atteignable que par URL directe. | Sortir le bloc `<RoleGuard allowedRoles={['ROLE_ADMIN','ROLE_DAF']}>` du bloc ADMIN et le placer au niveau des autres sections. | — | **CORRIGÉ (P6, lot V3-D, 2026-07-24)** — ⚠ code relu avant correction : la sidebar ayant été modifiée en **V2-A**, l'entrée se trouvait en **208-210** (et non 206-208), toujours imbriquée dans le bloc ADMIN ouvert ligne **189** — le finding tient, les numéros de ligne non. Le bloc `<RoleGuard allowedRoles={['ROLE_ADMIN','ROLE_DAF']}>` est sorti du bloc ADMIN et placé au niveau des autres sections : son garde `ROLE_DAF`, jusque-là **code mort** (le garde ADMIN externe filtrait avant lui), est enfin effectif. **Preuve runtime** : la sidebar du `daf` passe de **15 liens sans `escalation-rules`** (relevé P2) à **17 liens avec** ; contrôle de non-sur-ouverture : `/admin/users` et `/admin/security` restent **absents** pour le DAF, et les entrées financières restent absentes pour l'ADMIN. **Tests** : `SidebarNavigationGuards.test.tsx` (5 tests, dont la non-régression SoD dans les deux sens). |
| AUDIT-006 | P1 | supplier-portal | **P3** (dette — conformité au style ; révisé P2→P3 en P5, décision D7) | Seul `try/catch` brut des 40 contrôleurs : requalifie les erreurs métier en HTTP 500 | `src/main/java/com/oct/invoicesystem/domain/supplier/controller/SupplierPortalController.java:220-240` (`catch (Exception e)` → `throw new RuntimeException(...)`) | Violation directe de CLAUDE.md §3 (« tout passe par `GlobalExceptionHandler`, pas de try/catch brut en contrôleur »). Le `catch (Exception e)` capture aussi les `ValidationException` métier levées par `supplierService.uploadDocument` (fichier vide, >10 Mo, MIME refusé) et les relance en `RuntimeException`, qui tombe dans le handler générique (`GlobalExceptionHandler.java:142`) → **500 au lieu de 400/413**. Le fournisseur reçoit « une erreur inattendue » au lieu du motif réel. | ⚠ **HYPOTHÈSE RÉFUTÉE EN P3 (2026-07-22)** — le comportement redouté ne se produit pas. Upload d'un PDF de **12 582 927 octets** (12 Mo) : au portail → **HTTP 413** `{"message":"Uploaded file exceeds the maximum allowed size"}` ; en interne (AA) → **HTTP 413**, même message. Contrôle MIME également testé : un exécutable Windows (en-tête `MZ`) renommé `facture.pdf` et déclaré `application/pdf` est **refusé en 400** — `{"message":"Unsupported MIME type: application/x-msdownload"}` — au portail comme en interne. Apache Tika fait donc bien son travail, et la limite de taille est interceptée **avant** d'atteindre le `try/catch`. **Le défaut de code reste réel** (le `catch (Exception e)` de `SupplierPortalController:220-240` est bien le seul try/catch brut des 40 contrôleurs et viole CLAUDE.md §3), **mais son impact runtime supposé n'existe pas** sur les cas testés. Sévérité à abaisser de P2 vers **P3 (dette de conformité au style, sans effet observable)** en P5. | Correction inchangée sur le fond (supprimer le try/catch brut), mais la priorité tombe : c'est de la mise en conformité, pas une correction de bug. | — | OUVERT — **sévérité révisée en P5 (D7) : P2 → P3** (hypothèse réfutée en P3, aucun 500 constaté ; mise en conformité CLAUDE.md §3) → lot V4-D |
| AUDIT-007 | P1 | invoice / documents | **P2** (mineur — cloisonnement inter-départements) | Documents de facture énumérables et téléchargeables par tout utilisateur interne, sans contrôle de département | `src/main/java/com/oct/invoicesystem/domain/invoice/controller/InvoiceDocumentController.java:65-69` (`list`) et `:76-89` (`download`), tous deux `@PreAuthorize("isAuthenticated() and !hasRole('SUPPLIER')")` | Le seul garde-fou est « pas SUPPLIER » : **ADMIN passe ce filtre**, ainsi que n'importe quel validateur d'un autre département. `listByInvoice` (`InvoiceDocumentService.java:198`) ne filtre que par `invoiceId`. À décharge, `generateDownloadUrlAndLog` (`:219`) utilise bien `findByIdAndInvoiceId`, donc pas de confusion document↔facture, et chaque téléchargement est journalisé (`DocumentAccessLog`, append-only). Le défaut est l'absence de contrôle de périmètre, pas un IDOR de couple d'identifiants. | **PARTIELLEMENT PROUVÉE (P2, 2026-07-22)** — volet ADMIN confirmé **et aggravé** : avec un jeton `admin`, `GET /api/v1/invoices/{id}/documents` → **200** (métadonnées des pièces jointes) et `GET …/documents/{docId}/download` → **200** renvoyant une **URL MinIO présignée `X-Amz-Expires=900`** (15 min) donnant accès au PDF de la facture. Ce n'est donc pas une simple énumération. **Reste à prouver en P3** : le volet inter-départements (validateur d'un autre département). Détail complet et sondage des 10 endpoints en **AUDIT-018**. **VOLET INTER-DÉPARTEMENTS DÉSORMAIS PROUVÉ (P3, 2026-07-22)** : facture `FAC-2026-00030` du département **Informatique** ; jeton `drh` (validateur N1 **DRH**, autre département, sans aucun rôle sur cette facture). Résultats — `GET /invoices/{id}` → **403** (le cloisonnement tient sur la facture) **mais** `GET /invoices/{id}/documents` → **200** (métadonnées : nom de fichier, taille, empreinte SHA-256), `GET /invoices/{id}/documents/{docId}/download` → **200** renvoyant une **URL MinIO présignée `X-Amz-Expires=900`** donnant accès au PDF, et `GET /invoices/{id}/workflow/steps` → **200** avec le circuit nominatif (`aa2` / « Bernard Comptable », `rsi`, `dsi`, dates et commentaires). Reproduit à l'identique avec le jeton `com` (N1 Communication) sur la même facture. **L'incohérence est flagrante : le système refuse la facture à ce validateur mais lui livre ses pièces jointes et son circuit d'approbation.** | Aligner sur `InvoiceController` en excluant ADMIN (`!hasRole('ADMIN')`) et ajouter un contrôle de périmètre départemental côté service, cohérent avec `DepartmentAccess`. Les 3 endpoints doivent appliquer **la même règle que `GET /invoices/{id}`** — c'est le test de non-régression le plus simple : « si la facture est refusée, ses documents et son circuit le sont aussi ». | Recoupe AUDIT-003 · **repris et étendu par AUDIT-018** (mêmes endpoints + `/workflow/steps` + `/checklist`) | **CORRIGÉ (P6, lot V2-A, 2026-07-23)** — corrigé **avec AUDIT-018** (qui l'étend) : une seule correction pour les deux, cf. la ligne AUDIT-018. La règle de non-régression du finding est exactement celle qui a été implémentée : « si `GET /invoices/{id}` refuse, ses documents et son circuit refusent aussi » — même garde de contrôleur, même `getByIdScoped` de service. **Preuve** : les tests « validateur d'un autre département → 403 » d'`InvoiceSurfacesSodTest` et d'`InvoiceDocumentControllerTest`. |
| AUDIT-008 | P1 | purchasing / matching | **P2** (mineur — SoD ADMIN sur un contrôle financier) | ADMIN modifie **seul** les seuils de tolérance du rapprochement 3-way | `src/main/java/com/oct/invoicesystem/domain/purchasing/controller/MatchingConfigController.java:46` (`POST`, `hasRole('ADMIN')`) et `:37` (`GET`, `hasAnyRole('ADMIN','ASSISTANT_COMPTABLE')`) · page `frontend/src/pages/admin/AdminMatchingConfigPage.tsx:80` (ADMIN) | Les seuils de tolérance et l'exigence de GRN sont **le** paramètre qui décide si un écart de facturation passe ou bloque. Les confier à ADMIN seul — rôle par ailleurs privé de tout accès financier — permet à un administrateur technique de relâcher un contrôle financier sans qu'aucun rôle financier (DAF) ne soit dans la boucle. À trancher au regard du cahier des charges : paramétrage « technique » assumé, ou contrôle financier à cosigner par le DAF ? | Vide (P1 statique) | Décision en P5. Piste : passer l'écriture à `hasAnyRole('ADMIN','DAF')` ou à DAF seul, et tracer le changement au journal d'audit financier. | — | **CORRIGÉ (P6, lot V2-A, 2026-07-23 — décision D5)** — l'écriture des seuils passe à `hasRole('DAF')` et la lecture à `hasAnyRole('DAF','ASSISTANT_COMPTABLE')` : l'ADMIN n'a plus **aucune** surface sur ce contrôle financier. Côté interface, `AdminMatchingConfigPage` passe en `PageRoleGuard ROLE_DAF` et l'entrée « Rapprochement » sort du bloc ADMIN de la sidebar pour rejoindre la section DAF. **Preuve** : `MatchingConfigControllerSodTest` (nouveau — l'un des 3 contrôleurs non testés d'AUDIT-013) : ADMIN → 403 en écriture **et** en lecture, avec `verify(never()).updateConfig` prouvant qu'aucun relâchement n'atteint le service ; AA → 403 en écriture. |
| AUDIT-009 | P1 | retention / compliance | **P2** (mineur — SoD ADMIN sur pièces financières) | ADMIN décide seul de la **purge** d'un document, y compris pièce justificative de facture | `src/main/java/com/oct/invoicesystem/domain/retention/controller/RetentionDispositionController.java:39` (`PUT …/disposition`, `hasRole('ADMIN')`) et `:32` · `RetentionPolicyController.java:30/37/44` (ADMIN) | La disposition `PURGED` détruit une pièce justificative. Le pouvoir de destruction appartient donc entièrement à un rôle réputé sans accès financier, alors que CLAUDE.md §3 impose le soft-delete des enregistrements financiers et que `V33__enforce_financial_retention.sql` verrouille la suppression dure en base. Tension entre « ADMIN = gardien de la rétention » (défendable) et « ADMIN ≠ financier ». | Vide (P1 statique) | Décision en P5. Piste : double validation (ADMIN propose, DAF confirme) sur la disposition `PURGED` uniquement ; la politique de rétention peut rester ADMIN. | AUDIT-008 (même arbitrage SoD) | **CORRIGÉ (P6, lot V2-A, 2026-07-23 — décision D5)** — double validation implémentée par un nouvel état `PURGE_PROPOSED` : l'ADMIN propose (`RETAINED` ou `PURGE_PROPOSED`), le **DAF seul** confirme `PURGED`, et uniquement sur un document déjà proposé — proposeur et confirmateur sont donc toujours deux rôles distincts. Le listing des documents en attente inclut les purges proposées, sans quoi le DAF n'aurait rien à confirmer. **Aucune migration** : la colonne est un `VARCHAR(20)` sans contrainte `CHECK` (vérifié dans `V11`). 4 clés i18n ajoutées dans les 2 catalogues, dont `retention.disposition.invalid_target` qui était **absente des deux** (même famille qu'AUDIT-039). **Preuve** : `RetentionDispositionServiceTest` — le chemin nominal à deux mains, plus 3 refus : « l'ADMIN ne purge pas seul », « le DAF ne purge pas ce qui n'a pas été proposé », « le DAF ne propose pas sa propre purge ». |
| AUDIT-010 | P1 | transverse / API | **P2** (mineur — robustesse / divulgation de schéma) | Paramètre `sort` repris du client sans liste blanche (3 sites), atteignable depuis le portail fournisseur | `src/main/java/com/oct/invoicesystem/domain/invoice/controller/InvoiceController.java:182-186` · `src/main/java/com/oct/invoicesystem/domain/invoice/service/InvoiceService.java:247-252` · `src/main/java/com/oct/invoicesystem/domain/department/service/DepartmentService.java:33-39` | `sort.split(",")[0]` est passé tel quel à `Sort.by`. Pas d'injection SQL (Spring Data valide la propriété contre le métamodèle), mais un nom de propriété arbitraire provoque une 500 divulguant la structure de l'entité, et une valeur de type `relation.sousRelation` déclenche des jointures non prévues. `InvoiceService.java:247` est atteint depuis `SupplierPortalController`, donc exposé à un compte SUPPLIER. `sortParams[0]` sur une chaîne vide lève en plus une exception non métier. | **PROUVÉE (P3, 2026-07-22), avec une nuance qui corrige l'hypothèse P1.** (a) `GET /api/v1/invoices?sort=nimportequoi` → **HTTP 500** `{"message":"An unexpected error occurred"}`. (b) `GET /api/v1/departments?sort=zzz` → **500** (le second site est confirmé). (c) **Atteignable depuis le portail fournisseur** : `GET /api/v1/supplier/invoices?sort=zzz` avec un jeton `SUPPLIER` → **500** — un compte externe peut donc provoquer l'erreur, ce qui était le point le plus sensible du finding. (d) ⚠ **`?sort=` vide ne casse PAS** : `GET /invoices?sort=` → **200** normal, de même que `/departments?sort=` → 200. L'hypothèse P1 « `sortParams[0]` sur chaîne vide lève une exception » est donc **fausse** — seul le nom de propriété inconnu déclenche la 500. (e) Un chemin de relation valide fonctionne (`?sort=department.nameFr,asc` → 200), confirmant que des jointures non prévues sont bien atteignables. **Pas de divulgation de schéma dans la réponse** (`include-stacktrace: never` tient, message générique) : le défaut est la 500 elle-même, pas une fuite. | Liste blanche des colonnes triables par endpoint, repli silencieux sur le tri par défaut si la valeur est inconnue. Le cas « chaîne vide » est déjà couvert et n'a pas besoin d'être traité. | — | **CORRIGÉ (P6, lot V2-E, 2026-07-23)** — nouvel utilitaire partagé `SortWhitelist` appliqué aux **3** sites (plutôt que 3 implémentations vouées à diverger) : liste blanche de champs **scalaires** par endpoint, repli **silencieux** sur le tri par défaut. Le repli est délibéré : le tri est une préoccupation de présentation, refuser la requête entière serait une réponse plus dure que l'erreur de l'appelant. Conformément à la mesure P3, le cas `?sort=` **vide n'est pas traité** — il renvoyait déjà 200, l'hypothèse P1 inverse étant fausse. **Preuve** : `SortWhitelistTest` (champ inconnu et chemin de relation → repli ; champ autorisé honoré, sans quoi une liste blanche qui aplatit tout passerait) et `SortAndPendingQueueIntegrationTest` — les 3 sites HTTP **plus le chemin portail fournisseur**, qui était le point le plus sensible du finding (compte externe). |
| AUDIT-011 | P1 | infrastructure / sécurité | **P2** (mineur — exposition de configuration) | `/actuator/**` en `permitAll()` : `metrics` et `info` accessibles sans authentification | `src/main/java/com/oct/invoicesystem/config/SecurityConfig.java:66` (`.requestMatchers("/actuator/**").permitAll()`) · `src/main/resources/application.yaml:127` (`include: health,info,metrics`) · `:140` (`env.enabled: true`) | `health` protège son détail (`show-details: when-authorized`), mais `metrics` et `info` restent ouverts à un anonyme : noms de beans, métriques JVM/HTTP, éventuellement chemins d'URI. Surface de reconnaissance inutile pour un système financier exposé. | **PROUVÉE (P3, 2026-07-22)** — requêtes **sans aucun en-tête `Authorization`** : `GET /actuator/health` → **200** `{"status":"UP"}` · `GET /actuator/info` → **200** (nom de l'application, version 1.0.0, description) · `GET /actuator/metrics` → **200** avec la **liste complète des noms de métriques** (`hikaricp.connections.*`, `executor.*`, `disk.free`, `disk.total`, `jvm.*`, `http.server.requests`…) · `GET /actuator/metrics/jvm.memory.used` → **200** avec la **valeur mesurée** (`3.63986144E8` octets) et les étiquettes disponibles. Un anonyme obtient donc la version applicative, la topologie du pool de connexions et l'état mémoire de la JVM. ✅ **À décharge, mesuré aussi** : `/actuator/env` et `/actuator/beans` renvoient bien **404** (non exposés) — la fuite est circonscrite à `health`/`info`/`metrics`, conformément au `include` de `application.yaml:127`. | Restreindre `/actuator/**` à ADMIN, en gardant `/actuator/health` en `permitAll` pour la sonde Docker (le conteneur `oct_backend` est marqué `healthy` par ce point d'entrée : le fermer casserait le healthcheck). Réduire `include` à `health` seul en production. | — | **CORRIGÉ (P6, lot V2-D, 2026-07-23)** — `/actuator/**` passe à `hasRole('ADMIN')`. **Le point de vigilance du lot est respecté** : `/actuator/health` (et `/actuator/health/**`) reste explicitement en `permitAll`, car le healthcheck du conteneur `oct_backend` en dépend ; son détail est de toute façon déjà protégé par `show-details: when-authorized`. **Preuve** : `ActuatorExposureTest` (6 tests) — anonyme → **401** sur `/actuator/metrics`, `/actuator/info` et `/actuator/metrics/jvm.memory.used` (les trois exactement mesurés en P3) ; anonyme → **200** sur `/actuator/health` (non-régression du healthcheck) ; AA authentifié → 403 ; et la contre-preuve ADMIN → 200, sans laquelle supprimer purement l'actuator satisferait le test. |
| AUDIT-012 | P1 | infrastructure / secrets | **P2** (mineur — secret par défaut actif en production) | Secret MinIO avec valeur par défaut en dur dans le profil **partagé** (donc actif en prod si la variable manque) | `src/main/resources/application.yaml:105` (`secret-key: ${MINIO_SECRET_KEY:dev-minio-secret-change-me}`) | Contrairement à `JWT_PRIVATE_KEY`/`JWT_PUBLIC_KEY`/`ENCRYPTION_KEY` (`:83-84`, `:91`) qui n'ont **aucune** valeur par défaut, le secret MinIO en a une, et hors du bloc `dev` : si `MINIO_SECRET_KEY` n'est pas défini en production, le stockage documentaire démarre avec un secret public connu. Viole CLAUDE.md §3 (« jamais de secret en dur »). | Vide (P1 statique) | Retirer la valeur par défaut (`${MINIO_SECRET_KEY}`) pour que l'absence fasse échouer le démarrage, et ne garder un défaut que dans le bloc `dev`. | — | **CORRIGÉ (P6, lot V2-D, 2026-07-23)** — le défaut `dev-minio-secret-change-me` est retiré du bloc **partagé** : `secret-key: ${MINIO_SECRET_KEY}` sans repli, donc l'absence de la variable fait désormais **échouer le démarrage**, à l'identique de `JWT_PRIVATE_KEY` et `ENCRYPTION_KEY`. Le défaut de confort local est déplacé dans le **seul** profil `dev` ; le profil `test` a déjà ses propres identifiants dans `src/test/resources/application-test.yml`. **Preuve** : `MinioSecretHasNoSharedDefaultTest` — lit le YAML **packagé** (et non la propriété résolue, qu'un profil de test renseignerait de toute façon, ce qui ne prouverait rien) pour vérifier que le bloc partagé n'a plus de repli, **et** que le profil `dev` l'a toujours. |
| AUDIT-013 | P1 | tests | **P2** (mineur — dette de tests sur des surfaces sensibles) | 14 contrôleurs sur 40 sans aucune couverture de test, dont `AuthController` et `SupplierController` | Contrôleurs sans aucune référence dans `src/test` (recherche littérale) : `AuthController`, `SupplierController`, `SupplierRelationshipController`, `AnnouncementController`, `ArchiveFolderController`, `BackupController`, `ChecklistTemplateController`, `GoodsReceiptController`, `IntegrationConnectorController`, `InvoiceChecklistController`, `MatchingConfigController`, `PaymentAlertRuleController`, `RoleController`, `SecurityHealthController` | CLAUDE.md §3 impose « un test d'intégration pour chaque endpoint de contrôleur ». `AuthController` (login, MFA/TOTP, réinitialisation de mot de passe) est la surface la plus sensible du système et n'a aucun test ; `MatchingConfigController` porte le contrôle financier d'AUDIT-008 ; `SupplierController` gère des coordonnées bancaires chiffrées. La suite est verte (628 tests) mais aveugle sur ces chemins. | Vide (P1 statique) | Prioriser en P6 : d'abord `AuthController` (login + MFA + verrouillage après 5 échecs), puis `MatchingConfigController` et `SupplierController`. | AUDIT-002 (le test « PO d'un autre fournisseur » relève du même manque) | OUVERT — **cadrage tranché en P5 (D8)** : rattrapage **ciblé** (`AuthController`, `MatchingConfigController`, `SupplierController`) + 1 test par finding corrigé → lot V5-A ; les 11 autres contrôleurs = dette assumée (`docs/TASKS.md §A`) |
| AUDIT-014 | P1 | transverse / frontend | **P3** (dette — gestion d'erreurs muette) | ~14 fichiers avec des mutations `apiClient` sans aucun `onError` : les échecs sont silencieux | `frontend/src/pages/admin/AdminCompliancePage.tsx:30,33,34,37,38,40,43,44,46` (10 mutations, 0 gestion) · `frontend/src/pages/ReportsPage.tsx:164,168` (exports) · `frontend/src/components/admin/IntegrationConnectors.tsx:35,39,43,47,51` · `frontend/src/components/archive/ArchiveFolderTree.tsx:33,42,50` · `frontend/src/pages/admin/AdminAccessRequestsPage.tsx:39` · `frontend/src/pages/NotificationsPage.tsx:49,54` · `frontend/src/pages/supplier/SupplierProfilePage.tsx:32` · et 7 autres | Aucun `onError` ni rendu `isError` : en cas d'échec serveur, l'utilisateur ne reçoit **aucun retour** — l'action semble avoir réussi. Particulièrement gênant sur les exports de rapports et sur l'approbation/refus des demandes d'accès. | **CONFIRMÉE (P3, 2026-07-22) — et le comportement observé est pire que « silencieux ».** Protocole : `docker stop oct_backend`, puis navigation en session `aa` authentifiée. (a) Sur `/notifications` : la console émet `Failed to load resource: net::ERR_CONNECTION_REFUSED @ /api/v1/profile`, l'application **déconnecte l'utilisateur et le renvoie sur `/login`** sans aucun message — une panne serveur est traitée comme une expiration de session (capture `docs/audit/p3-captures/AUDIT-backend-down-deconnexion.png`). (b) Pire, sur l'écran de connexion lui-même, une tentative avec des identifiants **corrects** affiche « **Identifiants incorrects** » (capture `AUDIT-login-identifiants-incorrects-alors-que-serveur-down.png`) : l'utilisateur est conduit à croire qu'il s'est trompé de mot de passe alors que le serveur est arrêté. Ce message trompeur est le défaut le plus visible du lot et concerne **tous les utilisateurs** dès la première seconde d'une panne. **⚠ AMPLEUR RÉELLE MESURÉE EN P4 (2026-07-23) — très supérieure à l'estimation P1.** Dénombrement exhaustif par analyse syntaxique de chaque bloc `useMutation({...})` de `frontend/src` : **95 mutations au total, dont 84 sans aucun `onError` (88 %)**, réparties sur **42 fichiers** — et non « ~14 fichiers » comme estimé en P1. En affinant : 22 fichiers (44 mutations) disposent d'une gestion alternative (`isError` au rendu ou `try/catch`), mais **20 fichiers totalisant 40 mutations n'ont strictement aucune gestion d'erreur** — l'échec y est 100 % silencieux : `api/suppliers.ts` (6), `components/admin/IntegrationConnectors.tsx` (5), `components/archive/ArchiveFolderTree.tsx` (3), `components/supplier/SupplierRelationship.tsx` (3), puis 16 fichiers à 1-2 mutations. **Cas illustratif vérifié par lecture** : `AdminAccessRequestsPage.tsx:39-45` n'a qu'un `onSettled`, qui s'exécute **identiquement en succès et en échec** — la liste est rafraîchie, la demande reste « en attente » et l'administrateur ne reçoit aucun message ; l'échec est indistinguable d'un succès sans effet. À noter que le backend, lui, répond correctement (`PATCH /access-requests/{id}` → **400** avec un message traduit « Échec de la validation. ») : le défaut est entièrement côté frontend. | Deux corrections distinctes. (1) Distinguer, dans l'intercepteur axios, l'échec réseau (`!error.response`) du 401 métier : n'effacer la session que sur 401, et afficher une bannière « service indisponible » sinon. (2) Sur `LoginPage`, n'afficher « Identifiants incorrects » que si la réponse porte effectivement un 401 ; en l'absence de réponse, afficher un message de service injoignable. Puis ajouter les `onError` manquants sur les ~14 fichiers listés. | AUDIT-015 (même famille) · le volet (2) est **AUDIT-035** · **chiffrage exact ajouté en P4** | **CORRIGÉ (P6, lot V3-B, 2026-07-24)** — ⚠ **diagnostic re-dérivé depuis le code** : le finding attribuait la déconnexion à l'intercepteur axios. Vérification faite, l'intercepteur ne se déclenche que sur `error.response?.status === 401` (`apiClient.ts:52`) — une panne réseau (`!error.response`) n'y entre **jamais**. La cause réelle est `App.tsx` : le `.catch(() => ...)` de la réhydratation `GET /profile` avalait **toute** erreur, ERR_CONNECTION_REFUSED compris, et purgeait la session. Correction : nouvel utilitaire `lib/apiError.ts` (`isNetworkError` / `hasStatus` / `getApiErrorKey` / `getApiErrorMessage`) ; seule une **vraie** rejection d'authentification purge le jeton, une panne affiche `OfflineBanner` (service indisponible + bouton Réessayer) **session intacte**. Volet `onError` : composant `ErrorToaster` monté une fois + `onError: notifyApiError` sur **33 mutations** dans les **15** fichiers qui n'avaient aucune gestion — mesure après correction : **95 mutations au total, 0 fichier sans aucune gestion** (les 50 restantes sans `onError` ont un `isError` au rendu ou un `try/catch`). Aucune dépendance ajoutée. **Preuve runtime** (`docker stop oct_backend`, session `daf`) : sur `/notifications`, bannière « Service indisponible », `accessToken` **et** `refreshToken` **conservés**, aucun renvoi vers `/login`. **Tests** : `apiError.test.ts` (6) + `SessionSurvivesOutage.test.tsx` (2) ; **contre-preuve** — le test de conservation de session échoue quand la branche `isNetworkError` est retirée. |
| AUDIT-015 | P1 | i18n / frontend | **P3** (dette — message non traduit) | 11 sites affichent le message d'erreur backend **brut**, sans le passer par `t()` | `frontend/src/pages/GoodsReceiptsPage.tsx:177` · `frontend/src/pages/InvoiceCreatePage.tsx:399` · `frontend/src/pages/PaymentsPage.tsx:138` · `frontend/src/pages/supplier/SupplierInvoiceSubmitPage.tsx:289` · `frontend/src/pages/admin/AdminDelegationsPage.tsx:102` · `frontend/src/pages/admin/AdminMatchingConfigPage.tsx:60` · `frontend/src/pages/admin/SecuritySettingsPage.tsx:80` · `frontend/src/pages/MyAccessRequestsPage.tsx:76` · `frontend/src/pages/MyDelegationsPage.tsx:54` · `frontend/src/components/matching/MatchingLineResolveModal.tsx:34` · `frontend/src/pages/admin/AdminUsersPage.tsx:102` | Le backend renvoie des **clés i18n** comme messages d'erreur (règle PROB-006) ; ces sites les affichent telles quelles → l'utilisateur voit `error.payment.only_bon_a_payer` au lieu d'une phrase. Le motif correct existe déjà dans le code : `frontend/src/components/invoice/InvoiceActionPanel.tsx:174-180` (traduit, et ne retombe sur le brut que si `t(key) === key`), repris aussi en `PaymentsPage.tsx:505` — le même fichier est donc incohérent avec lui-même. | **PARTIELLEMENT RÉFUTÉE (P3, 2026-07-22) — la prémisse « le backend renvoie des clés i18n comme messages d'erreur » est FAUSSE pour les erreurs.** Sondage direct de l'API : toutes les erreurs métier testées reviennent **déjà traduites**, et respectent `Accept-Language` — `Accept-Language: fr` → « *Un paiement ne peut être enregistré que pour les factures au statut BON_A_PAYER.* » ; `Accept-Language: en` → « *Payment can only be recorded for invoices in BON_A_PAYER status.* ». Idem « *La facture doit contenir au moins un document* », « *Seules les factures BROUILLON peuvent être supprimées.* », « *Vous n'avez pas la permission d'effectuer cette action.* ». **En revanche, les messages de SUCCÈS sont bien des clés brutes** : `action.submit.success`, `action.assign.success`, `action.validate.success`, `action.bon_a_payer.success`, `action.reject.success`, `payment.recorded.success`, `payment.processed.success`. Le risque d'affichage brut se déplace donc des erreurs vers les **confirmations**. ✅ Vérification runtime en interface (validation N1 jouée par `drh` sur `FAC-2026-00032`) : **aucune clé brute affichée à l'écran** — le frontend traduit correctement sur ce chemin. Le finding subsiste comme **dette de robustesse** (11 sites qui afficheraient la clé si elle n'était pas dans le catalogue), pas comme défaut visible. | Correction inchangée (utilitaire partagé), mais priorité abaissée : à traiter en même temps que le lot i18n. Vérifier au passage que les 7 clés de succès listées ci-dessus existent bien côté frontend. | AUDIT-014 | OUVERT |
| AUDIT-016 | P1 | frontend / dette | **P3** (dette — code mort et incohérences mineures) | Export mort `StaffRoute`, rôle fantôme `ROLE_VALIDATEUR_N1_FIN`, `any` en signature publique | `frontend/src/components/auth/ProtectedRoute.tsx:49` (`StaffRoute` défini, jamais importé) · `frontend/src/pages/ProfilePage.tsx:42` (`ROLE_VALIDATEUR_N1_FIN`) · `frontend/src/api/suppliers.ts:45` (`filters: Record<string, any>`) · 7 casts `as any` sur des erreurs axios | `ROLE_VALIDATEUR_N1_FIN` a été supprimé du système — `frontend/src/constants/roles.ts:4` et `V5__seed_roles_and_admin.sql:5` le documentent explicitement — mais il subsiste dans la liste `MFA_REQUIRED_ROLES` de `ProfilePage`. Sans effet fonctionnel (aucun utilisateur ne porte ce rôle), mais c'est une incohérence avec la source de vérité. `StaffRoute` est du code mort. | Vide (P1 statique) | Supprimer `StaffRoute`, retirer `ROLE_VALIDATEUR_N1_FIN` de `ProfilePage.tsx:42`, typer `useSuppliers`, introduire un type `ApiError` partagé pour éliminer les 7 casts. | — | **CORRIGÉ (P6, lot V2-C, 2026-07-23)** — les **3** volets, pas seulement `StaffRoute`. (1) `StaffRoute` supprimé : il était le correctif manquant d'AUDIT-026, mais une fois la règle de `ProtectedRoute` inversée il ferait doublon, donc une occasion de plus de diverger. (2) `ROLE_VALIDATEUR_N1_FIN` retiré de `MFA_REQUIRED_ROLES` (`ProfilePage.tsx`) : rôle supprimé du système, aucun porteur, mais il contredisait la source de vérité (`constants/roles.ts`, `V5`). (3) Nouveau type `ApiError` + helper `apiErrorMessage` : les **7** casts `as any` de production disparaissent, et `useSuppliers` prend un `SupplierFilters` typé au lieu de `Record<string, any>` en signature publique. **Preuve** : `tsc --noEmit` à 0 erreur, frontend 257/257, et plus aucun `as any` hors tests. |
| AUDIT-017 | P1 | documentation | **P3** (cohérence documentaire) | `CLAUDE.md` en retard sur le code : étape `EN_CONTROLE_AA` absente du §5 et override de matching décrit « DAF ou ADMIN » | `CLAUDE.md §5` (workflow sans `EN_CONTROLE_AA`) · `CLAUDE.md §9` (« ROLE_DAF ou ROLE_ADMIN » pour l'override) vs code `src/main/java/com/oct/invoicesystem/domain/invoice/controller/InvoiceController.java:322` (`@PreAuthorize("hasRole('DAF')")`) — la Javadoc de cette même méthode (`:324`) répète l'erreur « DAF or Admin » | Deux écarts doc/code. Pour l'override, le **code fait foi** et est conforme à la SoD (ADMIN ≠ financier) : c'est la documentation qui est fausse, pas le code — la corriger en ouvrant l'accès à ADMIN serait une régression de sécurité. Pour `EN_CONTROLE_AA`, `docs/WORKFLOW.md:49-50` documente bien l'étape ; seul `CLAUDE.md §5` est périmé. | Sans objet (documentaire) | Mettre `CLAUDE.md §5` à jour avec `SOUMIS → EN_CONTROLE_AA → EN_VALIDATION_N1`, corriger `CLAUDE.md §9` en « DAF uniquement », et corriger la Javadoc `InvoiceController.java:324`. | — | OUVERT |
| AUDIT-018 | P2 | invoice / workflow / documents — **SoD ADMIN** | **P1** (majeur — SoD, accès financier effectif de l'ADMIN) | L'ADMIN obtient le **circuit d'approbation nominatif** et une **URL de téléchargement signée du PDF de facture**, malgré l'exclusion financière | `src/main/java/com/oct/invoicesystem/domain/workflow/controller/ApprovalController.java` (`GET /invoices/{invoiceId}/workflow/steps`) · `src/main/java/com/oct/invoicesystem/domain/invoice/controller/InvoiceDocumentController.java:65-69` (`list`) et `:76-89` (`download`) · `src/main/java/com/oct/invoicesystem/domain/checklist/controller/InvoiceChecklistController.java` (`GET /invoices/{id}/checklist`) | Ces endpoints se gardent par `isAuthenticated() and !hasRole('SUPPLIER')` : le filtre exclut le fournisseur mais **laisse passer l'ADMIN**, alors que `InvoiceController` l'exclut explicitement (`!hasRole('ADMIN')`). Sondage runtime de 10 endpoints financiers avec le compte `admin` : 7 renvoient bien **403** (`/invoices`, `/invoices/{id}`, `/history`, `/matching`, `/reports/kpis`, `/payments`) mais **3 renvoient 200** — `/documents`, `/workflow/steps`, `/checklist`. `/workflow/steps` expose qui a validé, à quelle date et les commentaires (« OK », « BAP N6 ») ; `/documents/{docId}/download` renvoie une **URL MinIO présignée valide 15 min** donnant accès au PDF de la facture. L'ADMIN accède donc en pratique à de la donnée financière, ce que la matrice SoD (`AUDIT_SYSTEM_MODEL.md §5`) interdit. | **Prouvée (P2)** : `docs/audit/p2-captures/AUDIT-003-admin-invoice-detail-desktop-light.png` + trace réseau de la session (`GET /workflow/steps` → 200 avec le circuit nominatif ; `GET /documents/{id}/download` → 200 + URL présignée `X-Amz-Expires=900`). Sondage des 10 endpoints reproductible avec un jeton `admin`. | Exclure ADMIN sur les trois surfaces (`!hasRole('ADMIN')`, comme `InvoiceController`), puis ajouter un contrôle de périmètre départemental côté service. Couvrir par un test d'intégration « ADMIN → 403 » sur `/documents`, `/workflow/steps` et `/checklist`. | **Étend AUDIT-007** (qui ne visait que `/documents` et sans preuve runtime) ; même principe que AUDIT-003 | **CORRIGÉ (P6, lot V2-A, 2026-07-23)** — les 3 surfaces (`/documents`, `/documents/{id}/download`, `/workflow/steps`) plus `/checklist` (GET **et** POST) appliquent désormais la règle de la facture elle-même : `!hasRole('ADMIN')` au contrôleur **et** `invoiceService.getByIdScoped()` au service, exactement comme `InvoiceController`. **Preuve** : `InvoiceSurfacesSodTest` (6 tests) — ADMIN → 403 et validateur d'un autre département → 403 sur `/workflow/steps` et `/checklist`, avec `verify(never())` prouvant que le circuit nominatif n'est même pas construit ; `InvoiceDocumentControllerTest` — les 2 tests qui encodaient le défaut (`list_AsAdmin_Returns200`, `download_AsAdmin_Returns200`) sont devenus des 403, et `verify(never()).generateDownloadUrlAndLog` prouve qu'**aucune URL MinIO présignée n'est plus émise**. |
| AUDIT-019 | P2 | frontend / layout — **responsive** | **P1** (majeur — application inutilisable sur mobile) | Aucun comportement responsive : la sidebar reste à 256 px sur mobile et réduit le contenu à 134 px de large | `frontend/src/components/layout/Sidebar.tsx:93` (`<aside className="w-64 flex flex-col shrink-0 …">`) · `frontend/src/layouts/SupplierLayout.tsx:45` (**même classe, même défaut**) · `frontend/src/components/layout/AppShell.tsx:7-9` (`flex h-screen`, aucun breakpoint) | La largeur `w-64` (256 px) est **inconditionnelle** : aucun breakpoint Tailwind (`sm:`, `md:`, `lg:`) ni classe `hidden` dans les deux layouts, et aucun bouton menu (« hamburger ») n'existe. À 390 px de viewport, la sidebar consomme 256 px et `main` tombe à **134 px** : titres coupés, références de facture rendues sur 3 lignes, tableaux illisibles. Le défaut touche **les 50 pages authentifiées** (staff + portail fournisseur). À décharge, les pages publiques (`/login`) sont correctement responsives : le défaut est circonscrit aux deux layouts. | **Prouvée (P2)** : `docs/audit/p2-captures/daf-invoices-mobile-dark.png` (sidebar pleine largeur, contenu écrasé) · mesure DOM à 390 px : `{viewport:390, asideWidth:256, mainWidth:134, asidePosition:"static", hamburger:false}` · contre-preuve `docs/audit/p2-captures/public-login-mobile-light.png` (login responsive). **Quantifié sur les 992 observations** : 92 des 94 pages mobiles ont des éléments hors viewport (pire cas : le tableau de `/invoices` mesure **1190 px pour 390 px** de large). **Cas aggravé** : `/admin/department-access` en mobile porte 9 tableaux de ~340 px dans des conteneurs de **36 px** en `overflow-x: visible` — seule page où aucun conteneur scrollable ne rattrape le débordement, le contenu est donc réellement inatteignable. | Rendre la sidebar repliable : `hidden md:flex` sur l'`<aside>` + tiroir déclenché par un bouton menu dans le `Header`, en factorisant le comportement entre `AppShell` et `SupplierLayout` (les deux portent la même classe, à corriger ensemble). | — | **CORRIGÉ (P6, lot V3-A, 2026-07-24)** — les deux layouts (`AppShell` staff et `SupplierLayout` portail) portaient la même classe `w-64` inconditionnelle : ils sont corrigés **ensemble** via une enveloppe commune `SidebarShell` + un contexte `SidebarDrawerContext`. Sous `md`, l'`<aside>` devient un tiroir hors écran (`fixed` + `-translate-x-full`) ouvert par un bouton menu `md:hidden` du `Header` (partagé par les deux layouts), avec scrim, bouton de fermeture, fermeture à la navigation et à Échap ; à partir de `md`, la colonne statique d'origine est **préservée à l'identique**. Ajout décisif : `min-w-0` sur le conteneur de `main` — sans lui, un enfant large repousse la colonne hors du viewport et aucun conteneur défilable ne peut rattraper. **Énumération exhaustive** des 37 fichiers portant un `<table>` : 12 `<table>` bruts sans conteneur défilable enveloppés dans un `overflow-x-auto`, dont `GoodsReceiptsPage` (un parent `overflow-hidden` coupait les colonnes) et `DepartmentAccessPage` (cas aggravé du finding). **Preuve runtime Playwright à 390 px** (staff `daf` et `admin`, portail `supplier`) : `mainWidth` **134 → 390 px**, `asideLeft: -256`, hamburger présent, `docScrollWidth = 390` (plus aucun débordement de page) ; **0 élément réellement inatteignable** (mesure : aucun ancêtre défilable) alors que le tableau de `/invoices` mesure toujours 909 px ; les 9 conteneurs de `/admin/department-access` passent de **36 px / overflow visible** à **293 px / overflow auto**. Non-régression desktop 1280 px vérifiée : `position: static`, aside 256 px, `main` 1024 px, hamburger et scrim masqués. **Tests** : 5 tests `SidebarDrawer.test.tsx` ; **contre-preuve** — 4/5 échouent quand les classes responsive sont retirées. |
| AUDIT-020 | P2 | design system / a11y | **P2** (mineur — contraste sous le seuil WCAG AA, usage majoritairement tertiaire) | Le token `--ink-faint` est sous le seuil WCAG AA dans **les deux thèmes** (3,09:1 en clair, 3,51:1 en sombre) | `frontend/src/index.css:92` (clair : `--ink-faint: 43 11% 55%` = `#98917F`) · `frontend/src/index.css:157` (sombre : `39 8% 43%` = `#767065`) · `frontend/tailwind.config.js:72` (`faint: "hsl(var(--ink-faint))"`) · **362 usages** de `text-ink-faint` dans `frontend/src` | axe-core (WCAG 2.1 AA) remonte `color-contrast` (impact `serious`) sur **47/47 pages** balayées, toujours sur ce même token : `#98917F` sur `#ffffff` = **3,09:1** et `#767065` sur `#171b21` = **3,51:1**, pour un seuil exigé de 4,5:1. Le commentaire `index.css:138` assume le compromis pour un usage « tertiary/decorative », ce que confirme l'inventaire des usages (messages d'état vide, libellés secondaires) : **seuls 2 `<label>` de formulaire** sont concernés. Le défaut est donc réel et systémique, mais son impact est modéré — d'où P2 et non P1. | **Prouvée (P2)**, **étendue en P4** aux pages publiques non couvertes : le balayage axe-core de `/register/supplier` (2026-07-23) remonte le même `color-contrast` (impact `serious`) sur 2 nœuds `text-ink-faint` — le défaut déborde donc du périmètre interne et touche la page d'**inscription publique d'un fournisseur externe**. Capture d'origine : `docs/audit/p2-captures/AUDIT-contrast-ink-faint-reports-dark.png` · sortie axe-core : `{fgColor:"#98917F", bgColor:"#ffffff", contrastRatio:3.09, expectedContrastRatio:"4.5:1"}` (clair) et `{fgColor:"#767065", bgColor:"#171b21", contrastRatio:3.51}` (sombre) | Assombrir `--ink-faint` jusqu'à 4,5:1 dans les deux thèmes (le token n'a que 2 définitions, la correction est centralisée), **ou** réserver `text-ink-faint` au décoratif et basculer les 2 `<label>` sur `text-ink-soft`. Rejouer axe-core après correction. | — | CORRIGE (V4-A, 42f1594) : --ink-faint recalibre contre tous les fonds reels mesures par axe -- clair 43 11% 37%, sombre 39 8% 53%, >= 4.62:1 partout ; 2 definitions (index.css) ; balayage axe-core apres = 2 noeuds ink-faint residuels sur 48 ; correction centralisee sur les 362 usages |
| AUDIT-021 | P2 | frontend / i18n / a11y | **P2** (mineur — localisation incorrecte, WCAG 3.1.1) | `<html lang="en">` codé en dur et jamais synchronisé avec la langue i18n : dates natives au format US dans une interface française | `frontend/index.html:2` (`<html lang="en">`) · aucune synchronisation dans `frontend/src` (recherche `documentElement.lang` / `setAttribute('lang')` = **0 occurrence**) · à comparer à `frontend/src/hooks/useTheme.ts:19-20` qui, lui, pilote bien `documentElement` pour le thème | L'attribut `lang` reste figé à `en` alors que `localStorage.i18nextLng = 'fr'` et que toute l'interface est en français. Double conséquence : (1) les `<input type="date">` s'affichent selon la locale du navigateur — observé en `mm/dd/yyyy` dans une session `en-US` sur `/invoices` — au lieu de suivre la langue applicative ; (2) violation WCAG 3.1.1 (*Language of Page*), les lecteurs d'écran prononçant du texte français avec la phonétique anglaise. | **Prouvée (P2)** : `docs/audit/p2-captures/daf-invoices-desktop-dark.png` (champs `mm/dd/yyyy` sous une interface FR) · mesure DOM : `{lang:"en", navLang:"en-US", i18nLang:"fr"}` | Synchroniser `document.documentElement.lang` sur le changement de langue i18n (effet dans le provider i18n ou un hook dédié, sur le modèle de `useTheme`), et initialiser `index.html` à `lang="fr"` (langue par défaut de l'application). | AUDIT-020 (même famille a11y) | **CORRIGÉ (P6, lot V3-C, 2026-07-24)** — `document.documentElement.lang` est synchronisé sur l'événement `languageChanged` d'i18next (modèle de `useTheme`, qui pilote déjà `documentElement`), et `index.html` passe de `lang="en"` à `lang="fr"`, langue par défaut de l'application. **Aucun conflit avec AUDIT-027** (V4-D) qui vise la ligne 7 du même fichier (`<title>frontend</title>`), pas la ligne 2. **Preuve runtime** : bascule EN → `documentElement.lang = 'en'` ; retour FR → `lang = 'fr'`. WCAG 3.1.1 satisfait. **Tests** : `formatLocale.test.ts` (`lang` suit `i18n.language` dans les deux sens). |
| AUDIT-022 | P2 | report / frontend | **P2** (mineur — section de rapport cassée au chargement) | `/reports` déclenche un **HTTP 400 systématique** sur `/reports/payment-cycle` à chaque ouverture de la page | `frontend/src/pages/ReportsPage.tsx:55-56` (`useState('')` pour `fromDateCycle` et `toDateCycle`) · `:132-142` (requête `enabled: canView`, sans condition sur les dates) · backend `src/main/java/com/oct/invoicesystem/domain/report/controller/ReportController.java:137-139` (`@RequestParam Instant from` / `to`, **obligatoires**) | Les deux dates sont initialisées à la chaîne vide ; la requête part malgré tout car `enabled` ne teste que le droit d'accès (`canView`). Les paramètres valant alors `undefined`, axios les omet, et le backend — qui déclare `from` et `to` comme `@RequestParam` sans `required=false` ni valeur par défaut — répond **400**. La section « cycle de paiement » est donc en erreur à chaque ouverture de `/reports`, jusqu'à saisie manuelle des deux dates. Défaut invisible dans le DOM seul : c'est la trace réseau qui le révèle (règle `verify-runtime-not-snapshot`). | **Prouvée (P2)** : trace réseau du balayage, rôle `daf`, page `/reports` → `[{"status":400,"url":"/api/v1/reports/payment-cycle"}]` (reproduit à chaque chargement) | Conditionner la requête aux deux dates (`enabled: canView && !!fromDateCycle && !!toDateCycle`), **ou** initialiser les bornes à une période par défaut (ex. 30 derniers jours) cohérente avec les autres sections de la page. | AUDIT-014 (famille « erreurs non gérées côté frontend ») | **CORRIGÉ (P6, lot V3-B, 2026-07-24)** — la requête `payment-cycle` est conditionnée aux deux dates (`enabled: canView && !!fromDateCycle && !!toDateCycle`), motif déjà en vigueur pour les autres sections pilotées par des dates de la même page (`:128`). **Vérification du périmètre** : le second endpoint à paramètres obligatoires (`/export/pdf/compliance`, `startDate`/`endDate`) n'est **pas** concerné — c'est un export déclenché par l'utilisateur, pas une requête automatique au chargement. **Preuve runtime** (`daf`, backend en marche) : à l'ouverture de `/reports`, **0 appel** à `payment-cycle` et **0 réponse 400** (contre un 400 systématique à chaque chargement) ; après saisie des deux dates, l'appel part et renvoie **200** — non-régression fonctionnelle vérifiée. **Tests** : `ReportsPaymentCycleGuard.test.tsx`. |
| AUDIT-023 | P2 | frontend / a11y — navigation | **P2** (mineur — libellé tronqué + région défilable inatteignable au clavier) | Libellé de navigation tronqué sur toutes les pages admin, et matrice des permissions défilable uniquement à la souris | `frontend/src/components/layout/Sidebar.tsx:93` (conteneur `w-64`, largeur utile 180 px) — entrée « Configuration de la matrice d'approbation » · `frontend/src/pages/admin/AdminPermissionMatrixPage.tsx:130-131` (`<Panel className="overflow-x-auto">` + `<table className="w-full …">`) | Deux défauts distincts relevés sur le même parcours. (1) Le libellé « Configuration de la matrice d'approbation » demande 268 px pour 180 px disponibles : il s'affiche « Configuration de la matric… » sur **toutes** les pages admin. (2) La matrice des permissions mesure **2 619 px de large pour un viewport de 1 440** — les 7 dernières colonnes de rôles (à partir de « DEX ») ne sont pas visibles. Le `overflow-x-auto` **existe bien** (donc aucune donnée n'est perdue à la souris), mais axe-core remonte `scrollable-region-focusable` : la région défilable n'a pas de `tabindex`, elle est donc **inatteignable au clavier**. | **Prouvée (P2)** : `docs/audit/p2-captures/` → capture `admin___admin_permissions__desktop__light.png` et `…__dark.png` (colonnes coupées à « DEX — Va… », libellé sidebar tronqué) · mesures DOM : libellé `{scrollW:268, clientW:180}` · tableau `{right:2619, vw:1440}` | (1) Raccourcir le libellé (ex. « Matrice d'approbation ») ou autoriser le retour à la ligne. (2) Ajouter `tabIndex={0}` + `role="region"` et un `aria-label` sur le conteneur défilable ; envisager une colonne « Utilisateur » figée pour garder le contexte au défilement. | AUDIT-020 (famille a11y) | CORRIGE (V4-A, 42f1594) : libelle deja raccourci en V3 ; region defilable rendue focalisable (tabIndex=0 + role=region + containerLabel) sur la primitive ui/Table.tsx ET la matrice des permissions ; axe select-name/button-name = 0 ; dette a11y V3-A traitee (tiroir mobile ferme en inert sous md via useMediaQuery) |
| AUDIT-024 | P2 | frontend / a11y — formulaires | **P2** (mineur — formulaires inutilisables au lecteur d'écran) | Champs de formulaire sans nom accessible : les `<label>` ne sont pas associés à leur champ (`htmlFor` absent dans **25+ fichiers**) | `frontend/src/pages/admin/AdminUserFormPage.tsx:104` (10 champs, **0 `htmlFor`**) · `frontend/src/pages/ProfilePage.tsx` (8/0) · `frontend/src/pages/admin/AdminDepartmentFormPage.tsx` (7/1) · `frontend/src/pages/PaymentsPage.tsx` (6/0) · `frontend/src/pages/admin/AdminDelegationsPage.tsx` (6/0) · `frontend/src/pages/GoodsReceiptsPage.tsx` (5/0) · `frontend/src/pages/admin/AdminChecklistTemplatesPage.tsx` (5/0) · +18 autres fichiers | Le motif dominant est `<label className="…">Libellé</label>` suivi d'un `<input>`/`<select>` **frère**, sans `htmlFor`/`id` ni `aria-label`. Le libellé est donc visible à l'écran mais **non rattaché programmatiquement** : un lecteur d'écran annonce « zone d'édition » sans dire de quel champ il s'agit, et le clic sur le libellé ne donne pas le focus au champ. axe-core le remonte en `label` (18 occurrences), `select-name` (16) et `button-name` (4, boutons icône sans `aria-label`) sur l'échantillon balayé. Le motif correct existe déjà dans le code (`ReportsPage.tsx` : 3 `htmlFor` sur 6 labels, `LoginPage.tsx` utilise bien `id`+`htmlFor`) — la base de code est donc incohérente avec elle-même. | **Prouvée (P2)**, **étendue en P4** : le balayage axe-core des 4 pages publiques non couvertes en P2 (2026-07-23) montre que `/register/supplier` porte **10 champs sans nom accessible** (`label`, impact **critical** : `companyName`, `taxId`, `contactPhone`, `address`…). C'est la page d'**inscription publique d'un fournisseur externe** : la portée du finding dépasse donc le périmètre interne. ✅ Les 3 autres pages publiques (`/forgot-password`, `/reset-password`, `/verify-email`) sont **exemptes de toute violation** (0 violation axe-core WCAG 2.1 AA). Sortie P2 d'origine — `label` sur `/admin/users/new` (`input[name="firstName"]`), `/admin/departments/new` (`input[name="nameFr"]`), `/admin/retention-policy`, `/admin/security`, `/admin/compliance`, `/my-delegations`, `/admin/announcements`, `/admin/approval-matrix`, `/admin/matching-config` · `select-name` sur 8 pages · `button-name` / `link-name` sur les **boutons de retour en icône seule** (flèche), sans texte ni `aria-label` : `/admin/users/new` (`<button class="p-2 hover:bg-white/10 rounded-full">`), `/invoices/:id` (`<button class="p-2 -ml-2 rounded-[4px] … shrink-0">`), `/payments/alert-rules` (`<a href="/payments" class="p-2 -ml-2 …">`) — un lecteur d'écran annonce « bouton » / « lien » sans indiquer la destination | Ajouter `id` + `htmlFor` sur chaque couple libellé/champ (ou `aria-label` quand aucun libellé visible n'est prévu), et `aria-label` sur les boutons icône. Chantier mécanique et sans risque fonctionnel : bon candidat pour un sous-agent Sonnet en P6, relu par Opus. Rejouer axe-core après correction. | AUDIT-020, AUDIT-021, AUDIT-023 (même famille a11y — à traiter en un seul lot) | CORRIGE (V4-A, 42f1594) : 82 couples label/champ associes (htmlFor/id) sur 34 fichiers + aria-label sur champs de filtre sans libelle visible et boutons icone + 10 champs de /register/supplier ; axe label/select-name/button-name = 0 (etaient 34/50/2) sur 51 pages x 2 themes ; tests contre-factuels 3/3 |
| AUDIT-025 | P2 | matching / frontend | **P2** (mineur — état vide présenté comme une panne) | `/matching/:invoiceId` affiche « **Erreur de chargement** » pour une facture sans rapprochement, alors que le 404 est le comportement **normal** du backend | `frontend/src/pages/matching/MatchingDetailPage.tsx:27` (`const { data, isLoading, isError, refetch } = useQuery(...)`) et `:65` (`) : isError || !data ? (`) | La page ne distingue pas **« erreur serveur »** de **« aucun rapprochement pour cette facture »** : la condition `isError || !data` traite les deux cas par le même écran d'erreur. Or `GET /api/v1/matching/{invoiceId}/lines` renvoie légitimement **404** quand la facture n'a pas de PO — vérifié en base : la facture testée a `purchase_order_id` **vide** et la table `three_way_matching_results` est **entièrement vide** (0 ligne). L'utilisateur voit donc « Erreur de chargement » pour un cas de figure parfaitement normal, ce qui laisse croire à une panne du système. Le rendu ne contient que 39 caractères (« Retour à la liste / Erreur de chargement ») : pas d'explication, pas d'action utile. | **Prouvée (P2)** : capture `scratch/p2-audit/sample/captures/daf___matching_18027651-…__desktop__light.png` · trace réseau rôle `daf` → `[{"status":404,"url":"/api/v1/matching/{id}/lines"}]` · contrôle base : `select count(*) from three_way_matching_results` = **0** | Traiter le 404 comme un **état vide** dédié (« Aucun rapprochement pour cette facture — cette facture n'est pas rattachée à un bon de commande »), et ne conserver l'écran d'erreur que pour les échecs réels (5xx, réseau). Distinguer `isError` de `!data` dans la condition `:65`. | AUDIT-014 (famille « gestion d'erreurs frontend ») | **CORRIGÉ (P6, lot V3-B, 2026-07-24)** — `MatchingDetailPage` distingue enfin l'état vide de la panne : le **404**, réponse normale du backend pour une facture sans bon de commande, affiche un état vide dédié (« Aucun rapprochement pour cette facture » + explication), l'écran d'erreur restant réservé aux échecs réels (5xx, réseau). La condition `isError \|\| !data` de `:65` est remplacée par un `isEmpty` explicite, et `retry: false` évite de retarder l'état vide. **Tests** : `MatchingDetailEmptyState.test.tsx` — 404 → état vide **et** 500 → écran d'erreur, les deux sens vérifiés. |
| AUDIT-026 | P2 | auth / routing — **cloisonnement fournisseur** | **P1** (majeur — un fournisseur externe atteint des écrans internes) | `ProtectedRoute` filtre le fournisseur par une **liste blanche de 4 préfixes** : toute autre route staff lui reste ouverte (`/profile`, `/notifications`, `/my-delegations`, `/access-requests`) | `frontend/src/components/auth/ProtectedRoute.tsx:19-27` (`isAdminPath` = `startsWith('/admin') \|\| '/dashboard' \|\| '/invoices' \|\| '/reports'`) · routes concernées `frontend/src/AppRoutes.tsx:87` (`/profile`), `:88` (`/access-requests`), `:89` (`/my-delegations`), `:100` (`/notifications`) · **`StaffRoute` (`ProtectedRoute.tsx:47-49`) ferait exactement ce contrôle mais n'est jamais importé** (code mort — cf. AUDIT-016) | Le contrôle est fait par **énumération des chemins interdits** au lieu d'une liste blanche des chemins autorisés : tout ce qui ne commence pas par `/admin`, `/dashboard`, `/invoices` ou `/reports` échappe au filtre. Un compte `ROLE_SUPPLIER` atteint donc l'interface interne. Deux écrans sont particulièrement problématiques : **`/access-requests`** lui présente un formulaire de demande de rôle listant « Administrateur » et « DAF (Directeur Administratif et Financier) », et **`/my-delegations`** un formulaire « Déléguez vos approbations à un collègue » — deux fonctions internes dénuées de sens pour un tiers externe, qui divulguent en outre la structure des rôles de l'entreprise. À décharge : le **backend tient** (403 sur `/access-requests/mine` et `/approvals/delegations/*`), donc les formulaires sont vides et non soumissibles — la fuite est d'interface, pas de données. | **Prouvée (P2)** : balayage rôle `supplier`, 8 pages staff sondées → **4 atteintes sans redirection** (`/profile`, `/access-requests`, `/notifications`, `/my-delegations`) contre 3 correctement redirigées vers `/supplier/dashboard` (`/invoices`, `/invoices/new`, `/invoices/:id`). Rendus relevés : `/access-requests` → « Mes demandes d'accès … Rôle : Administrateur / DAF … » ; `/my-delegations` → « Mes délégations d'approbation … Déléguez vos approbations à un collègue ». Traces réseau : `403` sur `/api/v1/access-requests/mine` et `/api/v1/approvals/delegations/mine`. Captures : `scratch/p2-audit/sample/captures/supplier___*__desktop__light.png` | Inverser la logique : appliquer `StaffRoute` (déjà écrit, jamais utilisé) à **toutes** les routes staff, ou remplacer `isAdminPath` par « tout chemin ne commençant pas par `/supplier` ». Supprimer ensuite le code mort résiduel. Ajouter un test frontend « un compte SUPPLIER sur une route staff → redirection ». | **Recoupe AUDIT-016** (le code mort `StaffRoute` est précisément le correctif manquant) · AUDIT-004 (même famille : garde de page absente) | **CORRIGÉ (P6, lot V2-C, 2026-07-23)** — la liste **noire** de 4 préfixes est remplacée par une règle posée positivement : tout chemin ne commençant pas par `/supplier` est staff. C'est la cause racine qui est traitée, pas les 4 chemins constatés — une liste noire ne vaut que ce qu'elle valait le jour où elle a été écrite. Vérifié que toutes les routes sous `ProtectedRoute` sont bien staff et que le portail a son propre `/supplier/profile`, donc aucun parcours légitime cassé. **Preuve** : `ProtectedRoute.supplier.test.tsx` (6 tests) — les **4 routes du finding** (`/profile`, `/access-requests`, `/my-delegations`, `/notifications`) redirigent vers `/supplier/dashboard`, plus la non-régression des routes déjà couvertes et la contre-preuve « un compte staff passe toujours ». **Vérifié contre-factuellement** : avec l'ancienne liste noire, 4 des 6 tests échouent. |
| AUDIT-027 | P2 | frontend / identite produit | **P3** (cosmetique — mais visible en soutenance) | Le titre de l'onglet est **`frontend`** (valeur par defaut de Vite) sur les 56 pages du systeme | `frontend/index.html:7` (`<title>frontend</title>`) — aucune surcharge dynamique : aucun `document.title` ni composant de type `<Helmet>` dans `frontend/src` | Le gabarit `index.html` genere par Vite n'a jamais ete personnalise. Le titre n'etant jamais mis a jour cote React, **toutes** les pages partagent la meme valeur : releve sur les 992 observations, un seul titre distinct existe — `frontend`. Consequences : onglets indistinguables quand plusieurs pages sont ouvertes, favoris et historique du navigateur inexploitables, et surtout une identite produit absente pour un systeme destine a une soutenance (le nom « OCT Invoices » figure pourtant dans la sidebar). | **Prouvee (P2)** : sonde `docTitle` sur les 992 observations -> `[...new Set(...)]` = **`frontend`** (valeur unique, tous roles / pages / viewports / themes confondus) | Mettre un titre de base dans `index.html` (ex. « OCT Invoices — Owendo Container Terminal ») et, si l'on veut aller plus loin, positionner `document.title` par page (« Factures · OCT Invoices ») via un petit hook appele dans chaque page ou un effet centralise sur le changement de route. | AUDIT-021 (meme fichier `index.html`, ou l'attribut `lang` est aussi a corriger) | OUVERT |
| AUDIT-028 | P3 | purchasing / GRN | **P1** (majeur — fonctionnalité entière inopérante) | `GET /api/v1/goods-receipts` renvoie une liste **vide codée en dur** : la page « Bons de Réception » n'affiche jamais rien | `src/main/java/com/oct/invoicesystem/domain/purchasing/controller/GoodsReceiptController.java:51-61` (`listGRNs` : `purchaseOrderId != null ? grnService.getGRNsByPurchaseOrder(...) : List.of()`) · appelant `frontend/src/pages/GoodsReceiptsPage.tsx:60-64` (`apiClient.get('/goods-receipts')`, **sans aucun paramètre**) | Le contrôleur retourne littéralement `List.of()` quand le paramètre facultatif `purchaseOrderId` est absent — il n'existe **aucun** chemin « lister tous les GRN ». Or le frontend appelle l'endpoint sans paramètre : les deux moitiés sont incompatibles par construction. Le service dispose pourtant du nécessaire (`grnRepository`), seule la branche « sans filtre » n'a jamais été écrite. Ce n'est pas une base vide : `select count(*) from goods_receipt_notes` = **11**, aucun `deleted_at`. | **PROUVÉE (P3, 2026-07-22)** — API : `GET /api/v1/goods-receipts` → **200 `{"data":[]}`** avec le jeton `aa` **et** avec le jeton `daf` (idem avec `?page=0&size=50`). Contre-preuve dans le même souffle : `GET /api/v1/goods-receipts?purchaseOrderId=c0ad7345-…` → **200 avec le GRN complet** (`GRN-2026-101`, ses 3 lignes, quantités reçues) — les données existent donc bien et l'endpoint fonctionne **uniquement** avec le filtre. Interface (capture `docs/audit/p3-captures/AUDIT-028-grn-liste-vide.png`, session `aa`) : la page `/goods-receipts` affiche « **Aucun bon de réception enregistré** » ; trace réseau `GET /api/v1/goods-receipts => [200]`, charge utile vide. Base : **11 GRN** présents. | Implémenter la branche manquante : `grnService.getAllGRNs()` (paginé) quand `purchaseOrderId` est absent, sur le modèle de `PurchaseOrderController`. Ajouter le test d'intégration « liste sans filtre → n éléments » — absent aujourd'hui, `GoodsReceiptController` faisant partie des 14 contrôleurs non testés d'AUDIT-013. | **AUDIT-013** (ce contrôleur est dans la liste des non testés — le défaut serait tombé avec un test) | **CORRIGÉ (P6, lot V1-E, 2026-07-23)** — branche « sans filtre » implémentée sur le modèle de `PurchaseOrderController.listPurchaseOrders` : `GoodsReceiptNoteRepository.findAllActive(Pageable)` (filtre `deletedAt IS NULL`) → `GoodsReceiptService.getAllGRNs` → contrôleur paginé. Le type de retour passe à `PagedResponse<GoodsReceiptDTO>` sur les **deux** branches ; vérifié en revue que la branche filtrée n'avait **aucun consommateur** (ni frontend, ni client interne, ni API.md), donc aucun contrat cassé — une forme unique vaut mieux qu'un retour polymorphe. **Preuve** : `GoodsReceiptControllerTest`, 4 tests — « liste sans filtre → 3 GRN » (le cas du finding, qui échouerait sur le code d'avant), « filtre par BC → 2 », « DAF → 200 », « **ADMIN → 403** » (SoD) ; un GRN **soft-supprimé** est créé dans le `setUp` et n'apparaît dans aucune des deux branches. Ce contrôleur sort ainsi de la liste des 14 non testés d'AUDIT-013. **Deux défauts collatéraux revélés par le correctif et fermés dans le même lot** : (a) **N+1** — `toDTO` traverse lignes, BC et réceptionnaire, tous LAZY, soit `1 + N × (3 + M)` requêtes (~1000 pour `size=200`) → `@EntityGraph`, et départage du tri par `createdAt` car `receiptDate` est un `LocalDate` (pagination sinon instable) ; (b) **colonnes fantômes** — le type frontend déclarait `supplierName`, `receivedDate`, `status`, `notes`, **quatre champs absents du DTO** : invisible tant que la liste était vide, la page se serait remplie avec une date vide et trois colonnes à « — ». Type et colonnes réalignés sur le DTO réel. Le mock du test frontend servait un tableau nu — forme que le backend ne produit pas — donc il passait à l'identique avant/après : corrigé. Suivi `size: 200` sans contrôles de pagination consigné en **`docs/TASKS.md` G9**. Backend **679/0/0 BUILD SUCCESS**, frontend **251/251**, `tsc` 0. Journalisé **PROB-136**. |
| AUDIT-029 | P3 | payment / invoice | **P1** (majeur — contrôle financier absent) | Une facture est soldée et **archivée** par un paiement d'un montant quelconque : aucun rapprochement entre montant payé et montant dû | `src/main/java/com/oct/invoicesystem/domain/payment/service/PaymentServiceImpl.java:74-111` (`recordPayment` — aucune comparaison entre `request.amountPaid()` et `invoice.getAmount()`) · `:117-138` (`finalizePayment` enchaîne `RECORD_PAYMENT` puis `ARCHIVE`) | `recordPayment` vérifie le statut (`BON_A_PAYER`), l'absence de paiement déjà enregistré et la positivité du montant (`@Positive` sur le DTO), **mais jamais que le montant payé corresponde au montant facturé**. Le premier paiement enregistré, quel que soit son montant, déclenche `finalizePayment` qui fait passer la facture en `PAYE` puis immédiatement en `ARCHIVE`. Comme `existsByInvoiceId` interdit ensuite tout second paiement, la facture est **définitivement close** sur un règlement partiel — sans alerte, sans reste à payer, sans possibilité de compléter. | **PROUVÉE (P3, 2026-07-22)**, deux fois et par les deux chemins de paiement. (a) *Paiement immédiat* : facture `FAC-2026-00050` (département DRH) d'un montant de **600 000 XAF** → `POST /payments/invoice/{id}` avec `amountPaid=1` (**1 XAF**, méthode VIREMENT) → **200 `payment.recorded.success`**, statut de la facture immédiatement **`ARCHIVE`**. (b) *Paiement planifié puis traité* : facture `18027651-…` d'un montant de **2 000 000 XAF** → paiement `SCHEDULED` de **100 XAF** par chèque, puis `POST /payments/{id}/process` → **200**, facture **`ARCHIVE`**. Historique en base : `BON_A_PAYER→PAYE` à 00:05:33.765 puis `PAYE→ARCHIVE` à 00:05:33.788. ✅ Contrôles voisins bien présents (à décharge) : `amountPaid` **négatif** → 400 « *Le montant payé doit être positif* » ; `amountPaid = 0` → 400, même message. Seul le rapprochement au montant dû manque. | Ajouter dans `recordPayment` une règle explicite : soit exiger `amountPaid == invoice.getAmount()` (paiement intégral, message i18n dédié en cas d'écart), soit — si les règlements partiels sont voulus — introduire un cumul et ne déclencher `finalizePayment` que lorsque la somme des paiements atteint le montant dû, en levant l'interdiction `existsByInvoiceId`. **Choix de cadrage à trancher en P5** au regard du cahier des charges ; en l'état le système n'implémente ni l'un ni l'autre. | **AUDIT-030** (l'archivage automatique fait partie du même enchaînement) | **CORRIGÉ (P6, lot V1-C, 2026-07-23)** — contrôle explicite ajouté dans `PaymentServiceImpl.recordPayment`, placé **avant toute écriture** : `request.amountPaid().compareTo(invoice.getAmount()) != 0` lève `WorkflowException("error.payment.amount_must_match_invoice")`, clé présente dans les **deux** catalogues. `compareTo` et non `equals` : `equals` distingue les échelles décimales, donc un règlement légitime saisi `1000` sur une facture à `1000.00` aurait été refusé. **Tous les chemins d'entrée sont couverts** — vérifié en revue : `recordPayment` (direct), `recordBatchPayment` (passe par le proxy `self.recordPayment` et alimente de toute façon `invoice.getAmount()`), et `processPayment` qui n'exécute qu'un `Payment` déjà validé à la création ; le montant d'une facture n'étant modifiable qu'en `BROUILLON`/`REJETE`, aucune dérive n'est possible entre planification et exécution. **Preuve** : `recordPayment_FailsIfAmountDoesNotMatchInvoiceAmount` (scénario exact du finding — 1 XAF sur une facture de 1000 → refus, **aucun** paiement enregistré, **aucun** événement de machine à états émis), `recordPayment_FailsIfAmountExceedsInvoiceAmount` (sur-paiement refusé), `recordPayment_AcceptsSameAmountWithDifferentScale` (1000 vs 1000.00 → accepté), et le test d'intégration HTTP `PaymentControllerTest.recordPayment_RejectsPartialAmount` (**4xx**, facture restée `BON_A_PAYER`). Interface : le champ « montant payé » de `PaymentsPage` passe en **lecture seule** (les règlements partiels étant hors périmètre, un champ libre invitait à l'erreur là même où le défaut s'est produit). Backend **664/0/0 BUILD SUCCESS**, frontend **241/241**, `tsc` 0. Journalisé **PROB-133**. |
| AUDIT-030 | P3 | invoice / machine à états | **P2** (mineur — état documenté jamais observable) | L'état `PAYE` est court-circuité : toute facture payée est archivée dans la même transaction, en ~23 ms | `src/main/java/com/oct/invoicesystem/domain/payment/service/PaymentServiceImpl.java:129-136` (`finalizePayment` envoie `RECORD_PAYMENT` **puis immédiatement** `ARCHIVE` avec le drapeau `AUTO_ARCHIVE`) · machine à états `src/main/java/com/oct/invoicesystem/config/StateMachineConfig.java:100-108` (transitions 9 et 10) | Les deux transitions sont émises l'une après l'autre sans point d'arrêt : `PAYE` n'est jamais un état de repos. Aucune facture ne peut donc être consultée, filtrée ou traitée à l'état « Payé » — alors que l'interface propose bien ce filtre (relevé sur `/supplier/invoices` : « Brouillon · Soumis · En contrôle AA · En validation N1 · En validation N2 · Validé · Bon à payer · **Payé** · Rejeté · Archivé »), que `InvoiceStatus` le déclare et que `CLAUDE.md §5` documente `BON_A_PAYER → PAYE → ARCHIVE` comme trois étapes distinctes. L'archivage est en outre une décision de gestion documentaire, discutable comme effet automatique d'un paiement. | **PROUVÉE (P3, 2026-07-22)** — historique complet de `FAC-2026-00030`, obtenu en jouant le workflow entier : `BROUILLON→SOUMIS` (23:37:58), `SOUMIS→EN_CONTROLE_AA` (23:38:34), `→EN_VALIDATION_N1`, `→EN_VALIDATION_N2`, `→VALIDE`, `→BON_A_PAYER` (23:38:34), puis **`BON_A_PAYER→PAYE` à 23:39:45.559877** et **`PAYE→ARCHIVE` à 23:39:45.589620** — **29 ms d'écart**. L'état est donc bien traversé (la machine à états est correcte) mais jamais observable. Confirmé par le dénombrement final : `select status, count(*) from invoices group by status` ne renvoie **aucune ligne `PAYE`** malgré 5 factures archivées après paiement. | Décision de cadrage en **P5** : (a) séparer les deux transitions — le paiement laisse la facture en `PAYE`, l'archivage devient une action explicite (ou un traitement planifié après délai de rétention) ; ou (b) assumer l'archivage automatique et **retirer `PAYE` des filtres d'interface** + corriger `CLAUDE.md §5`. L'incohérence actuelle entre l'interface (qui propose le filtre) et le comportement (qui rend le filtre toujours vide) n'est pas tenable en soutenance. | AUDIT-029 · AUDIT-017 (doc `CLAUDE.md §5`) | **CORRIGÉ (P6, lot V1-C, 2026-07-23)** — `finalizePayment` n'émet plus que `RECORD_PAYMENT` : la facture reste en **`PAYE`**, désormais un état de repos observable et filtrable. L'archivage devient une action explicite — `POST /api/v1/invoices/{id}/workflow/archive`, réservée à **AA et DAF**, **ADMIN exclu** (pas d'accès financier) — exposée dans `InvoiceActionPanel` sur une facture `PAYE`. Le drapeau `AUTO_ARCHIVE` n'ayant plus **aucun** émetteur en production, il est **retiré** de `WorkflowExtendedStateKeys` ; la garde d'archivage teste maintenant le **statut source** (`status != PAYE` → refus i18n `error.invoice.archive_requires_paid`), ce qui est **plus strict** que l'ancienne version, laquelle faisait confiance à l'appelant pour poser le drapeau. **Preuve** : `recordPayment_leavesInvoiceInPaye_notArchive` (le paiement n'archive plus), `archive_explicitAction_movesPayeToArchive` (l'action explicite archive bien), `archive_refusedWhenInvoiceNotPaid` (facture non payée → 4xx, statut inchangé), **`archive_forbiddenForAdmin`** (SoD, critère d'audit → **403**) et `archive_forbiddenForUnauthorizedRole` ; non-régression des 8 transitions via `StateMachineTransitionExhaustiveTest` (T8 archive depuis `PAYE`, I1 archive bloqué depuis `BROUILLON`). Côté frontend, 4 tests dont « un validateur ne voit aucune action sur une facture `PAYE` ». **Effets de bord aval vérifiés en revue** : rapports (aging, cash-flow), retards et compteurs du portail traitent `PAYE` et `ARCHIVE` de façon **symétrique** → aucun impact ; en revanche une facture payée non encore archivée n'apparaît plus dans `/archive` et n'est pas classable dans un dossier — **comportement voulu** par D3, le classement documentaire dépendant désormais d'une action humaine. Documentation mise en cohérence (`docs/WORKFLOW.md` règle métier n°7 et table des transitions, `docs/API.md`, Javadoc `PaymentService`/`PaymentStatus`, `@Operation` Swagger de `PaymentController`, `InvoiceValidationService` règle 7). Backend **664/0/0**, frontend **241/241**, `tsc` 0. Journalisé **PROB-134**. |
| AUDIT-031 | P3 | supplier-portal / matching | **P1** (majeur — parcours fournisseur+PO structurellement impossible) | Le portail fournisseur **ignore silencieusement** les lignes de facture (`lineItems`), ce qui rend tout rapprochement impossible sur cette voie | `src/main/java/com/oct/invoicesystem/domain/supplier/controller/SupplierPortalController.java:260-281` (`toInvoice` : recopie `amount`, `currency`, dates, description, `purchaseOrderId`… **mais jamais `request.lineItems()`**) — à comparer à la voie interne, qui les persiste bien | `SupplierPortalController` réutilise le DTO `InvoiceCreateRequest`, lequel **déclare** `List<LineItem> lineItems`. Le portail accepte donc le champ sans erreur, puis le jette. Or `ThreeWayMatchingService.performMatching` (`:98-100`) lève `ValidationException("Invoice or PO has no line items")` dès qu'un des deux côtés n'a pas de lignes, et `performMatchingCheck` requalifie l'échec en `WorkflowException` bloquante. Conséquence en chaîne : **toute facture du portail référençant un PO est refusée à la soumission**, quel que soit son contenu, et le fournisseur reçoit un message qui ne dit pas pourquoi. | **PROUVÉE (P3, 2026-07-22)** — `POST /api/v1/supplier/invoices` avec **3 `lineItems` valides** (recopiés du PO, montants cohérents) → **201 Created**, aucun avertissement. Contrôle en base immédiatement après : `select count(*) from invoice_items where invoice_id='052a7915-…'` = **0** — les lignes n'ont pas été enregistrées. Puis `POST /supplier/invoices/{id}/submit` → **400** « *Le rapprochement n'a pas pu être évalué ; soumission refusée.* », et le journal backend donne la cause réelle : `ValidationException: Invoice or PO has no line items` (`ThreeWayMatchingService.java:99`). **Contre-épreuve par la voie interne** : la même facture créée par `aa` avec les mêmes `lineItems` enregistre bien **3 lignes** en base et le rapprochement s'exécute — le défaut est donc bien localisé au seul portail. | Recopier les lignes dans `toInvoice` comme le fait la voie interne (et couvrir par un test d'intégration « facture portail avec lignes → lignes persistées »). Tant que ce n'est pas fait, AUDIT-001 ne peut pas être corrigé utilement : ajouter un sélecteur de PO au formulaire produirait des factures systématiquement rejetées à la soumission. | **Bloque AUDIT-001** · recoupe AUDIT-002 · **AUDIT-013** (`SupplierPortalController` non testé) | **CORRIGÉ (P6, lot V1-B, 2026-07-23)** — `toInvoice` recopie désormais les lignes. Cause profonde traitée : `attachLineItems` était une méthode **privée** d'`InvoiceController`, donc la voie interne la possédait et le portail non ; elle est extraite dans `domain/invoice/service/InvoiceLineItemFactory` et **les deux voies d'admission appellent la même implémentation** — elles ne peuvent plus diverger. **Preuve** : `SupplierPortalIntegrationTest.portalInvoiceWithLines_persistsThem` (facture portail avec lignes → `invoice_items` = 1, `lineNumber` = 1, description conservée) et `portalInvoiceAgainstOwnPo_reachesMatchedOnSubmit` (parcours complet : PO propre → soumission → journal backend « **Matching check passed … with status MATCHED** », résultat de rapprochement persisté). Backend **655/0/0 BUILD SUCCESS**, frontend **237/237**, `tsc` 0 erreur. Journalisé **PROB-132**. |
| AUDIT-032 | P3 | invoice / validation | **P1** (majeur — données financières incohérentes acceptées) | Aucune validation métier à la création d'une facture : montant **nul**, montant **négatif**, échéance **antérieure** à l'émission et devise **hors périmètre** sont tous acceptés | `src/main/java/com/oct/invoicesystem/domain/invoice/dto/InvoiceCreateRequest.java:14-26` (`@NotNull BigDecimal amount` **sans `@Positive`**, `@NotBlank String currency` **sans contrainte de valeur**, `issueDate`/`dueDate` sans validation croisée) — à comparer à `domain/payment/dto/PaymentRequest.java:11-13` qui, lui, porte bien `@Positive` | Le DTO ne pose que des contraintes de présence. Aucune annotation ni contrôle de service ne vérifie la positivité du montant, la cohérence des dates, ni l'appartenance de la devise à un ensemble autorisé. Le contraste avec `PaymentRequest` (qui refuse correctement un montant ≤ 0) montre que la règle existe ailleurs dans le code : c'est une omission, pas un choix. **La devise est le point le plus sensible** : `XOF` est explicitement proscrite par la règle projet (migration `V45__normalize_currency_xof_to_xaf.sql` a précisément servi à l'éliminer) et se réintroduit ici par l'API. | **PROUVÉE (P3, 2026-07-22)** — création via `POST /api/v1/invoices` (jeton `aa`) : `amount = 0` → **201** (`FAC-2026-00035`) · `amount = -50000` → **201** (`FAC-2026-00036`) · `issueDate = 2026-08-30` avec `dueDate = 2026-07-01` (échéance **2 mois avant** l'émission) → **201** · `currency = "USD"` → **201** · `currency = "XOF"` → **201** (`FAC-2026-00041`). Persistance vérifiée depuis le navigateur en session `aa` : `GET /api/v1/invoices?size=100` renvoie `FAC-2026-00036 = -50000 XAF`, `FAC-2026-00035 = 0 XAF`, et l'ensemble des devises distinctes contient **`XOF` et `USD`**. Reproduit **aussi par l'interface** du portail fournisseur : facture `FAC-2026-00049` créée avec émission 31/12/2026 et échéance 01/01/2026, visible dans « Mes Factures » (capture `docs/audit/p3-captures/AUDIT-dates-incoherentes-facture-creee.png`). ✅ À décharge : un montant démesuré (10^15) est rejeté en **409** par la contrainte `numeric(15,2)` de la base, et une devise de plus de 3 caractères en **409** — la base rattrape ce que l'application laisse passer. | Ajouter sur `InvoiceCreateRequest` (et `InvoiceUpdateRequest`) : `@Positive` sur `amount`, une contrainte de devise (liste blanche — **`XAF` seule** au regard de la règle projet, ou énumération explicite si l'EUR/USD est voulu), et une validation croisée `dueDate >= issueDate` (annotation de classe). Aligner le formulaire fournisseur, qui propose aujourd'hui **EUR et USD** dans sa liste déroulante. Nettoyer ensuite les données de test incohérentes créées en P3. | **AUDIT-033** (le formulaire propose des devises hors périmètre) | **CORRIGÉ (P6, lot V1-D, 2026-07-23)** — validation posée à **deux niveaux, volontairement redondants**. (1) **DTO** (`InvoiceCreateRequest` **et** `InvoiceUpdateRequest`) : `@Positive` sur `amount`, `@Pattern(regexp = "XAF")` sur `currency`, et une contrainte de **classe** `@DueDateNotBeforeIssueDate` (nouveau validateur + interface `HasIssueAndDueDate`, sans réflexion) pour la validation croisée des dates — ce niveau produit un message **par champ**. (2) **SERVICE** : `InvoiceService.enforceFinancialInvariants`, appelé par `createInvoice` **et** `updateInvoice`. **Ce second niveau n'est pas décoratif** : la revue a établi que s'arrêter aux DTO n'aurait fermé **qu'un chemin sur deux** — `InvoiceImportService` (import CSV/XML de masse) construit l'entité **directement avec le builder**, sans DTO ni `@Valid`, et une ligne `currency=XOF` ou `amount=-50000` serait passée telle quelle. **Preuve** : 11 tests d'intégration — les 5 preuves runtime du finding rejouées sur `POST /invoices` (montant 0 et −50 000 → 400 ; échéance antérieure → 400 ; `XOF`, `USD`, `EUR` → 400), les contournements d'une liste blanche (`xaf` minuscule, `" XAF "` avec espaces → 400), la **mise à jour** (`updateInvoice`), le cas licite `dueDate == issueDate` → 201, et surtout `csvImport_enforcesFinancialInvariants` (6 lignes : 1 conforme créée, **5 refusées**) qui verrouille le chemin d'import. L'assertion `$.errors[0]` contient « dueDate » verrouille un contrat non évident : une contrainte de classe remonte en `ObjectError`, or `GlobalExceptionHandler` n'itère que sur `getFieldErrors()` — sans `addPropertyNode`, le motif du refus **disparaîtrait silencieusement** de la réponse. **Nettoyage des données de test** : les 23 occurrences `"EUR"` des 8 fichiers de fixtures normalisées en `XAF` (dont la dette connue `SupplierPortalIntegrationTest`). Backend **675/0/0 BUILD SUCCESS**, frontend **250/250**, `tsc` 0. Journalisé **PROB-135**. |
| AUDIT-033 | P3 | supplier-portal / frontend | **P2** (mineur — cohérence de la règle devise) | Le formulaire fournisseur propose **EUR** et **USD** alors que la devise du système est XAF | `frontend/src/pages/supplier/SupplierInvoiceSubmitPage.tsx` (liste déroulante `currency` : `XAF (FCFA)`, `EUR`, `USD`) | La règle projet fixe **XAF** (Franc CFA BEAC) comme devise du système ; la migration `V45` a été écrite pour éliminer les occurrences de `XOF`. Proposer EUR et USD dans le formulaire ouvre une saisie que rien ne valide côté serveur (AUDIT-032) et qu'aucun écran ne sait convertir : les rapports agrègent les montants sans tenir compte de la devise. Soit le multidevise est un besoin réel — et il manque alors taux de change, conversion et affichage —, soit c'est un reliquat de gabarit à retirer. | **PROUVÉE (P3, 2026-07-22)** — relevé DOM du formulaire `/supplier/invoices/new` (session `supplier`, après « Ignorer — saisir manuellement ») : le `<select name="currency">` contient **3 options** — « XAF (FCFA) », « EUR », « USD ». Capture `docs/audit/p3-captures/AUDIT-001-portail-formulaire-sans-champ-PO.png` (même écran). Corrélé au constat base d'AUDIT-032 : des factures en `USD` **et en `XOF`** coexistent déjà. | Décision en **P5** : si le système est mono-devise (position retenue jusqu'ici), retirer EUR/USD du sélecteur et verrouiller `currency` à `XAF` côté DTO. Sinon, ouvrir un chantier multidevise complet (taux, conversion, agrégation des rapports) — ce qui dépasse largement le périmètre de l'audit. | AUDIT-032 | **CORRIGÉ (P6, lot V1-D, 2026-07-23)** — EUR et USD retirés du sélecteur de `SupplierInvoiceSubmitPage.tsx`. Le système étant mono-devise, une liste déroulante à **une seule option** serait une affordance trompeuse : la devise est affichée en **lecture seule** et soumise via les `defaultValues` du formulaire. **Portée élargie en revue** : le formulaire de **bon de commande** (`PurchaseOrdersPage.tsx`) proposait les mêmes EUR/USD — corrigé aussi, et surtout `PurchaseOrderCreateRequest.currency` reçoit la liste blanche `@Pattern(regexp = "XAF")`, sans quoi le retrait côté formulaire n'aurait été qu'un habillage (l'API acceptait encore un BC en EUR). Schémas Zod alignés sur les **deux** formulaires de facture — `z.literal('XAF')` + `.refine` sur `dueDate >= issueDate` — de sorte que le client refuse en amont ce que le serveur refuse ; le rendu du message d'erreur `dueDate` a été ajouté sur les deux pages, sans quoi le `.refine` aurait bloqué la soumission **sans rien afficher**. **Preuve** : `InvoiceCurrencyAndDateRules.test.tsx` (9 cas : XAF accepté, XOF/EUR/USD/`xaf` refusés, montants 0 et −50 000 refusés, échéance antérieure refusée **avec la clé i18n rattachée au champ `dueDate`**, échéance égale acceptée). Frontend **250/250**, `tsc` 0. Journalisé **PROB-135**. |
| AUDIT-034 | P3 | purchasing / matching | **P2** (mineur — piste d'audit perdue + message divulguant) | Un rapprochement en **échec** n'est jamais persisté (rollback), et son message d'erreur expose les données du bon de commande | `src/main/java/com/oct/invoicesystem/domain/purchasing/service/ThreeWayMatchingService.java:68-80` (`matchingResultRepository.save(result)`) · `src/main/java/com/oct/invoicesystem/domain/invoice/service/InvoiceStateMachineServiceImpl.java:193-198` (la `WorkflowException` du MISMATCH annule la transaction, donc le `save` qui précède) · `ThreeWayMatchingService:189+` (`generateDiscrepancyNotes`, remonté tel quel dans le message d'API) | Le résultat est bien sauvegardé, mais la `WorkflowException` levée juste après pour bloquer la soumission fait remonter le rollback jusqu'au `save`. Résultat : **seuls les rapprochements réussis laissent une trace**, et les échecs — précisément les cas à auditer et à justifier — disparaissent. Cela contredit la nature *append-only* revendiquée pour `ThreeWayMatchingResult` (CLAUDE.md §9) et prive le DAF de tout historique pour décider d'un override. Second défaut sur le même chemin : les notes d'écart, destinées à un usage interne, sont renvoyées telles quelles dans le message HTTP 400 (voir AUDIT-002 : elles divulguent le total du PO et le numéro/date du GRN d'un autre fournisseur). | **PROUVÉE (P3, 2026-07-22)** — dénombrement `select count(*) from three_way_matching_results` = **0** après **quatre** tentatives de soumission ayant toutes déclenché un rapprochement en MISMATCH (traces backend : `Matching result for invoice … : MISMATCH`). Contre-preuve décisive dans la même session : une facture alignée sur `PO-MATCH-001` (dont le GRN couvre 100 % des quantités) est soumise avec succès → `matchingStatus=MATCHED`, `GET /invoices/{id}/matching` → **200** avec le résultat complet, et le compteur passe à **1**. Le mécanisme d'écriture fonctionne donc : seul le chemin d'échec est rollbacké. | Persister le résultat **hors de la transaction bloquante** (`@Transactional(propagation = REQUIRES_NEW)` sur l'enregistrement du résultat, ou enregistrement avant le lever de l'exception dans une transaction dédiée). Séparer par ailleurs les notes d'écart internes du message renvoyé au client : message i18n générique côté API, détail consultable via `GET /invoices/{id}/matching` par les seuls rôles habilités. | **AUDIT-002** (le message divulgue les données d'un tiers) · AUDIT-025 (état vide de `/matching/:id` — la table vide en était la conséquence) | **CORRIGÉ (P6, lot V2-B, 2026-07-23)** — deux volets. (1) *Message divulgant* : les notes d'écart ne remontent plus dans la réponse HTTP, remplacées par la clé générique `matching.mismatch.override_required` ; le détail part au journal applicatif et reste consultable via `GET /invoices/{id}/matching` par les seuls rôles habilités. (2) *Trace perdue* : nouveau bean `MatchingResultRecorder` en `REQUIRES_NEW` — un bean **séparé**, car l'auto-invocation ne passe pas par le proxy Spring et l'annotation serait restée inerte. Appliqué au **seul** chemin MISMATCH : `MATCHED`/`PARTIAL` commitent avec l'appelant et n'y gagneraient rien, alors qu'une transaction isolée ne voit que les lignes **commitées** et casserait un appelant dont les données ne le sont pas encore. **Preuve** : `MatchingResultRecorderIntegrationTest` — écriture puis rollback de la transaction appelante → la ligne survit ; **vérifié contre-factuellement** : sans `REQUIRES_NEW` le test échoue. `MatchingResultRecorderTest` verrouille en plus la propagation et le fait que le recorder soit un bean distinct. |
| AUDIT-035 | P3 | auth / frontend | **P2** (mineur — message d'erreur trompeur) | Serveur injoignable → l'écran de connexion affiche « **Identifiants incorrects** » | `frontend/src/pages/LoginPage.tsx` (branche d'erreur ne distinguant pas l'absence de réponse d'un 401) · même famille que l'intercepteur visé par AUDIT-014 | La gestion d'erreur du login traite tout échec comme un rejet d'authentification. Quand le backend est arrêté (`net::ERR_CONNECTION_REFUSED`, aucune réponse HTTP), l'utilisateur lit qu'il s'est trompé d'identifiants. Effet concret : il essaie d'autres mots de passe, **risque le verrouillage de son compte** (politique de 5 échecs) et n'alerte pas l'exploitation, puisque rien n'indique une panne. Diagnostic faussé pour l'utilisateur comme pour le support. | **PROUVÉE (P3, 2026-07-22)** — `docker stop oct_backend`, puis saisie des identifiants **valides** `aa` / `Test1234!` sur `/login` : l'écran affiche « **Identifiants incorrects** » (capture `docs/audit/p3-captures/AUDIT-login-identifiants-incorrects-alors-que-serveur-down.png`), la console ne portant que `net::ERR_CONNECTION_REFUSED` sur `/api/v1/auth/login`. Aucune réponse HTTP n'a été reçue : le message est donc produit sans le moindre 401. | Dans le gestionnaire d'erreur du login : si `!error.response`, afficher un message de service indisponible (clé i18n dédiée, FR + EN) ; ne conserver « Identifiants incorrects » que pour un **401 effectivement reçu**. À traiter avec le volet (1) d'AUDIT-014, même intercepteur. | **AUDIT-014** (même famille, même correction d'ensemble) | **CORRIGÉ (P6, lot V3-B, 2026-07-24)** — `LoginPage` : « Identifiants incorrects » n'est plus affiché que pour un **401 effectivement reçu** ; absence de réponse → message de service indisponible, 500 → erreur générique, 423 (compte verrouillé) inchangé. **Énumération** : les **deux** autres branches d'erreur de la page — étape OTP (`mfaMutation`) et confirmation d'enrôlement (`setupConfirmMutation`) — souffraient du même défaut et affichaient « Code invalide » sur une panne, poussant l'utilisateur vers le verrouillage à 5 échecs ; elles sont corrigées aussi. **Preuve runtime** (backend arrêté, identifiants **corrects** `aa` / `Test1234!`) : « Identifiants incorrects » **absent**, « Service indisponible » affiché — capture `AUDIT-035-login-service-indisponible.png`. **Tests** : `LoginPageErrors.test.tsx` (4 cas : panne, 401, 423, 500). |
| AUDIT-036 | P3 | workflow / données | **P2** (mineur — étape d'approbation jamais clôturée) | L'étape « Contrôle AA » reste indéfiniment à `PENDING` en base, même une fois le contrôle effectué | `src/main/java/com/oct/invoicesystem/domain/workflow/service/ApprovalServiceImpl.java:73-84` (`assignAA` crée l'étape avec `ApprovalStepStatus.PENDING`) — aucune méthode ne la fait ensuite passer à `APPROVED`, contrairement à `validateN1` (`:99`) et `validateN2` (`:117`) qui créent la leur directement en `APPROVED` | Le contrôle AA crée son étape à `PENDING` puis déclenche la transition d'état de la facture, mais rien ne clôture l'étape. Les niveaux N1 et N2 n'ont pas ce défaut. Conséquence : l'historique d'approbation affiche « EN ATTENTE » pour une étape accomplie, y compris sur des factures déjà **archivées**. C'est une donnée d'audit fausse — l'écran laisse croire qu'une facture payée attend encore son contrôle comptable. | **PROUVÉE (P3, 2026-07-22)** — sur `FAC-2026-00032` (facture validée N1, statut `VALIDE`), la table `approval_steps` donne : étape 0 « Controle AA » → **`PENDING`**, `action_at` **vide** ; étape 1 « Validation N1 - DRH » → `APPROVED`, `action_at` renseigné. **Défaut systémique, pas ponctuel** : dénombrement sur l'ensemble de la base — `Controle AA` = **8 `PENDING`** et 3 `REJECTED`, **aucun `APPROVED`**, alors que les étapes N1/N2 comptent 14 `APPROVED`. Visible en interface : l'écran de détail de facture affiche « Controle AA — **EN ATTENTE** — Bernard Comptable (aa2) » sur une facture pourtant validée. | Dans `assignAA`, créer l'étape directement en `APPROVED` avec `action_at` (le contrôle AA est instantané, comme N1/N2), ou la clôturer explicitement après la transition. Prévoir une migration de rattrapage pour les 8 lignes existantes — **attention** : `approval_steps` est concernée par les contraintes de rétention financière, à vérifier avant toute mise à jour. | — | OUVERT |
| AUDIT-037 | P3 | invoice / API | **P2** (mineur — endpoint public en erreur 500) | `GET /api/v1/invoices/pending-validation` renvoie **500** pour **les 11 rôles validateurs** (`LazyInitializationException`) | `src/main/java/com/oct/invoicesystem/domain/invoice/service/InvoiceService.java:353-355` (`getPendingValidationQueue` — **sans `@Transactional(readOnly = true)`**, contrairement aux méthodes voisines `:357` et `:370`) · exposé par `InvoiceController.java:169-181` | La méthode retourne un `Page<Invoice>` avec ses associations paresseuses non initialisées ; la session Hibernate est fermée avant que le mapping vers `InvoiceDTO` n'accède à `Department.getCode()`, d'où l'exception. Les méthodes voisines du même service portent bien l'annotation — c'est une omission isolée. ⚠ **Sévérité tempérée après vérification runtime** : la page `/approvals` **n'utilise pas cet endpoint** (elle interroge `/invoices?status=…&department=…`), donc aucun écran n'est cassé. Le défaut porte sur un endpoint documenté et autorisé, mais orphelin côté interface. | **PROUVÉE (P3, 2026-07-22)** — balayage systématique des **9 rôles validateurs non couverts en P2** (`drh`, `dg`, `dex`, `com`, `qhsse`, `infra`, `dir_infra`, `atelier`, `dir_tech`) : `GET /api/v1/invoices/pending-validation?size=1` → **500 sur les 9**, sans exception. Journal backend : `org.hibernate.LazyInitializationException: Could not initialize proxy [Department#5c327c42-…] - no session … at Department$HibernateProxy.getCode()`. Contre-preuve interface : `/approvals` en session `drh` **fonctionne** et liste bien 2 factures en attente (capture `docs/audit/p3-captures/P3-approvals-drh-fonctionnelle.png`) ; trace réseau → seulement `GET /invoices?status=EN_CONTROLE_AA…` et `GET /invoices?status=EN_VALIDATION_N1…`, **jamais** `/pending-validation`. | Ajouter `@Transactional(readOnly = true)` sur `getPendingValidationQueue`, à l'identique de `getMatchingResult` (`:357`). Décider ensuite en P5 si l'endpoint doit être conservé (aucun appelant frontend) ou retiré ; s'il est conservé, le couvrir par un test d'intégration avec un rôle validateur — il aurait suffi à détecter le défaut. | AUDIT-013 (dette de tests) | **CORRIGÉ (P6, lot V2-E, 2026-07-23)** — ⚠ **le diagnostic du finding était faux et a été réfuté avant correction** : il attribuait le 500 à un `@Transactional(readOnly = true)` manquant sur `getPendingValidationQueue`, or `git log` montre que l'annotation existe **depuis l'écriture de la méthode** (commit `0cde8a3`). La vraie cause est au dépôt : le contrôleur mappe la `Page<Invoice>` vers `InvoiceDTO` **après** la fermeture de la transaction du service, donc les proxys `department`/`supplier` ne sont plus résolvables. Les deux requêtes de liste voisines fonctionnent précisément parce qu'elles portent `@EntityGraph` ; celle-ci ne l'avait pas — elle l'a désormais. **Preuve** : `SortAndPendingQueueIntegrationTest` **seede une facture `EN_VALIDATION_N1`** — sur une file vide l'endpoint renvoyait 200 même sans le correctif et n'aurait rien prouvé. **Vérifié contre-factuellement** : sans `@EntityGraph`, le test échoue en reproduisant exactement `LazyInitializationException: Could not initialize proxy [Department#…] - no session`. |
| AUDIT-038 | P3 | supplier-portal / navigation | **P3** (cosmétique — lien inadapté, sans conséquence) | Le fil d'Ariane du portail fournisseur pointe vers `/dashboard` (route **staff**) au lieu de `/supplier/dashboard` | `frontend/src/components/layout/Header.tsx:77` (`<Link to="/dashboard">`, chemin **codé en dur**) — le `Header` est partagé entre `AppShell` (staff) et `SupplierLayout` (portail) | Le composant d'en-tête ne tient pas compte du contexte dans lequel il est monté : il renvoie toujours vers le tableau de bord staff. Un fournisseur qui clique sur « TABLEAU DE BORD » demande donc une route interne. **Vérifié sans conséquence de sécurité** : `ProtectedRoute` intercepte et redirige vers `/supplier/dashboard` — le fournisseur revient simplement à son point de départ. Défaut de finition, visible en démonstration. | **PROUVÉE (P3, 2026-07-22)** — relevé DOM en session `supplier` sur `/supplier/dashboard` : l'unique lien de l'en-tête est `{ texte: "TABLEAU DE BORD", href: "/dashboard" }`. Clic effectif → l'URL finale reste **`/supplier/dashboard`** (redirection par le garde de route, aucun écran d'erreur, aucune fuite). | Rendre la cible du fil d'Ariane dépendante du contexte : `/supplier/dashboard` quand le chemin courant commence par `/supplier`, `/dashboard` sinon — ou passer la racine en propriété depuis le layout appelant. | AUDIT-026 (même famille : cloisonnement des routes fournisseur) | OUVERT |
| AUDIT-039 | P4 | i18n / export — **transverse** | **P1** (majeur — deux exports totalement inopérants) | Clé i18n `invoice.status.en_controle_aa` **absente des deux catalogues** : tout export de liste tombe en **HTTP 500** | `src/main/resources/i18n/messages_fr.properties` et `messages_en.properties` (9 clés `invoice.status.*` déclarées pour **10** valeurs de l'enum) · `src/main/java/com/oct/invoicesystem/domain/invoice/model/InvoiceStatus.java` (`EN_CONTROLE_AA`) · sites d'appel : `InvoiceService.java:344`, `ReportServiceImpl.java:280,297,298,340` | La clé est construite dynamiquement par `"invoice.status." + status.name().toLowerCase()`. L'état `EN_CONTROLE_AA` a été ajouté au code (cf. AUDIT-017) **sans** que la clé correspondante soit ajoutée aux catalogues i18n. `messageSource.getMessage(...)` est appelé **sans valeur de repli**, donc il lève `NoSuchMessageException` au lieu de dégrader. Il suffit d'**une seule** facture dans cet état pour que l'export entier échoue. La parité FR/EN mesurée en P1 (310 clés de chaque côté) est réelle mais **ne garantit pas la complétude** : les deux fichiers sont incomplets *de la même façon*, ce qu'une comparaison FR↔EN ne peut pas détecter. | **PROUVÉE (P4, 2026-07-23)** — preuve complète : `docs/audit/p4-captures/AUDIT-039-export-500-cle-i18n-manquante.txt`. `GET /api/v1/invoices/export?format=csv` → **500**, `format=xlsx` → **500**, `GET /api/v1/reports/export/excel` → **500**, en **FR comme en EN** (`Accept-Language`). Journal backend : `org.springframework.context.NoSuchMessageException: No message found under code 'invoice.status.en_controle_aa' for locale 'fr'`. **Isolation décisive** : le même export filtré sur un autre statut réussit — `?status=BROUILLON` → **200**, `?status=ARCHIVE` → **200** — tandis que `?status=EN_CONTROLE_AA` → **500**. Base : 1 seule facture dans cet état suffit à casser l'export global. ✅ À décharge : le PDF d'une facture **unitaire** reste en 200, y compris sur une facture `EN_CONTROLE_AA` (ce chemin n'utilise pas la clé) — seuls les exports de **liste** sont touchés. | Ajouter `invoice.status.en_controle_aa` aux deux catalogues (FR : « En contrôle AA » ; EN : « Under AA review »). **Correctif de robustesse indispensable en complément** : utiliser la surcharge `getMessage(code, args, defaultMessage, locale)` sur les 5 sites d'appel, pour qu'une clé manquante dégrade en libellé brut au lieu de renvoyer une 500. Ajouter un test paramétré balayant **les 10 valeurs** de `InvoiceStatus` et vérifiant que chaque clé existe dans les deux catalogues — ce test aurait empêché le défaut. | **AUDIT-017** (même cause racine : `EN_CONTROLE_AA` ajouté au code sans mise à jour de la doc ni des catalogues) · AUDIT-013 (dette de tests) | **CORRIGÉ (P6, lot V1-A, 2026-07-23)** — (a) clé `invoice.status.en_controle_aa` ajoutée aux **deux** catalogues (FR « En contrôle AA » en échappement `ô`, EN « Under AA review ») ; (b) **correctif de robustesse** : helper `shared/i18n/InvoiceStatusLabels` passant systématiquement un `defaultMessage` (libellé porté par l'enum) — les 5 sites d'appel (`InvoiceService:343`, `ReportServiceImpl:280,297,298,340`) y sont migrés, une clé absente dégrade désormais en libellé brut au lieu de renvoyer une 500 ; (c) **preuve** : `InvoiceStatusLabelsTest` — test paramétré `@EnumSource(InvoiceStatus)` vérifiant **sans repli** l'existence de la clé dans les 2 catalogues pour les **10** valeurs, + cas « catalogue vide → dégradation au lieu de `NoSuchMessageException` » (23 tests). Suite backend **651/0/0 BUILD SUCCESS** (plancher 628), frontend **237/237**, lancées séparément. Défauts collatéraux corrigés : NPE latent sur `getStatus()` (`ReportServiceImpl:280`) et repli sémantiquement faux « statut nul → brouillon » (`:340`). Journalisé **PROB-131**. |
| AUDIT-040 | P4 | report / performance | **P2** (mineur — N+1 quadratique, invisible au volume actuel) | `GET /reports/supplier/{id}/performance` relit **toute** la table d'historique une fois **par facture** (`findAll()` dans une boucle) | `src/main/java/com/oct/invoicesystem/domain/report/service/ReportServiceImpl.java:675` (`historyRepository.findAll().stream().filter(...)`) **à l'intérieur** de la boucle ouverte ligne 674 · même méthode, `:638` (`invoiceRepository.findAll()` puis filtrage en mémoire) | Le filtrage est fait en Java au lieu de l'être en SQL : pour chaque facture du fournisseur, l'intégralité de `invoice_status_history` est chargée puis filtrée. Le coût est **O(factures × historique)**, soit quadratique en volume. Un `findByInvoiceId` — ou un seul chargement hors boucle indexé par `invoiceId` — suffirait. Le défaut est aujourd'hui indolore (base de démonstration) mais dégénère en production : à 10 000 factures et 100 000 lignes d'historique, un seul appel génère des centaines de millions de lignes lues. | **PROUVÉE (P4, 2026-07-23)** — preuve : `docs/audit/p4-captures/AUDIT-040-n1-supplier-performance.txt`. Mesure par compteur `seq_scan` PostgreSQL avec `pg_stat_force_next_flush()` (le collecteur étant asynchrone, un relevé immédiat renvoie 0 — piège écarté). Fournisseur `40433645-…` porteur de **25 factures** : `seq_scan(invoice_status_history)` passe de **378 à 403**, soit **delta = 25**, exactement une lecture complète de la table par facture — le facteur prédit par le code. Reproduit deux fois. Latence actuelle 114 ms (volume de démonstration). ✅ **Contre-mesure, à décharge** : les autres rapports ont été mesurés et sont **sobres** — `/reports/bottlenecks`, `/reports/payment-cycle`, `/reports/executive-summary` consomment 1 à 7 scans malgré leurs propres `findAll()`, car ils ne bouclent pas. Le défaut est **isolé à cette méthode**, il n'est pas systémique. | Remplacer `historyRepository.findAll()` par un `findByInvoiceIdIn(ids)` unique **hors boucle**, indexé en `Map<UUID, List<InvoiceStatusHistory>>` ; et remplacer `invoiceRepository.findAll()` (`:638`) par un `findBySupplierId(supplierId)`. Aucun changement de comportement fonctionnel attendu — bon candidat à un test de non-régression sur la valeur retournée. | — | OUVERT |
| AUDIT-041 | P4 | documentation / API | **P2** (mineur — contrat d'API faux, trompeur pour tout intégrateur) | `docs/API.md` ne décrit que **86 des 219 endpoints (39 %)** et documente **19 endpoints qui n'existent pas** | `docs/API.md` (488 lignes) vs les 40 contrôleurs de `src/main/java/**/controller/*.java` · exemples de chemins fantômes : `POST /invoices/{id}/validate-n1`, `/bon-a-payer`, `/reject` (le vrai préfixe est `/invoices/{id}/`**`workflow`**`/…`), `GET /auth/me`, `POST /auth/logout`, `GET /reports/kpi` (le vrai est `/reports/kpis`) | Le document n'a pas suivi les refontes successives (introduction du sous-chemin `/workflow`, renommage `kpi`→`kpis`). Deux écarts distincts : **(a) couverture** — 152 endpoints réels non documentés, dont des domaines entiers (accès, annonces, délégations, conformité, rétention, intégrations, sécurité) ; **(b) exactitude** — 19 chemins documentés renvoient 404. Le plus grave est l'écart de **rôles** : API.md accorde `ADMIN` sur au moins 15 surfaces financières (créer/modifier/supprimer une facture, `bon-a-payer`, paiements, rapports, audit) alors que le code l'exclut explicitement (`!hasRole('ADMIN')`). ⚠ **Le code fait foi et est conforme à la SoD** : il faut corriger la documentation, **surtout pas** ouvrir l'accès à ADMIN — ce serait une régression de sécurité (même arbitrage qu'en O-4/AUDIT-017). | **PROUVÉE (P4, 2026-07-23)** — preuve : `docs/audit/p4-captures/AUDIT-041-api-md-vs-code.txt`. Extraction automatique : **219** endpoints réels (annotations `@*Mapping`) contre **86** documentés ; 152 non documentés, 19 fantômes. **Vérification runtime des fantômes avec un jeton `daf` valide** : `/auth/me` → **404**, `/reports/kpi` → **404**, `/invoices/{id}/payment` → **404**, `/invoices/{id}/export?format=pdf` → **404**, `/reports/export?format=pdf` → **404**, `POST /auth/logout` → **404**, `POST /invoices/{id}/validate-n1` → **404**. Contre-épreuve dans le même souffle : le vrai chemin `POST /invoices/{id}/workflow/validate-n1` répond **400** (donc il existe). Écart de rôles relevé ligne à ligne (API.md:48, 50, 51, 101, 102, 125, 128, 139, 141, 184, 193-196, 255) contre `InvoiceController.java:77,99,141,195,206,233`. | Régénérer `docs/API.md` **depuis le code** plutôt que de le corriger à la main — la source fiable existe déjà : l'application expose OpenAPI/Swagger (annotations `@Operation` présentes). Décision de cadrage en P5 : (a) publier le contrat généré et réduire API.md à un guide d'usage, ou (b) le maintenir à la main avec un test de cohérence en CI. Dans les deux cas, corriger **en priorité** les colonnes « Roles » qui accordent ADMIN sur des surfaces financières, car c'est le seul écart porteur d'un risque de régression SoD si quelqu'un « alignait le code sur la doc ». | **AUDIT-017** (même famille : documentation en retard sur le code) · AUDIT-018 (SoD ADMIN) | OUVERT — **cadrage tranché en P5 (D6)** : **régénérer depuis OpenAPI** (springdoc 2.7.0 présent), API.md réduit à un guide ; corriger en priorité les colonnes « Roles » → lot V4-B |
| AUDIT-042 | P4 | i18n / frontend | **P2** (mineur — la préférence de langue est perdue à chaque rechargement) | `lng: 'fr'` forcé dans la configuration i18next court-circuite le `LanguageDetector` : l'anglais ne survit pas à un F5 | `frontend/src/i18n/index.ts:16` (`lng: 'fr'`) en contradiction avec `:21-25` (`detection: { order: ['localStorage','navigator'], caches: ['localStorage'], lookupLocalStorage: 'i18nextLng' }`) | La configuration se contredit : `LanguageDetector` est branché (`:8`) et paramétré pour lire `localStorage`, mais l'option `lng` **impose** une langue au démarrage et prend le pas sur toute détection. Le bouton de bascule écrit bien `i18nextLng='en'`, et cette valeur est correctement relue… mais jamais appliquée à l'initialisation. Conséquence : un utilisateur anglophone doit re-basculer la langue **à chaque chargement de page**, ce qui rend le bilinguisme — exigence explicite de CLAUDE.md §1 et §3 — inutilisable en pratique. | **PROUVÉE (P4, 2026-07-23)** — preuve : `docs/audit/p4-captures/AUDIT-042-043-i18n-langue-et-formats.txt`. Session Playwright, compte `daf` : (1) clic sur `#btn-language-switcher` → `localStorage.i18nextLng='en'` et interface anglaise vérifiée au DOM (« Purchase Orders », « Payments », « Approval Queue ») ; (2) navigation vers une autre page (rechargement complet) → `i18nextLng` **revient à `'fr'`** et l'interface repasse en français (« Bons de Commande », « Paiements »). Reproduit **deux fois**, sur `/invoices` et `/payments`. | Retirer la ligne `lng: 'fr'` et conserver `fallbackLng: 'fr'`, qui suffit à garantir le français par défaut **tout en** laissant le détecteur restaurer le choix de l'utilisateur. Corriger au passage `AUDIT-021` (attribut `lang` de `<html>`) dans le même lot : les deux défauts portent sur la même chaîne de décision de la langue. | **AUDIT-021** (même famille — `<html lang>` jamais synchronisé) · AUDIT-043 | **CORRIGÉ (P6, lot V3-C, 2026-07-24)** — la ligne `lng: 'fr'` est retirée : elle court-circuitait `LanguageDetector`, si bien que le choix stocké dans `localStorage` était relu puis **écrasé** à chaque chargement. `fallbackLng: 'fr'` suffit à garantir le français par défaut. **Défaut supplémentaire découvert en activant réellement la détection** : `load: 'languageOnly'` + `supportedLngs` ont dû être ajoutés — seules les ressources `fr` et `en` existent, et un navigateur annonçant un code régional (`en-US`, `en-GB`, `fr-CA`) résolvait vers une langue **sans catalogue**. Le `lng` codé en dur masquait ce bug ; il est désormais couvert (vérifié en runtime : après `localStorage.clear()`, un navigateur `en-US` obtient bien le catalogue anglais). **Preuve runtime** (`daf`) : bascule EN, puis navigation avec **rechargement complet** → l'interface **reste en anglais** et `i18nextLng` vaut toujours `en` (avant : retour à `fr`). **Tests** : `formatLocale.test.ts` (persistance + absence de `lng` imposé) ; **contre-preuve** — 5/8 échouent avec l'ancienne configuration. |
| AUDIT-043 | P4 | i18n / cohérence inter-écrans | **P2** (mineur — trois formats concurrents pour les mêmes données) | Dates et montants s'affichent dans **trois formats différents selon l'écran**, et le helper central ignore la langue de l'utilisateur | **(A)** `frontend/src/lib/format.ts:1-19` — constante `FR = 'fr-FR'` **forcée**, utilisée par 29 fichiers · **(B)** ternaire `i18n.language === 'en' ? 'en-US' : 'fr-FR'` dupliqué à la main dans **7** sites · **(C)** `frontend/src/pages/ReportsPage.tsx:479,513` — `toLocaleDateString()` / `toLocaleString()` **sans locale** (suit le navigateur) · **(D)** `frontend/src/pages/admin/AdminBackupsPage.tsx:91,172` — `'fr-FR'` en dur hors du helper | Le helper censé centraliser le formatage (« bon » motif, adopté par 29 fichiers) est précisément celui qui est **faux** : il force `fr-FR` quelle que soit la langue. Les pages qui font le travail correctement (régime B) le font par duplication manuelle, et deux sites laissent le navigateur décider (régime C), ce qui rend le rendu **dépendant de la machine du visiteur**. Résultat : dans une même session anglaise, deux écrans affichent le même type de donnée dans deux formats différents. | **PROUVÉE (P4, 2026-07-23)** — mesure DOM dans **une seule session** basculée en anglais (compte `daf`), preuve : `docs/audit/p4-captures/AUDIT-042-043-i18n-langue-et-formats.txt` + capture `docs/audit/p4-captures/AUDIT-format-invoices-EN-montants-FR-dates-ISO.png`. `/payments` (régime B) : dates `7/22/2026`, séparateur **virgule** (5 occurrences) → format US **correct**. `/invoices` (régime A) au même instant : montants `1 500 000 XAF` avec séparateur **espace** (20 occurrences, **0** virgule) → format **français** dans une interface anglaise, et dates rendues en **ISO brut** `2026-06-01` (ni FR, ni EN). | Faire du helper la source unique et le rendre sensible à la langue : `formatAmount`/`formatDate`/`formatDateTime` doivent lire `i18n.language` (ou recevoir la locale en paramètre) au lieu de la constante `FR`. Migrer ensuite les régimes B, C et D vers ce helper — chantier mécanique, sans risque fonctionnel, bon candidat pour un sous-agent en P6. Traiter les dates ISO brutes de `/invoices` au passage (elles ne passent par aucun formateur). | AUDIT-042 · AUDIT-021 (même chaîne de décision de la langue) | **CORRIGÉ (P6, lot V3-C, 2026-07-24)** — le helper central `lib/format.ts` (utilisé par 29 fichiers) lit `i18n.language` **à l'appel** au lieu de la constante `FR` forcée, via un `currentLocale()` exporté. Point de conception : il importe `i18next` et **non** `@/i18n`, sinon il tire `initReactI18next` dans les 10 fichiers de test qui mockent `react-i18next`. Les régimes concurrents sont éliminés par **énumération exhaustive** : **7** sites du régime B (ternaire dupliqué) migrés vers le helper, **2** du régime C (aucune locale, rendu dépendant du navigateur), **2** du régime D (`fr-FR` en dur) passés à `currentLocale()`, et les **dates ISO brutes** de `/invoices` (`issueDate`/`dueDate`, qui ne passaient par aucun formateur) enfin formatées. **Preuve runtime** en session anglaise : `/invoices` affiche `1,500,000 XAF` en **virgule** avec **0** occurrence de séparateur espace (avant : 20 espace / 0 virgule) et les dates `2026-06-01` deviennent `6/1/2026` ; `/payments` au même instant affiche le **même** format US → cohérence inter-écrans rétablie. Retour en FR : dates `22/07/2026`. Devise **XAF** conservée partout. **Tests** : `formatLocale.test.ts` (montants, dates, date-heure et valeurs vides sur les **deux** locales). |
| AUDIT-044 | P4 | i18n / frontend | **P3** (cosmétique — écran d'erreur non traduit) | `ErrorBoundary` affiche **3 chaînes françaises codées en dur** : l'écran de plantage reste en français pour un utilisateur anglophone | `frontend/src/components/ErrorBoundary.tsx:35` (« Une erreur est survenue sur cette page »), `:38` (« Erreur inattendue »), `:45` (« Réessayer ») — aucun appel à `t()` dans le fichier | Violation de la règle CLAUDE.md §3 (« jamais de texte français ou anglais en dur »). Le cas est particulier : un composant de classe React ne peut pas utiliser le hook `useTranslation`, ce qui explique probablement l'omission — mais la solution existe (passer les libellés en props depuis un parent, ou utiliser `i18n.t()` directement, qui ne dépend pas d'un hook). Impact limité : l'écran n'apparaît qu'en cas de plantage d'une page. ✅ Recherche exhaustive menée sur les ~56 pages : ce sont les **seules** chaînes en dur trouvées, avec `Français`/`English` dans `AdminUserFormPage.tsx:173`, qui sont des **noms de langue** et donc légitimement non traduits. Le reste du frontend est propre. | **PROUVÉE (P4, 2026-07-23)** — balayage automatique de tout `frontend/src` (texte JSX + attributs `placeholder`/`title`/`aria-label`, filtré sur marqueurs francophones) : **2 fichiers, 2 occurrences** seulement, dont `ErrorBoundary.tsx`. Lecture du fichier confirmant les 3 littéraux et l'absence totale de `t()`. | Utiliser `i18n.t('errors.boundaryTitle')` etc. via l'import direct de l'instance i18next (pas le hook), et ajouter les 3 clés aux deux catalogues `fr.json`/`en.json`. | AUDIT-015 (famille i18n frontend) | OUVERT |
| AUDIT-045 | P4 | frontend / performance | **P3** (dette — chunk 3× trop lourd sur une page très visitée) | `InvoiceDetailPage` embarque **react-pdf** en import statique : 378 Ko (110 Ko gzip) chargés même sans ouvrir de document | `frontend/src/pages/InvoiceDetailPage.tsx:11` (`import { DocumentViewerModal } from '@/components/invoice/DocumentViewerModal'`, statique) · la modale n'est montée que conditionnellement (`:518`) · `frontend/src/components/invoice/DocumentViewerModal.tsx` (rendu PDF via react-pdf) | L'import statique force l'inclusion de react-pdf dans le chunk de la page, alors que la visionneuse n'est utile qu'au clic sur un document. Le motif correct est déjà le standard du projet : `AppRoutes.tsx:10-12` utilise `lazy()` pour toutes les routes. `InvoiceDetailPage` est le **3ᵉ plus gros fichier** du bundle (378 Ko) derrière le worker PDF et l'entrée applicative, et c'est l'une des pages les plus visitées du système. | **PROUVÉE (P4, 2026-07-23)** — inventaire du bundle déployé (`docker exec oct_frontend`) : `InvoiceDetailPage-BJd4IKUc.js` = **378 467 o** (110 640 o gzip), contre ~40 Ko pour `ReportsPage` et `DashboardPage`. Lecture du code confirmant l'import statique et le montage conditionnel. ✅ **Mesuré à décharge — le reste de la performance frontend est sain** : gzip actif (entrée 570 Ko → **180 Ko**), `Cache-Control: public, immutable, max-age=31536000`, découpage en **116 chunks** (lazy loading opérant), et Core Web Vitals excellents sur `/reports` (**FCP 112 ms, LCP 492 ms**, 1 Mo brut / ~300 Ko gzip au premier chargement). Aucun autre défaut de performance frontend relevé. | Charger la modale à la demande : `const DocumentViewerModal = lazy(() => import('@/components/invoice/DocumentViewerModal'))` + `<Suspense>`, comme le fait déjà `AppRoutes.tsx`. Gain attendu : ~110 Ko gzip en moins au chargement de chaque fiche facture. | — | OUVERT |
| AUDIT-046 | P6 | frontend / a11y -- theme sombre | **P2** (mineur -- texte saisi illisible en theme sombre) | En theme sombre, les champs natifs sans classe `bg-*` gardent le fond blanc du navigateur alors que leur texte suit `--ink` (clair) : encre claire sur blanc, 1,23:1 | `frontend/src/index.css` (@layer base, aucune regle par defaut sur input/select/textarea) -- 178 champs sur 49 fichiers sans `bg-*` ; ex. `/login` `#username`, `/register/supplier`, `/admin/announcements` | Les champs natifs n heritent PAS du fond de la page ; sans classe de fond explicite ils restent blancs. La primitive `ui/Input.tsx` posait deja `bg-surface`, mais 178 champs ne l utilisent pas. Decouvert par le balayage axe-core de V4-A -- non vu en P2 car la campagne forcait la classe `dark` apres chargement (clef reelle = `oct-theme`), mesurant en fait le theme clair. | **PROUVEE (V4-A, 2026-07-24)** -- runtime Playwright, theme sombre reel (`oct-theme=dark` + `colorScheme`) : `/login` `#username` computed `color:rgb(233,231,226)` sur `background:rgb(255,255,255)` = 1,23:1 (seuil 4,5:1), page pourtant sombre (`body` `rgb(19,22,27)`). axe-core : 49 noeuds `#e9e7e2/#ffffff`. Cas extremes releves au passage : `#0f2540` sur `#161c24` = 1,1:1 (navy sur navy). | Regle @layer base : `input:not(checkbox):not(radio):not(range):not(color), select, textarea { bg-surface text-ink }` + `::placeholder { text-ink-faint }`, surchargeable par les utilitaires Tailwind. Verifie runtime : clair inchange, sombre `#username` bg `rgb(23,27,33)` = lisible. | AUDIT-020 (famille a11y contraste) | CORRIGE (V4-A, 42f1594) : regle @layer base ; balayage apres = 0 noeud `#e9e7e2/#ffffff` ; les autres contrastes de tokens (pos/info/warn/navy) renvoyes en vague 5 (option C utilisateur) |

---

## Baseline de tests (établie au début de P1, 2026-07-22)

Mesurée **avant** toute analyse, sur `audit/exhaustif-p1-code-statique` (= `6d71d6d`, aucun code
modifié depuis `main`). C'est la référence contre laquelle toute régression de P6 sera jugée.

| Suite | Commande | Résultat |
|---|---|---|
| Backend | `./mvnw.cmd test` | **628 tests · 0 échec · 0 erreur · 0 ignoré** — `BUILD SUCCESS` |
| Frontend | `npm run test` (vitest) | **237 tests · 60 fichiers · 0 échec** |

⚠ Un premier lancement **simultané** des deux suites a produit 1 échec frontend apparent
(`Failed to start forks worker` / `Timeout waiting for worker to respond`) : c'est une
**contention CPU** entre Maven et vitest, pas un test cassé. Relancée seule, la suite frontend
passe intégralement. **Ne pas lancer les deux suites en parallèle.**

La baseline est **entièrement verte des deux côtés** : l'audit démarre sur une suite saine.
P1 n'a modifié **aucun** fichier de code — seuls les documents d'audit sont touchés.

## Synthèse par statut

| Statut | Nombre |
|---|---|
| OUVERT | 11 |
| EN COURS | 0 |
| CORRIGÉ | 35 |
| HORS-SCOPE | 0 |
| **Total** | **46** |

## Synthèse par sévérité

| Sévérité | Nombre | IDs |
|---|---|---|
| P0 (bloquant) | 0 | — |
| P1 (majeur) | **10** | AUDIT-001, AUDIT-002, AUDIT-018, AUDIT-019, AUDIT-026, AUDIT-028, AUDIT-029, AUDIT-031, AUDIT-032, AUDIT-039 |
| P2 (mineur) | **24** | **AUDIT-003** (entrant, révisé P1→P2 en P5), AUDIT-004, AUDIT-005, AUDIT-007 → AUDIT-013, AUDIT-020 → AUDIT-025, AUDIT-030, AUDIT-033 → AUDIT-037, AUDIT-040 → AUDIT-043 |
| P3 (cosmétique / dette) | **11** | **AUDIT-006** (entrant, révisé P2→P3 en P5), AUDIT-014 → AUDIT-017, AUDIT-027, AUDIT-038, AUDIT-044, AUDIT-045 |
| À confirmer | 0 | — (AUDIT-001 tranché en P3) |

> ⚠ **Révision de sévérité tranchée en P5 (décision D7, 2026-07-23)** : AUDIT-003 passe de **P1 à P2**
> (le backend renvoie bien 403 — aucune fuite, seulement une incohérence d'écran de refus) et
> AUDIT-006 de **P2 à P3** (hypothèse réfutée en P3 : aucun 500 constaté en runtime). Les deux restent
> à corriger. Le décompte P1 passe donc de 11 à **10** et P3 de 10 à **11**.

## Synthèse par phase

| Phase | Findings émis | État |
|---|---|---|
| P0 — Amorçage + compréhension | 1 (pré-identifié) | ✅ terminée |
| P1 — Audit code statique | 16 (AUDIT-002 → AUDIT-017) | ✅ terminée |
| P2 — Audit visuel + responsive | 10 (AUDIT-018 → AUDIT-027) + preuve runtime apportée à AUDIT-003, AUDIT-005, AUDIT-007 | ✅ terminée (échantillon représentatif — voir note de couverture) |
| P3 — Audit fonctionnel end-to-end | **11 (AUDIT-028 → AUDIT-038)** + preuve runtime apportée à **AUDIT-001, 002, 007, 010, 011, 014, 015** et **2 hypothèses réfutées** (AUDIT-006 intégralement, AUDIT-010 partiellement) | ✅ terminée |
| P4 — Audit transverse (perf, i18n, a11y, cohérence, erreurs) | **7 (AUDIT-039 → AUDIT-045)** + chiffrage exact apporté à **AUDIT-014** (84/95 mutations sans `onError`) et extension aux pages publiques de **AUDIT-020** et **AUDIT-024** | ✅ terminée |
| P5 — Consolidation + backlog priorisé | **0 (aucun finding — la P5 produit une décision)** + **8 décisions de cadrage tranchées** avec l'utilisateur et **2 sévérités révisées** (AUDIT-003, AUDIT-006) | ✅ terminée |
| P6 — Correction par vagues | — | **en cours** — **Vagues 1, 2 et 3 TERMINÉES ; vague 4 EN COURS** — V1-A→V1-E (8 findings), V2-A→V2-E (12 findings), **V3-A→V3-D (11 findings)** et **V4-A commencée : AUDIT-024, 020, 023 `CORRIGÉ` + AUDIT-046 découvert et `CORRIGÉ` dans le même lot** (accessibilité WCAG 2.1 AA, commit 42f1594), soit **35 `CORRIGÉ` / 46**. Gate V4-A : frontend **298/298 (71 fichiers)**, `tsc` 0, build vert, axe-core rejoué sur **51 pages × 2 thèmes** → `label`/`select-name`/`button-name` = **0** et contraste 369→137 (les 137 restants = famille de tokens hors AUDIT-020, renvoyée en vague 5, option C utilisateur). Voir `docs/AUDIT_BACKLOG.md` |

---

## Décisions de cadrage — Phase 5 (2026-07-23)

Les huit décisions ci-dessous sont **métier**, pas techniques : elles ont été tranchées **avec
l'utilisateur** et sont également consignées dans la ligne de chaque finding concerné (colonne
Statut). Le détail, le rationnel et les lots correspondants sont dans **`docs/AUDIT_BACKLOG.md` §1**.

| # | Findings | Décision retenue | Lot P6 |
|---|---|---|---|
| **D1** | AUDIT-001 · AUDIT-031 | Le fournisseur **doit** pouvoir facturer contre un PO : sélecteur de PO + persistance des `lineItems`. **Ordre imposé : AUDIT-031 d'abord** (sinon les factures seraient systématiquement rejetées). | V1-B |
| **D2** | AUDIT-029 | **Paiement intégral obligatoire** (`amountPaid == invoice.amount`) ; règlements partiels écartés. | V1-C |
| **D3** | AUDIT-030 | **Séparer `PAYE` et `ARCHIVE`** : l'archivage devient une action explicite ; le filtre « Payé » est conservé. | V1-C |
| **D4** | AUDIT-032 · AUDIT-033 | **Mono-devise XAF stricte** : liste blanche côté DTO, EUR/USD retirés du sélecteur. Chantier multidevise **écarté**. | V1-D |
| **D5** | AUDIT-008 · AUDIT-009 · piste **O-3** | **Retirer les 3 surfaces à l'ADMIN** : seuils de matching → **DAF** ; disposition `PURGED` → **double validation** (ADMIN propose, DAF confirme) ; référentiel fournisseur → **AA**. **La piste O-3, laissée ouverte en P1, est close par cette décision** et devient un item du lot (pas un nouveau finding). | V2-A |
| **D6** | AUDIT-041 | **Régénérer `docs/API.md` depuis OpenAPI** (springdoc 2.7.0 déjà au `pom.xml:223-225`) ; API.md réduit à un guide d'usage. Priorité aux colonnes « Roles » : **la doc est fausse, pas le code**. | V4-B |
| **D7** | AUDIT-003 · AUDIT-006 | **Révisions de sévérité confirmées** : AUDIT-003 **P1→P2**, AUDIT-006 **P2→P3**. Les deux restent à corriger. | V3-D · V4-D |
| **D8** | AUDIT-013 | Rattrapage **ciblé** : `AuthController`, `MatchingConfigController`, `SupplierController` **+ un test par finding corrigé**. Les 11 autres contrôleurs = dette assumée. | V5-A |

**Aucun finding n'est mis `HORS-SCOPE`** : les 45 seront tous corrigés en P6. Le statut `HORS-SCOPE`,
qui aurait exigé une validation explicite tracée de l'utilisateur, n'est utilisé nulle part.

---

## Pistes ouvertes P0 — arbitrage rendu en P1 (clos)

Les 10 observations de P0 ont toutes été instruites **dans le code réel** en P1. Chacune est soit
promue en `AUDIT-NNN`, soit **écartée avec justification**. Cette section est close : elle ne doit
plus être ré-instruite dans les phases suivantes.

| # | Observation P0 | Verdict P1 | Justification (vérifiée dans le code) |
|---|---|---|---|
| O-1 | `EN_CONTROLE_AA` absent de la doc | **Écartée partiellement → AUDIT-017** | La prémisse est **fausse pour `docs/WORKFLOW.md`** : les lignes 49-50, 81, 92, 135-138 documentent explicitement l'étape et le rôle requis par état source. Seul `CLAUDE.md §5` est périmé → consigné en AUDIT-017 (P3, documentaire). Aucun défaut de code. |
| O-2 | `POST /validate-n1` ouvert au DAF | **ÉCARTÉE — pas une faille** | Défense en profondeur en aval. `RoleMatchGuard.java:56-57` exige `dept.getN1Role()` pour `VALIDATE_N1`, et `ApprovalServiceImpl.validateN1` (`:94`) appelle `checkRole(currentUser, invoice.getDepartment().getN1Role())`. Un DAF sans le rôle N1 du département est donc **refusé** (`AccessDeniedException`). Le `@PreAuthorize` large n'est qu'un premier filtre grossier. S'ajoutent `ensureNotSubmitter`, `ensureNotN1Approver` (N2 ≠ N1) et `ensureWithinApprovalLimit`. |
| O-3 | `/api/v1/suppliers/**` ouvert à ADMIN + AA vs frontend AA seul | **ÉCARTÉE en P1 — à re-trancher en P5** | Écart réel et confirmé (10 endpoints `hasAnyRole('ADMIN','ASSISTANT_COMPTABLE')`, `SupplierController.java:61,67,75,81,95,141,149,171,193`, plus `:158` ADMIN seul), mais ce n'est pas un défaut isolé : c'est **le même arbitrage SoD** que AUDIT-008/009 (jusqu'où va l'ADMIN « technique »). Le référentiel fournisseur contient des coordonnées bancaires chiffrées → à trancher avec le cahier des charges en P5, pas à décider unilatéralement en P1. Couvert par AUDIT-013 pour le volet « aucun test ». |
| O-4 | Override matching : DAF seul (code) vs « DAF ou ADMIN » (doc) | **Promue → AUDIT-017** | Le **code fait foi** et est conforme à la SoD : `InvoiceController.java:322` = `hasRole('DAF')`. La doc est fausse — et la Javadoc de la méthode elle-même (`:324`) répète l'erreur. « Corriger » en ouvrant l'accès à ADMIN serait une **régression de sécurité**. |
| O-5 | `RECORD_PAYMENT` / `ARCHIVE` sans garde de machine à états | **ÉCARTÉE — contrôle équivalent en service** | `ARCHIVE` a une garde explicite : `InvoiceStateMachineServiceImpl.java:76-82` rejette l'événement si le drapeau `AUTO_ARCHIVE` est absent (« Archive transition is automatic »). `RECORD_PAYMENT` n'est émis que par `PaymentServiceImpl.finalizePayment` (`:129`), atteignable uniquement via `recordPayment`, qui vérifie le statut `BON_A_PAYER` (`:80`), l'absence de paiement existant (`:83`) et est protégé par `@PreAuthorize("hasRole('ASSISTANT_COMPTABLE')")`. Le contrôle est déplacé, pas absent. |
| O-6 | `InvoiceDetailPage` sans `PageRoleGuard` | **PROMUE → AUDIT-003 (P1)** | Confirmé : aucune occurrence de `PageRoleGuard` dans le fichier, contre `InvoiceListPage.tsx:313` qui en a un. |
| O-7 | `/payments/alert-rules` page orpheline | **ÉCARTÉE — prémisse fausse** | La page **est** atteignable : `PaymentsPage.tsx:286` contient un `<Link to="/payments/alert-rules">`. Elle n'a pas d'entrée dans la Sidebar globale, ce qui est un choix de navigation contextuelle cohérent (sous-page de Paiements), pas un orphelin. Par ailleurs l'audit frontend confirme **zéro page orpheline** : les 56 pages sont toutes routées dans `AppRoutes.tsx`. |
| O-8 | Pas de contrôle de propriété du `purchaseOrderId` au portail | **PROMUE → AUDIT-002 (P1)** | Confirmé sur deux niveaux : `toInvoice` (`SupplierPortalController.java:260-281`) recopie l'UUID sans validation, et `performMatchingCheck` (`InvoiceStateMachineServiceImpl.java:180`) charge le PO sans comparer son `supplier_id` (pourtant `nullable = false`, `PurchaseOrder.java:52-53`). |
| O-9 | `APPROVER_ROLES` non résolue | **ÉCARTÉE — conforme** | Lue : `DelegationController.java:64-70` = `ROLE_DAF` + les 11 validateurs N1/N2. **ADMIN en est absent**, conformément à la SoD. Aucun défaut. |
| O-10 | `DepartmentTransitionGuard` : critère « deux niveaux » | **ÉCARTÉE — conforme** | Lu intégralement : le routage N1→N2 repose sur `Department.isRequiresN2()`, **drapeau en base**, jamais une liste en dur (`requiresN2` et `isSingleLevel` lisent le `Department` depuis l'extended state). Conforme au cahier des charges et à la règle « seuils/critères en base, pas en dur ». |

### Ce qui a été vérifié et s'est révélé CONFORME (aucun finding)

Consigné pour éviter toute ré-instruction en P2→P4 :

- **Couverture `@PreAuthorize`** : les 40 contrôleurs (219 endpoints) sont couverts, soit méthode par
  méthode, soit au niveau classe (`SupplierPortalController:53`, `UserController:36`, `RoleController:25`).
  **Zéro endpoint non protégé.**
- **Cloisonnement du journal d'audit** : `AuditController` n'est **pas** une violation SoD malgré le
  `hasRole('ADMIN') or hasRole('DAF')` de `/`, `/export` et `/summary/export` : les trois passent par
  `allowedActionsForCurrentUser(authentication)` (`:132`, `:164`), qui restreint chaque rôle à son
  propre sous-ensemble d'actions (ADMIN=système, DAF=financier). C'est le correctif du finding
  antérieur N3, explicitement commenté aux lignes 111-114 et 161-162.
- **Injection SQL/JPQL** : aucune concaténation d'entrée utilisateur. Aucun `EntityManager`,
  `createQuery` ni `createNativeQuery` dans tout `src/main/java`. Les 2 requêtes natives
  (`InvoiceRepository:132`, `SupplierRepository:42`) sont intégralement paramétrées.
- **Journalisation de données sensibles** : zéro. Les 3 occurrences contenant un mot-clé sensible
  journalisent un libellé, jamais une valeur (`MfaService.java:85` journalise un message constant
  sans interpoler l'OTP).
- **Devise XAF** : zéro occurrence de `XOF` dans `frontend/src` et dans `src/main/java`. Les seules
  mentions sont dans `V45__normalize_currency_xof_to_xaf.sql`, la migration qui **élimine** XOF.
  Aucune régression.
- **i18n backend** : parité parfaite FR/EN — **310 clés de chaque côté, aucune clé orpheline dans un
  sens ni dans l'autre** (comparaison des jeux de clés, pas seulement des effectifs).
- **Encodage `messages_fr.properties`** : ⚠ **la consigne héritée est périmée.** Le fichier est en
  **UTF-8**, et `MessageSourceConfig.java:19` déclare explicitement `setDefaultEncoding("UTF-8")`.
  L'ensemble est donc cohérent. La règle « ISO-8859-1 / ASCII \uXXXX » ne correspond plus au code.
- **Dette frontend** : zéro `TODO`/`FIXME`/`HACK`, zéro `console.log`, zéro `@ts-ignore` en code de
  production. Zéro page orpheline, zéro composant non importé.
- **Dette backend** : zéro `TODO`/`FIXME`, zéro `System.out.println`, zéro `printStackTrace`.
- **Traces de pile** : non exposées (`application.yaml:69-72` et `:314-317`,
  `include-stacktrace: never`) ; le handler générique renvoie un message neutre.
- **Machine à états** : 14 transitions, aucune transition sortant de `ARCHIVE` (terminal), aucune
  sortie de `REJETE` autre que `RESUBMIT`. Aucune transition impossible ni orpheline détectée.

### Vérifié CONFORME en P2 (runtime, 2026-07-22)

Constats établis **par exécution** pendant l'audit visuel. À ne pas ré-instruire en P3/P4.

- **SoD frontend — l'ADMIN est bien bloqué sur les pages financières.** Balayage des 47 routes avec
  le compte `admin` : **18 pages** affichent l'écran `PageRoleGuard` « Accès non autorisé — Vous
  n'avez pas les droits nécessaires » (`/invoices`, `/invoices/new`, `/approvals`, `/financial-audit`,
  `/purchase-orders`, `/goods-receipts`, `/payments`, `/payments/alert-rules`, `/reports`,
  `/reports/builder`, `/archive`, `/matching`, `/matching/:id`, `/admin/suppliers` et ses 3
  sous-pages). Seule exception : `/invoices/:id` → voir AUDIT-003.
- **`SupplierRoute` fonctionne.** Un compte staff (`admin`, `daf`) qui saisit `/supplier/dashboard`
  ou `/supplier/invoices` est **redirigé vers `/dashboard`** — pas d'écran d'erreur, pas de fuite.
- **Devise XAF respectée.** Zéro occurrence de `XOF` dans le DOM rendu sur l'ensemble des pages
  balayées (sonde `hasXOF` sur chaque page × thème). Aucune régression de la règle projet.
- **Aucune clé i18n brute affichée.** La sonde (motif `a.b.c`) ne relève aucune clé non traduite
  dans le rendu, sur toutes les pages du balayage.
- **Parité clair / sombre.** À page et viewport identiques, aucun écart de contenu ni de nombre de
  violations axe-core entre les deux thèmes (seule variation : `/admin/audit`, ±66 caractères, dû à
  des données horodatées). Le mode sombre n'introduit **aucune** régression visuelle mesurable.
- **Focus clavier visible.** Sur les 12 premiers éléments focusables de `/invoices`, tous portent un
  indicateur (`outline: auto 3px`). Aucun élément à focus invisible détecté.
- **Page 404 fonctionnelle.** `/nonexistent-route-404` rend bien `NotFoundPage` (« 404 / Page
  introuvable / Retour à l'accueil »). ⚠ Le harnais la signalait à tort comme « vide » : la sonde
  cherche un `<main>`, absent hors layout — **faux positif écarté**, aucun finding.
- **Images accessibles.** Aucune `<img>` sans attribut `alt` sur les pages balayées.
- **Pages publiques responsives.** `/login` s'affiche correctement à 390 px (contre-preuve utile
  pour AUDIT-019, qui ne concerne que les deux layouts authentifiés).

### Vérifié CONFORME en P4 (transverse, 2026-07-23)

Constats établis **par mesure** pendant l'audit transverse. À ne pas ré-instruire en P5/P6 :
ces points sont sains et ne doivent pas consommer de budget de correction.

- **Parité i18n frontend : parfaite.** `fr.json` et `en.json` comptent **1209 clés chacun**, avec
  **zéro clé manquante dans un sens comme dans l'autre** (comparaison des jeux de clés à plat, pas
  des effectifs). Les 49 valeurs identiques entre FR et EN ont été inspectées une à une : ce sont
  toutes des chaînes légitimement identiques dans les deux langues (« Action », « Date »,
  « Documents », « Total »…). **Aucun oubli de traduction** (filtrage par marqueurs francophones :
  0 résultat).
- **Chaînes en dur : quasi inexistantes.** Balayage de tout `frontend/src` (texte JSX + attributs
  `placeholder`/`title`/`aria-label`) : **2 occurrences seulement**, dont une légitime
  (« Français »/« English » = noms de langue, `AdminUserFormPage.tsx:173`). La seule vraie est
  `ErrorBoundary.tsx` → AUDIT-044.
- **Performance frontend : saine.** gzip actif (entrée 570 Ko → **180 Ko** transférés),
  `Cache-Control: public, immutable, max-age=31536000`, **116 chunks** (lazy loading opérant sur
  toutes les routes). Core Web Vitals sur `/reports` : **FCP 112 ms, LCP 492 ms**, ~1 Mo brut /
  ~300 Ko gzip au premier chargement. Seule réserve : le chunk `InvoiceDetailPage` (AUDIT-045).
- **Latences backend : toutes sous 260 ms.** 12 endpoints mesurés au volume actuel (`/invoices`,
  `/reports/*`, `/payments`, `/purchase-orders`, `/matching`, `/departments`, `/notifications`) :
  de 17 ms à 252 ms. ⚠ Un pic apparent de **1,46 s** sur `/payments/export` a été instruit puis
  **écarté** : les exécutions suivantes tombent à **63 ms** — c'était le coût unique d'initialisation
  d'Apache POI, pas un défaut.
- **N+1 : un seul cas réel, pas de défaut systémique.** Les entités clés sont correctement
  configurées (`@EntityGraph(department, supplier)` sur `InvoiceRepository:43,61`, fetch-join des
  `items` sur `PurchaseOrderRepository`, `userRoles` sur `UserRepository`). Les rapports
  `/bottlenecks`, `/payment-cycle` et `/executive-summary` consomment **1 à 7 scans** malgré leurs
  `findAll()`, car ils ne bouclent pas. Seul `/reports/supplier/{id}/performance` boucle → AUDIT-040.
- **Accessibilité des pages publiques : conforme sur 3 des 4.** Balayage axe-core (WCAG 2.1 AA,
  injection locale depuis `node_modules/axe-core`) : `/forgot-password`, `/reset-password` et
  `/verify-email` → **0 violation**. Seule `/register/supplier` en porte, et elles relèvent de
  familles **déjà ouvertes** (AUDIT-020 contraste, AUDIT-024 labels), désormais complétées.
  **Réponse à la question de cadrage de la P4 : oui, AUDIT-020/021/023/024 couvrent bien la
  totalité des défauts a11y mesurables du système** — P4 n'en a révélé aucune nouvelle famille.
- **Gestion d'erreurs backend : correcte.** Les erreurs métier sondées renvoient un statut juste et
  un message **traduit** (`PATCH /access-requests/{id}` → 400 « Échec de la validation. » avec le
  détail du champ). Le défaut d'AUDIT-014 est **entièrement côté frontend** : le serveur fournit
  bien l'information, c'est le client qui ne l'affiche pas.


### Vérifié CONFORME en P3 (runtime fonctionnel, 2026-07-22)

Constats établis **en jouant les parcours**. À ne pas ré-instruire en P4.

- **La machine à états fonctionne intégralement.** Le workflow complet a été joué par l'API sur un
  département à **deux niveaux** (INFO) : `BROUILLON → SOUMIS → EN_CONTROLE_AA → EN_VALIDATION_N1 →
  EN_VALIDATION_N2 → VALIDE → BON_A_PAYER → PAYE → ARCHIVE`, **8 transitions** tracées dans
  `invoice_status_history`. Idem sur des départements à **un seul niveau** (DRH, TECH, INFRA) :
  le routage N1→N2 selon `Department.requiresN2` est correct dans les deux cas.
- **Les gardes de la machine à états tiennent toutes.** Vérifié par l'échec attendu :
  soumission sans document → « *La facture doit contenir au moins un document* » ;
  `assign-aa` sur une facture non `SOUMIS` → refus ; `validate-n1` hors état N1 → refus ;
  `bon-a-payer` avant `VALIDE` → « *statut requis : VALIDE* ». Aucune transition illégale n'a pu
  être forcée.
- **La séparation des devoirs est respectée sur les 15 rôles.** Balayage des **9 rôles validateurs
  jamais sondés en P2** (`drh`, `dg`, `dex`, `com`, `qhsse`, `infra`, `dir_infra`, `atelier`,
  `dir_tech`) × **14 endpoints** = 126 mesures : les **10 surfaces interdites** (rapports, paiements,
  audit financier, audit système, utilisateurs, PO, fournisseurs, matching, archive, rétention)
  renvoient **403 pour les 9 rôles, sans exception**. Seul écart relevé : `/pending-validation`
  (500 technique → AUDIT-037), qui n'est pas un défaut d'autorisation.
- **Le principe « un approbateur ne valide pas sa propre facture » est appliqué.** `ensureNotSubmitter`
  bloque effectivement : le compte `aa` ne peut pas faire le contrôle AA d'une facture qu'il a
  soumise (« *Un approbateur ne peut pas approuver sa propre facture soumise.* ») — d'où l'existence
  du second compte `aa2` (migration V47). Le cloisonnement N1/N2 (`ensureNotN1Approver`) tient aussi.
- **Le rejet est correctement contraint.** Motif obligatoire (code d'énumération validé côté
  serveur : une valeur inconnue est refusée avec la liste des codes admis), règle « **AUTRE** exige
  au moins 10 caractères de détail » appliquée, 6 codes de rejet exposés et traduits.
- **La resoumission exige une modification réelle.** `RESUBMIT` sans avoir modifié la facture →
  refus (`resubmissionVersionGuard`, V48) ; après un `PUT` modifiant le montant → resoumission
  acceptée, retour en `SOUMIS`. La garde de version fait exactement ce qu'elle promet.
- **Contrôle des fichiers déposés.** Taille : un PDF de **12 Mo** est refusé en **413** au portail
  **et** en interne. Type : un exécutable Windows renommé `facture.pdf` et déclaré
  `application/pdf` est refusé en **400** (`Unsupported MIME type: application/x-msdownload`) —
  Apache Tika inspecte bien le contenu réel, pas l'extension.
- **Le three-way matching donne le bon verdict.** Facture alignée sur `PO-MATCH-001` (GRN couvrant
  100 % des quantités) → `MATCHED`, soumission acceptée, résultat persisté. Facture réclamant les
  quantités **commandées** alors que le GRN n'a réceptionné que 54/60, 22/25 et 7/8 → `MISMATCH`
  bloquant, au-delà de la tolérance de 2 % lue en base (`matching_config`). Le calcul est juste dans
  les deux sens ; les seuils viennent bien de la base et non du code.
- **Les messages d'erreur de l'API sont traduits et respectent `Accept-Language`.** Aucune clé i18n
  brute dans les réponses d'erreur, en FR comme en EN (voir AUDIT-015, hypothèse P1 réfutée).
- **Le cloisonnement du portail fournisseur tient.** `GET /supplier/invoices` ne renvoie que les
  factures portant le `supplier_id` du compte connecté — vérifié en base sur 5 factures, y compris
  celles saisies par l'AA au nom de ce fournisseur (qui doivent lui être visibles, et le sont).
- **La limitation du débit de connexion fonctionne.** `RateLimitingFilter` renvoie bien **429**
  au-delà de 5 tentatives par minute et par IP — constaté deux fois pendant les campagnes de test.
- **Le lien « Êtes-vous un fournisseur ? S'inscrire » n'est pas un défaut.** Il pointe vers
  `/register`, mais `RegisterPage.tsx` **réutilise `SupplierRegisterPage`** en l'encadrant d'un
  bandeau « ce formulaire est réservé aux fournisseurs externes ». Le faux positif écarté en P2
  est confirmé écarté, code à l'appui.
- **La devise XAF reste la norme dans le rendu.** Toutes les listes de factures affichent `XAF`
  (les valeurs `USD`/`XOF` d'AUDIT-032 proviennent de mes propres injections de test par l'API,
  pas du jeu de données initial).
