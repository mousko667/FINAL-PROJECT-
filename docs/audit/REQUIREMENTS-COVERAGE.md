# Phase 6 — Requirements Coverage Audit (Réf. cahier des charges 25627)

> Per `docs/REQUIREMENTS-MATRIX.md`. Each sub-item: ✅ COMPLET / ⚠️ PARTIEL / ❌ ABSENT, with proof (file:line backend, frontend component, or runtime evidence). All ⚠️/❌ become `REQ-xxx` issues in `ISSUES.md` (Phase 8).

---

## Règle métier transversale — MFA obligatoire

**Verdict: ✅ COMPLET**

- `MfaSetupEnforcementFilter` exists in the security chain (`SecurityConfig.java`, confirmed P1-03) — enforces MFA setup for roles that require it.
- CLAUDE.md §9 (Phase 9 constraints) states MFA mandatory for `ROLE_DAF`, `ROLE_ADMIN`, `ROLE_VALIDATEUR_N1_*`, `ROLE_VALIDATEUR_N2_*` — i.e., all 5 non-supplier roles (accounting assistant is `ROLE_ASSISTANT_COMPTABLE`; need to confirm this role is also covered, see below).
- `AuthController.java:84-106`: `/mfa/setup`, `/mfa/confirm`, `/mfa/validate` all present; login flow (`/mfa/validate` returning full `LoginResponse`) matches the documented "first call returns `mfa_required`+`pre_auth_token`, second submits OTP" flow (per CLAUDE.md §9).

**Sub-item to verify in Phase 10 (not blocking)**: confirm `MfaSetupEnforcementFilter`'s role list literally includes `ROLE_ASSISTANT_COMPTABLE` (the matrix explicitly lists "accounting assistant" as one of the 5 MFA-mandatory roles, but CLAUDE.md §9's list — `DAF`, `ADMIN`, `VALIDATEUR_N1_*`, `VALIDATEUR_N2_*` — does not obviously include it by name unless `ASSISTANT_COMPTABLE` is itself a `VALIDATEUR_N1_*`-pattern role, which seems unlikely from naming). **Flagging as REQ-MFA-01 (low-confidence, needs file read)** — see Phase 10 follow-up list at the end of this document.

---

## Module 1 — Registration & Authentication

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Formulaire d'inscription avec sélection de rôle | ✅ COMPLET (by design, two paths) | Public self-registration is **supplier-only** (`SupplierRegisterPage.tsx`, routed via `/register` → `RegisterPage.tsx` which explicitly wraps it with a banner: *"This form is for external suppliers only. OCT staff accounts are created by the System Administrator"*). Staff roles (accounting assistant, L1/L2 approver, CFO, admin) are created via `UserController.createUser` (`POST /api/v1/users`, admin-only, `AdminUserFormPage.tsx`). This split is a **deliberate security design** (no public self-registration for financial-approval roles) — the matrix's "one form with role dropdown" wording describes a generic spec; the two-path implementation is the architecturally correct interpretation for a B2B terminal system. |
| 2 | Champs spécifiques fournisseur (company name, tax ID, contact, bank details) | ✅ COMPLET | `SupplierRegisterPage.tsx:12-22` (zod schema): `companyName`, `taxId`, `email`, `contactPhone`, `bankDetails`, `address` all present and submitted to `/auth/register/supplier`. |
| 3 | Champs spécifiques staff (employee ID, department, approval limits) | ✅ COMPLET | `UserCreateRequest.java`/`UserUpdateRequest.java`/`UserDTO.java`: `employeeId` (String), `departmentId` (UUID), `approvalLimit` (BigDecimal) all present. |
| 4 | Indicateur de force de mot de passe | ✅ COMPLET | `SupplierRegisterPage.tsx:42-59`: 4-bar strength meter (length≥8, uppercase, digit, special-char), with `t('auth.passwordStrength.{weak,fair,good,strong,hint}')` labels (these 5 keys are part of P4-01's 94 missing-key list — meter logic works, label TEXT has the i18n gap already tracked). |
| 5 | Vérification email | ✅ COMPLET | `AuthController.java:60-66` (`GET /auth/verify-email?token=...`), `EmailVerificationPage.tsx` (loading/success/error states, links back to login or re-registration). |
| 6 | Récupération / reset de mot de passe | ⚠️ PARTIEL — functionally complete, but **zero i18n** | `AuthController.java:68-82` (`/forgot-password`, `/reset-password`) ✅. `ForgotPasswordPage.tsx` and `ResetPasswordPage.tsx` are fully functional but **contain ZERO `t()` calls** — every string ("Reset password", "Enter your account email...", "Choose a new password", "Use at least 8 characters", "Password reset successful...", "Invalid or expired reset link.", etc.) is hardcoded English, shown to French-locale users too. This is a **new i18n gap distinct from P4-01** (P4-01 only covered missing keys for EXISTING `t()` calls — these two pages never call `t()` at all). → **REQ-01** |
| 7 | Setup MFA | ✅ COMPLET | See "Règle métier transversale" above. `ProfilePage.tsx` MFA section (`/mfa/setup`, `/mfa/confirm` wired) — already covered by P4-01 (16 `mfa.*` i18n keys missing) and P4-04 (QR `alt`), no NEW finding here. |
| 8 | Gestion de session + timeout | ⚠️ PARTIEL — backend real, frontend admin UI is a non-functional mock | Backend: JWT access/refresh token expiry configured in `application.yaml`, `ActiveSession` entity with `expiresAt`/`revoked` (real, T6-verified). Frontend: `SecuritySettingsPage.tsx` "Session Timeout" control (lines 71-88, `useState(60)`) is **local-only state**, never sent to any backend endpoint — `handleSave` (line 35-39) only sets a local `saved` flag with the literal disclosure text *"settings saved (simulation only in this version)"* (line 51). The Active Sessions table (lines 135-180) IS real (`GET/DELETE /api/v1/admin/sessions`). → **REQ-02** (shared with Module 13/14, see consolidated note below) |
| 9 | Redirection dashboard selon rôle | ✅ COMPLET | `LoginPage.tsx:59-63`: `ROLE_SUPPLIER` → `/supplier/dashboard`, all others → `/dashboard` (which itself renders role-specific views, see Module 2). |
| 10 | Écran gestion de profil | ✅ COMPLET | `ProfilePage.tsx` + `UserProfileController.java` (`GET/PUT /api/v1/profile`), email-change correctly blocked (PROB-008 compliance, `UserProfileController.java:50-52`). |
| 11 | Tracking des tentatives de connexion (lié `isAccountNonLocked()`) | ⚠️ PARTIEL — backend complete, frontend doesn't surface the distinct state | Backend: `User.isAccountNonLocked()` (`User.java:167`), `AuthService.ensureAccountNotLocked()` (`AuthService.java:347-358`) throwing `AccountLockedException` → `GlobalExceptionHandler.java:91-95` correctly returns **HTTP 423 LOCKED** with body `account.locked`. `UserController.unlockUser` (admin-only) ✅. **Frontend gap**: `LoginPage.tsx:112-117` — `loginMutation.isError` always renders the SAME generic `t('auth.loginError')` ("Identifiants incorrects"/"Invalid credentials") regardless of whether the backend returned 401 (bad credentials) or 423 (account locked). A locked-out user is told "wrong password" instead of "account locked — contact admin", which actively misleads them into repeatedly retrying (or trying to reset a password that isn't the problem). → **REQ-03** |
| 12 | Auto-inscription supplier vs création staff par administrator | ✅ COMPLET | Same as item 1 — this is literally the design already verified. |

### Module 1 coverage: **10/12 ✅ COMPLET, 2/12 ⚠️ PARTIEL** (≈ 92% functional, with 3 new findings: REQ-01, REQ-02, REQ-03)

---

## Phase 6 — Findings Recorded So Far (cumulative, updated as modules complete)

| ID | Severity | Module | Summary |
|---|---|---|---|
| REQ-01 | MOYENNE | M1 | `ForgotPasswordPage.tsx`/`ResetPasswordPage.tsx` have zero `t()` calls — 100% hardcoded English, violates CLAUDE.md §1 bilingual rule |
| REQ-02 | MOYENNE | M1/M13/M14 | `SecuritySettingsPage.tsx` security-policy form (MFA toggle, session timeout, max attempts, min password length) is explicitly simulation-only — no backend persistence/enforcement wiring. Active Sessions sub-section IS real. |
| REQ-03 | MOYENNE | M1 | `LoginPage.tsx` shows generic "invalid credentials" for ALL auth errors incl. HTTP 423 (account locked) — backend correctly differentiates, frontend doesn't |

*(Module 6's "MFA mandatory role list" sub-item flagged for low-confidence follow-up — see end of document)*

---

## Module 2 — Dashboard

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Vues par rôle (supplier/finance staff/manager) | ✅ COMPLET | `DashboardPage.tsx` has 4 distinct role-gated render branches: ADMIN (`:114-189`), SUPPLIER (`:192-240`), VALIDATOR/manager (`:243-300`), AA/DAF/finance (`:313-457`). |
| 2 | Supplier: factures soumises, statut paiement, actions en attente | ✅ COMPLET | `:194-200` — 5 KPI tiles (`submittedCount`, `pendingCount`, `approvedCount`, `paidCount`, `rejectedCount`) from `/supplier/dashboard`; quick actions to submit/view invoices (`:216-237`). |
| 3 | Finance: approbations en attente, file de traitement, analyse d'âge (aging) | ⚠️ PARTIEL | Pending approvals + processing queue ✅ (`:369-401`). "Aging" analysis is NOT on this dashboard — but Module 7 (Payment Tracking) is the natural home for aging; checked there, not double-counted as a Module 2 gap. |
| 4 | Manager: demandes d'approbation, métriques, alertes budget | ⚠️ PARTIEL | Validator queue + pending count ✅ (`:252-265`). BUT `:258` ("Traitées ce mois") and `:262` ("Approuvées") render the **literal placeholder `—`** — never wired to a real query, unlike the AA/DAF KPI cards which DO call `reportService.getKpis`. "Alertes budget" — no budget-alert UI exists anywhere in this component. → **REQ-04** |
| 5 | Summary cards (reçues, en validation, approuvées, payées) | ✅ COMPLET | `kpi.countByStatus` pie chart (`:404-424`) covers all 9 BAP statuses incl. these. |
| 6 | Feed d'activité récente | ❌ ABSENT | No "recent activity" / activity-feed component exists in `DashboardPage.tsx` or anywhere searched in `components/`. → folded into **REQ-04** as a second sub-item (same root cause: dashboard manager/activity views are partially stubbed) |
| 7 | Quick actions | ✅ COMPLET | `QuickLink` components used throughout all 4 role branches. |
| 8 | Centre de notifications avec compteur non-lus | ✅ COMPLET | `NotificationDropdown.tsx` (header component) + `NotificationController.java:46-51` (`GET /notifications/unread-count` → `notificationService.countUnread`). |
| 9 | KPIs de temps de traitement | ✅ COMPLET | `dashboard.avgProcessingTime` KPI card (`:344-349`), backed by `kpi.averageProcessingTimeDays`. |
| 10 | Responsive mobile | ✅ COMPLET (static analysis) | Tailwind responsive classes (`grid-cols-2 md:grid-cols-4`, `sm:grid-cols-3`, etc.) systematically applied across all branches. Not runtime-tested in a real viewport (out of audit scope), but the responsive utility classes are present and consistently used. |

### Module 2 coverage: **7/10 ✅ COMPLET, 2/10 ⚠️ PARTIEL, 1/10 ❌ ABSENT** (≈ 80%)

---

## Module 3 — Invoice Reception

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Interface de soumission fournisseur | ✅ COMPLET | `SupplierInvoiceSubmitPage.tsx`, `POST /api/v1/invoices` (`InvoiceController.java:137`). |
| 2 | Upload PDF / XML / image | ⚠️ PARTIEL — PDF/image confirmed, XML not found | `SupplierInvoiceSubmitPage.tsx` accepts file upload for OCR (`/ocr/extract`); OCR result distinguishes "Digital PDF" vs "Scanned document" (`:137-139`), implying PDF + image (scanned) support. **No XML-specific handling found** in OCR or invoice document upload — Apache Tika MIME validation (CLAUDE.md §3) likely accepts a broad set, but XML-as-invoice-format is not specifically evidenced. Low priority — flagging as a documentation/scope question rather than a numbered REQ (XML invoice upload is unusual for OCR-based extraction; PDF/image cover the realistic OCT use case). |
| 3 | Preview extraction OCR (réellement implémenté ou mocké?) | ✅ COMPLET — genuinely real, not mocked | `OcrController.java:28` (`POST /ocr/extract`, multipart). `SupplierInvoiceSubmitPage.tsx:55-148`: distinguishes digital-PDF (text-layer extraction, "high accuracy") vs scanned (Tess4J OCR, "please verify all fields") — confirmed Tess4J 5.11.0 + PDFBox 3.0.3 in `pom.xml` (P1-02 GAP1). Pre-fills form fields, editable confirm step before submission. **This is a real, working feature**, not a mock. |
| 4 | Champs facture (numéro, date, montant, fournisseur, description) | ✅ COMPLET | `SupplierInvoiceSubmitPage.tsx:172+` — invoice number (OCR-extractable), and standard invoice fields per `InvoiceCreateRequest` DTO (date, amount, supplier, description). |
| 5 | Liaison PO / numéro de référence | ✅ COMPLET | `purchaseOrderId` field on `Invoice`, drives Three-Way Matching (P1-04, verified). |
| 6 | Pièces jointes | ✅ COMPLET | `InvoiceDocumentController.java` — separate document-attachment endpoints exist (flagged in P1-05 for a minor layering issue, but functionally present). |
| 7 | Détection de doublons (alerte) | ✅ COMPLET — exceeds documentation | `InvoiceStateMachineServiceImpl.java:85-87,134-156` — `performDuplicateCheck()`, 365-day window, same supplier+description, excludes rejected/archived, throws `WorkflowException` blocking submission. Already credited in P1-04 as an undocumented-but-correct addition. |
| 8 | Confirmation avec numéro de référence | ✅ COMPLET | `Invoice.referenceNumber` generated and displayed (used as the primary identifier throughout `DashboardPage.tsx`'s queue views, e.g. `:281,384`). |
| 9 | Suivi de statut côté fournisseur | ✅ COMPLET | `SupplierInvoicesPage.tsx` + `/supplier/invoices` (P2-01-verified end-to-end this session). |
| 10 | Historique des soumissions | ✅ COMPLET | `SupplierInvoicesPage.tsx` IS the submission-history list view (paginated, status-filterable per the supplier portal pattern verified in P2-01/P3-02). |
| 11 | Upload en masse (bulk) | ❌ ABSENT | No bulk/multi-file upload UI found (`grep -rln "bulk\|batch.*upload" frontend/src/pages` → only `PurchaseOrdersPage.tsx`, unrelated to invoice bulk-submit). `InvoiceController.java` has 10 endpoints, none accept multiple invoices in one call. → **REQ-05** |
| 12 | API d'intégration pour soumission automatisée | ❌ ABSENT | No API-key/external-integration mechanism found (`grep -rln "ApiKey\|api_key" src/main/java` → zero matches). This overlaps with Module 12 (Integration), which is explicitly flagged in the matrix as "often out-of-scope for a bachelor" — **not raising a separate REQ here**, deferred to Module 12's verdict. |

### Module 3 coverage: **9/12 ✅ COMPLET, 1/12 ⚠️ PARTIEL (low-priority/scope question), 2/12 ❌ ABSENT** (≈ 75-83%, both ❌ items are arguably out-of-scope for a bachelor project's realistic use case — see REQ-05 severity note)

---

## Phase 6 — Findings Recorded So Far (cumulative, updated as modules complete)

| ID | Severity | Module | Summary |
|---|---|---|---|
| REQ-01 | MOYENNE | M1 | `ForgotPasswordPage.tsx`/`ResetPasswordPage.tsx` have zero `t()` calls — 100% hardcoded English, violates CLAUDE.md §1 bilingual rule |
| REQ-02 | MOYENNE | M1/M13/M14 | `SecuritySettingsPage.tsx` security-policy form (MFA toggle, session timeout, max attempts, min password length) is explicitly simulation-only — no backend persistence/enforcement wiring. Active Sessions sub-section IS real. |
| REQ-03 | MOYENNE | M1 | `LoginPage.tsx` shows generic "invalid credentials" for ALL auth errors incl. HTTP 423 (account locked) — backend correctly differentiates, frontend doesn't |
| REQ-04 | BASSE | M2 | Validator/manager dashboard: "Traitées ce mois"/"Approuvées" are hardcoded `—` placeholders (never queried); no recent-activity feed exists anywhere on the dashboard |
| REQ-05 | BASSE | M3 | No bulk/multi-invoice upload UI or endpoint exists — single-invoice submission only |
| REQ-06 | **HAUTE** | M4 | `POST /api/v1/workflow/reject` exists end-to-end on the backend, but **no frontend code anywhere calls it** — no reject button, form, or reason-selection UI exists on `ApprovalQueuePage.tsx`, `InvoiceDetailPage.tsx`, or any other page. A validator cannot reject an invoice through the UI. |
| REQ-07 | MOYENNE | M4 | Three Module 4 items are entirely absent (backend AND frontend, zero DB tables): workflow-configuration UI, multi-level routing-rule engine, validation-checklist templates. Auto-validation rules (PO matching, amount thresholds, supplier verification) ARE implemented, but as fixed logic spread across the codebase, not a unified configurable rule set. |
| REQ-08 | **HAUTE** | M5 | `MatchingConfigController` (tolerance %, tolerance amount, require-GRN) is a fully working, admin-gated backend endpoint with **zero frontend usage** — second instance of the P4-02 backend-complete/frontend-absent pattern. Defeats CLAUDE.md §9's intent ("tolerance thresholds stored in DB, not hardcoded") since only direct DB/API access can ever change them. |
| REQ-09 | BASSE | M5 | No "matching history" view (multiple matching attempts per invoice, e.g. MISMATCH→OVERRIDDEN sequence) or matching-report export exists — only the single latest `ThreeWayMatchingResult` per invoice is ever surfaced. |
| REQ-10 | **CRITIQUE** | M7 | `PaymentsPage.tsx:34-39` hardcodes `PAYMENT_METHODS` values (`BANK_TRANSFER`/`CHECK`/`MOBILE_MONEY`/`CASH`) that **do not exist** in the backend `PaymentMethod` enum (`VIREMENT`/`CHEQUE`/`ESPECES`). Every "Enregistrer le paiement" submission sends an invalid enum string → Jackson deserialization fails → HTTP 400. **The core payment-recording feature is completely broken** for all 4 offered options. |
| REQ-11 | MOYENNE | M7 | Remittance advice ("Avis de versement") is fully implemented end-to-end on the backend (`RemittanceAdviceServiceImpl.generateRemittanceAdvice`, `GET /payments/{id}/remittance` pre-signed URL) but the frontend "Avis" button (`PaymentsPage.tsx:287-292`) has **no `onClick` handler** — third instance of the backend-complete/frontend-absent pattern (after P4-02, REQ-08). |
| REQ-12 | BASSE | M7 | No batch/bulk payment processing, no payment-specific export, and no payment-alert configuration exist — `PaymentController` only supports recording ONE payment per invoice per call, with no `/export` or `/alerts` endpoints. Aging analysis and cash-flow projection ARE implemented but live in the Reports module (Module 11), not surfaced from the Payments page itself. |
| REQ-13 | **HAUTE** | M8 | `GET /api/v1/supplier/profile` (`SupplierPortalController.java:147-153`) returns the raw `Supplier` JPA entity (`ApiResponse<Supplier>`), violating CLAUDE.md §3 ("never expose JPA entities directly — always DTOs"). This entity includes `bankDetails` — the `@Convert(EncryptionAttributeConverter.class)` field — meaning the **decrypted plaintext bank details** are serialized into the supplier's own profile response. The integration test (`SupplierPortalIntegrationTest`, `@Transactional`) passes because it doesn't assert the field's absence, AND its `@Transactional` annotation masks the separate `LazyInitializationException` risk on `onboardedBy`/`documents` that `open-in-view: false` would otherwise cause in production. |
| REQ-14 | MOYENNE | M9 | `ArchivePage.tsx:175` displays a static, hardcoded claim — *"SHA-256 vérifié à chaque téléchargement"* (SHA-256 verified on every download) — that is **not true**: `InvoiceDocumentService.computeSha256()` runs ONLY at upload time (stores `checksum_sha256`); `InvoiceDocumentController.download()` generates a pre-signed MinIO URL with no server-side re-fetch or checksum comparison. The UI makes a false integrity guarantee to users. Same paragraph also claims a "10-year retention policy" with no corresponding `RetentionPolicy`/config entity anywhere in the codebase (`grep -rln "retention\|Retention" --include="*.java"` → zero matches outside this UI string's neighbors). |
| REQ-15 | BASSE | M9 | `ArchivePage.tsx:150-152` "Download PDF" button has **no `onClick` handler** — fourth instance of the backend-complete/frontend-absent (or in this case, frontend-rendered-but-dead) pattern. `InvoiceDocumentController.download()` exists and is reachable from `InvoiceDetailPage.tsx` (the invoice's own document list), so the underlying capability works — only the Archive page's shortcut button is non-functional. |
| REQ-16 | BASSE | M9 | No document-viewer with zoom/rotation, no invoice versioning, and no document-access audit logging exist. `grep -rln "DocumentAccessLog\|InvoiceVersion" --include="*.java"` → zero matches; `InvoiceDocumentService`/`InvoiceDocumentController` never call `AuditService` — downloading a financial document leaves no audit trail. |
| REQ-17 | **CRITIQUE** | M10 | `AuditController`'s `/audit-logs/system` (ADMIN) and `/audit-logs/financial` (DAF) endpoints filter `WHERE action IN (SYSTEM_ACTIONS \| FINANCIAL_ACTIONS)` — e.g. `"LOGIN"`, `"APPROVE"`, `"INVOICE_SUBMIT"`, `"PAYMENT"`. But the ONLY component that writes `AuditLog` rows for normal HTTP traffic, `AuditLoggingFilter.classifyAction()`, NEVER produces any of those values — it writes `action = "POST /api/v1/invoices -> 201"`-style free-text strings, with `entityType ∈ {"FINANCIAL_ACTION","SYSTEM_ACTION","HTTP_REQUEST"}` (a DIFFERENT field than what the controller filters on). **Both dedicated audit endpoints structurally return zero rows for all real traffic** — confirmed by `FinancialAuditPage.tsx:48-49`'s own code comment, which documents this exact symptom (but misdiagnoses the field) and works around it with an undocumented `entityType=HTTP_REQUEST` query that bypasses the financial-action filter entirely, showing ALL HTTP traffic (not just financial events) to the DAF. The single OTHER direct caller, `UserService.java:185` (`action="PROFILE_UPDATE"`), is ALSO not in either allowed-action list. **No `AuditLog` row in the database can ever have `action="LOGIN"`, `"LOGOUT"`, `"APPROVE"`, `"REJECT"`, `"PAYMENT"`, `"ROLE_CHANGE"`, etc.** — the entire "sub-typing of audit events" design (`AuditController`'s `SYSTEM_ACTIONS`/`FINANCIAL_ACTIONS` lists) is dead code that can never match. |
| REQ-18 | BASSE | M10 | `AuditLoggingFilter.resolveUserId()` (`AuditLoggingFilter.java:73-77`) is hardcoded to always `return null` (by its own comment, "to avoid a DB call in the filter") — every filter-originated `AuditLog` row has `user_id = NULL`, so `performedBy?.username` renders as `—` for the vast majority of audit entries on both `AdminAuditPage.tsx` and `FinancialAuditPage.tsx`. The "who did this" column — arguably the single most important field of an audit trail — is empty for nearly all rows. |
| REQ-19 | MOYENNE | M10 | No anomaly-detection of any kind exists (`grep -rln "anomal" src frontend/src` → zero matches) and no real-time/WebSocket-driven audit dashboard exists (the existing `WebSocketNotificationListener` suite, confirmed in earlier phases, serves user-facing notifications — not an audit/security dashboard). `AdminAuditPage.tsx`/`FinancialAuditPage.tsx` are both plain paginated tables with manual filter inputs and no auto-refresh. |
| REQ-20 | BASSE | M11 | `AdminDepartmentFormPage.tsx` collects a `budget` field (Zod-validated, `:13,82,87`) for every department, but `Department.java` (the JPA entity, `domain/department/model/`) has **no `budget` column**, and no Flyway migration ever adds one (`grep -in budget src/main/resources/db/migration/*.sql` → zero matches). The value is captured by the form and presumably submitted on create/update, then silently dropped before persistence — a frontend-only "ghost field". Separately, `DashboardKpiDTO` (Reports module) has no budget-vs-actual comparison field at all, so even if the column existed, no report would use it. → folded into **REQ-21** |
| REQ-21 | MOYENNE | M11 | Reporting & Analytics (Module 11) has no "custom report builder" (`grep -rln "report.*builder\|ReportBuilder" -i` → zero matches), no scheduled/recurring report configuration (`grep -rln "ScheduledReport\|ReportSchedule" -i` → zero matches), no report-distribution manager (the one "distribution" hit, `DashboardPage.tsx:408`, is a status-distribution CHART, unrelated), no executive-summary generator, and no budget-vs-actual comparison (see REQ-20). These five items (8-9, 11-13 of the matrix's 13) represent the "enterprise reporting platform" tier of features — none exist. The 8 items that DO exist (KPI dashboard, processing-time, supplier performance, aging, payment-cycle/cash-flow, bottlenecks, Excel/PDF export with preview-via-render) form a solid OPERATIONAL reporting layer; what's missing is the CONFIGURATION/AUTOMATION layer on top of it. |
| REQ-22 | MOYENNE | M12 | `WebhookController` (register/list/deactivate webhooks + paginated delivery log, `/api/v1/integrations/webhooks/*`) and `IntegrationStatusController` (`/api/v1/integrations/status` — per-webhook health + last-delivery-result) are a complete, correctly-architected, ADMIN-only backend (HMAC-SHA256 signing, 3-retry exponential backoff per CLAUDE.md §9, append-only delivery log — re-confirms Phase 3's webhook findings from the OTHER side) — but **zero frontend exists** (`grep -rln "webhook" -i frontend/src` → zero matches: no page, no API client method, no nav item, no route). Sixth instance of the backend-complete/frontend-absent pattern (after P4-02, REQ-08, REQ-11, and REQ-15 — and distinct from REQ-15/16, which were "button exists but dead"; here NO UI ELEMENT of any kind exists). Per `docs/SCOPE.md` (created this module, confirmed with project owner), this gap is explicitly NOT part of the broader Module 12 hors-scope exclusion — webhooks are a generic, already-built integration mechanism, not a named-third-party-system connector. |
| REQ-23 | BASSE | M13/M14 | Module 13 items 4 (Classification de sensibilité des données), 7 (Import/export en masse), 8 (Workflow de demande d'accès), and 9 (Éditeur de matrice de permissions), plus Module 14 item 1 (Matrice de permissions par rôle, the SAME gap viewed from the compliance angle) are entirely absent — `grep -rln "sensitivit\|sensibilit\|bulk.*import\|access.*request\|demande.*acc[èe]s\|permission.*matrix\|matrice.*permission" -i src frontend/src` → zero matches for all. These represent an "enterprise IAM platform" tier on top of the role-based access control that DOES exist and work (Module 13 items 1-3, 5-6, 10, 12). No `docs/PRD.md`/`docs/WORKFLOW.md` requirement was found mandating data-classification labels, bulk user CSV import/export, a self-service access-request workflow, or a visual permission-matrix editor — `UserController`'s `PUT /{id}/roles` (`assignRoles`, single-role replacement via `AdminUserFormPage.tsx`'s dropdown) is the system's actual, working permission-management mechanism, just not presented as a "matrix". Module 13 item 11 (Visionneuse d'audit trail utilisateur) is NOT counted here — it is a duplicate of the Module 10 audit-trail brokenness (**REQ-17/18**), not a separate gap. |
| REQ-24 | BASSE | M14 | Eight Module 14 items have NO existing implementation to build on (unlike the recurring backend-complete/frontend-absent pattern elsewhere): item 2 (encryption-status indicators — AES-256 via T1 is real but invisible to admins), item 5 (data-backup status), item 7 (privacy-policy acceptance tracking), item 8 (security-incident reporting), item 9 (SOX/IFRS/local-regulation compliance checklist), item 11 (compliance calendar), and item 12 (security-health dashboard — no widget rolls up MFA adoption, login-failure trends, encryption coverage, or `IntegrationStatusController`/`DashboardKpiDTO.webhookDeliverySuccessRate` into a single "security posture" view). All confirmed absent via `grep -rln` for the relevant French/English terms across `src` and `frontend/src` (zero matches each). Collectively these represent a "compliance/security meta-layer" that would sit ON TOP OF the system's existing, individually-correct controls (encryption, soft-delete, append-only audit/matching/webhook logs, MFA enforcement) — none of which currently surface as compliance POSTURE. Given a Bachelor FYP's scope and the absence of any PRD/WORKFLOW mandate for SOX/IFRS-specific tooling, several of these items are candidates for `docs/SCOPE.md` (Phase 8/9 triage), similar to Module 12's named-third-party-connector exclusions — but unlike Module 12, no AskUserQuestion/explicit-confirmation instruction exists for this module in `REQUIREMENTS-MATRIX.md`, so this audit does not unilaterally classify them as hors-scope and instead logs them here as low-severity findings for Phase 8/9 to triage. |

---

## Module 4 — Validation Workflow

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Interface de configuration du workflow | ❌ ABSENT | `grep -rln "WorkflowConfig\|workflow_config\|RoutingRule\|routing_rule\|ChecklistTemplate\|checklist_template\|AutoValidation\|auto_validation" --include="*.java" --include="*.tsx" --include="*.sql" -i src frontend/src` → **zero matches anywhere** (backend, frontend, or migrations). No DB table, no controller, no page. → **REQ-07** |
| 2 | Règles de routage multi-niveaux | ⚠️ PARTIEL — multi-level routing works, but is hardcoded, not configurable | The N1/N2 routing itself works correctly (state machine: `EN_VALIDATION_N1` → conditionally `EN_VALIDATION_N2` per department, per CLAUDE.md §5/§130-136). But WHICH departments need N2 is **hardcoded** — there is no `RoutingRule` entity or admin-editable rule set (same grep as item 1, zero matches). → folded into **REQ-07** |
| 3 | Templates de checklist de validation | ❌ ABSENT | Same grep as item 1 — zero matches for any checklist concept anywhere in the codebase. → folded into **REQ-07** |
| 4 | Règles de validation auto (PO matching, seuils de montant, vérification fournisseur) | ⚠️ PARTIEL — each check works individually, no unified config screen | PO matching = Three-Way Matching (Module 5, ✅). Amount thresholds = `MatchingConfig.tolerancePercentage`/`toleranceAmount` (admin-editable backend, see REQ-08). Supplier verification = `Supplier.status` lifecycle (`PENDING_VERIFICATION → ACTIVE`, admin-only, CLAUDE.md §9) IS enforced. All three capabilities are real but live in three different parts of the system with no unified "auto-validation rules" screen. Not a separate finding — the underlying capabilities exist; this is a UX/cohesion observation only. |
| 5 | File de validation en attente | ✅ COMPLET | `ApprovalQueuePage.tsx` + `GET /api/v1/workflow/steps` (`ApprovalController.java:33`) — lists pending approval steps assigned to the current validator. |
| 6 | Interface de revue | ✅ COMPLET | `InvoiceDetailPage.tsx` — full invoice detail incl. line items, documents, Three-Way Matching panel, approval-step timeline (lines 190-351). |
| 7 | Approbation / rejet avec commentaires | ⚠️ PARTIEL — approval works end-to-end, rejection has no UI at all | **Approval**: `POST /workflow/validate-n1`, `/validate-n2`, `/bon-a-payer` (`ApprovalController.java:56,72,84`) all exist and are wired from the frontend. **Rejection**: `POST /workflow/reject` (`ApprovalController.java:95`) exists on the backend; `grep -n "reject\|Reject" -i pages/InvoiceDetailPage.tsx pages/ApprovalQueuePage.tsx` → **zero matches in either file**. `InvoiceDetailPage.tsx:348-351` only **displays** an existing `step.rejectionReason` (read-only) — there is no form, button, or mutation call to CREATE one. → **REQ-06** |
| 8 | Sélection du motif de rejet | ❌ ABSENT | Direct consequence of item 7 — with no reject UI at all, there is by definition no reject-reason selector. Same root cause, folded into **REQ-06**. |
| 9 | Workflow de re-soumission | ✅ COMPLET | Per CLAUDE.md §5: `REJETE → SOUMIS` for correction. Transition confirmed in `InvoiceStateMachineServiceImpl` (Phase 1). The re-submission PATH works correctly — only the ACT of producing a `REJETE` invoice (item 7/8) lacks a UI trigger. |
| 10 | Historique d'approbation | ✅ COMPLET | `InvoiceDetailPage.tsx:260-289+` — Approval Steps timeline (approver, status, timestamps per step), backed by `GET /workflow/steps`. |
| 11 | Règles d'escalade pour retards | ✅ COMPLET | `DeadlineReminderJob.java:42-131` — `@Scheduled` job (daily 07:00): 24h-warning reminders for approaching deadlines, plus escalation of overdue steps to the assignee + DAF/Admin (`"🚨 Escalade SLA"`, lines 107-123), dispatched via real email + in-app notifications. |
| 12 | Monitoring SLA | ✅ COMPLET | `BottleneckDTO.java:33` (`isBottleneck`, "exceeds 3-business-day SLA") + `ReportServiceImpl.java:575-609` (`calculateApprovalBottlenecks()`, `SLA_DAYS = 3.0`) — computes average time-in-step per approval level and flags steps exceeding the SLA. Exposed via `ReportController` (cross-check in Module 11). |

### Module 4 coverage: **6/12 ✅ COMPLET, 3/12 ⚠️ PARTIEL, 3/12 ❌ ABSENT** (≈ 50-65%)

**Headline finding**: the rejection path — as core to a "validation workflow" as approval itself, and explicitly diagrammed in CLAUDE.md §5 (`REJETE → back to SOUMIS for correction`) — has a complete, presumably-tested backend endpoint with **zero frontend entry point**. Unlike REQ-08's tolerance-config (an admin convenience layered on a working core), this is a CORE alternative path of the BAP state machine that no human user can currently trigger. Conversely, escalation/SLA monitoring (items 11-12) are fully real and well-built — a positive surprise, since these were unread files at the start of this module.

---

## Module 5 — Three-Way Matching (= Phase 9D)

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Interface de matching à 3 voies | ✅ COMPLET | `InvoiceDetailPage.tsx:190-258` — dedicated Three-Way Matching panel, rendered when `purchaseOrderId` is set. |
| 2 | Affichage données PO | ✅ COMPLET | Same panel — PO data displayed alongside invoice data (verified Phase 1, P1-04). |
| 3 | Informations GRN (goods receipt note) | ✅ COMPLET | `GoodsReceiptController`/GRN pages — GRN entity fully modeled; `requireGrn` is itself a configurable matching parameter (item 7), GRN data feeds the matching panel. |
| 4 | Comparaison ligne à ligne | ✅ COMPLET | `ThreeWayMatchingService.match()` (`ThreeWayMatchingService.java:48+`) compares invoice/PO/GRN line items (quantities/amounts). |
| 5 | Indicateurs de statut (matched / partial / mismatch) | ✅ COMPLET | `MatchingStatus` enum (`MATCHED`/`PARTIAL`/`MISMATCH`/`OVERRIDDEN`) + `MatchingBadge` component, color-coded (verified Phase 1). |
| 6 | Identification et flagging des écarts | ✅ COMPLET | `ThreeWayMatchingResult` includes discrepancy notes, displayed in the matching panel. |
| 7 | Configuration des seuils de tolérance | ❌ ABSENT (frontend only) — backend complete | `MatchingConfigController.java` (62 lines, full file read): `GET /api/v1/matching-config` (`hasAnyRole('ADMIN','ASSISTANT_COMPTABLE')`) returns `tolerancePercentage`/`toleranceAmount`/`requireGrn`; `POST` (admin-only) updates them, persisted in `matching_config` table — confirms CLAUDE.md §9's "tolerance thresholds stored in DB, not hardcoded" is TRUE on the backend. `grep -rln "MatchingConfig\|tolerance" --include="*.tsx"` → **zero matches**. No settings page exposes this. → **REQ-08** |
| 8 | Override manuel avec justification | ✅ COMPLET | `InvoiceDetailPage.tsx:217-254` — override form gated on `MISMATCH` + `canOverride` + invoice status `BROUILLON`/`REJETE`, requires ≥10-char justification. Backend `ThreeWayMatchingService.recordOverride()` restricted to `ROLE_DAF`/`ROLE_ADMIN` (CLAUDE.md §9). Append-only `OVERRIDDEN` record — compliant. |
| 9 | Historique de matching | ⚠️ PARTIEL — append-only in DB, no history view | `ThreeWayMatchingResultRepository.java:13` (`findTopByInvoiceIdOrderByCreatedAtDesc`) confirms multiple result rows CAN exist per invoice (consistent with append-only design), but `getMatchingResult()` (`InvoiceService.java:189`) only ever returns the SINGLE LATEST result. No endpoint/UI shows the full chronological sequence (e.g., "MISMATCH on date X → OVERRIDDEN on date Y by user Z"). → folded into **REQ-09** |
| 10 | Workflow de résolution des items non-matchés | ✅ COMPLET | The override flow (item 8) IS the resolution workflow — a `MISMATCH` invoice cannot proceed past `SOUMIS` without either corrected PO/GRN data + re-match, or a `ROLE_DAF`/`ROLE_ADMIN` override (enforced in `InvoiceStateMachineServiceImpl`). |
| 11 | Export des rapports de matching | ❌ ABSENT | `grep -rn "matching.*export\|export.*matching" -i` → zero matches. No endpoint/UI to export Three-Way Matching results, contrast with `ReportController`'s PDF export for other report types (Module 11). → folded into **REQ-09** |

### Module 5 coverage: **8/11 ✅ COMPLET, 1/11 ⚠️ PARTIEL, 2/11 ❌ ABSENT** (≈ 73-82%)

**Headline finding**: the CORE matching engine (items 1-6, 8, 10 — comparison, status, discrepancy flagging, override-with-justification, resolution workflow) is excellent — fully implemented, correctly enforced, append-only as designed. Both gaps (REQ-08 tolerance config, REQ-09 history/export) are admin/power-user conveniences on top of a working core, not core-functionality gaps. REQ-08 is the more significant: CLAUDE.md §9 explicitly frames tolerance values as DB-configurable specifically so they CAN be changed without a redeploy — but with no UI, only direct DB/API manipulation can ever exercise that configurability.

---

## Module 6 — Approval Workflow (state machine BAP)

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Dashboard d'approbation | ✅ COMPLET | `ApprovalQueuePage.tsx` + `DashboardPage.tsx` validator branch (Module 2 item 1, ✅). |
| 2 | Vue détail facture pour approbateur | ✅ COMPLET | `InvoiceDetailPage.tsx` (Module 4 item 6, ✅). |
| 3 | Approbation / rejet avec commentaires | ⚠️ PARTIEL | Already assessed in Module 4 item 7 — approval ✅, rejection has zero frontend (**REQ-06**). Not double-counted; cross-referenced here as the same finding. |
| 4 | Visualisation multi-niveaux | ✅ COMPLET | Approval Steps timeline (`InvoiceDetailPage.tsx:260-289+`, Module 4 item 10) shows N1 and (where applicable) N2 steps with distinct statuses — multi-level visualization is real. |
| 5 | **Délégation d'approbation** (= T6) | ❌ ABSENT (frontend) — backend complete | Re-confirms **P4-02**: `DelegationController.java` (3 endpoints: create/list/revoke, V40 migration) fully implemented and tested (T6), but `grep -rln "delegation\|delegate" frontend/src -i` → zero matches. Same finding as P4-02 — **not** raising a new REQ-xx (already tracked in Phase 4's findings table for ISSUES.md), cross-referenced here per the matrix's explicit instruction to verify T6 in this module. |
| 6 | Config des règles d'escalade | ✅ COMPLET | Already assessed in Module 4 item 11 — `DeadlineReminderJob` SLA escalation to DAF/Admin. Cross-referenced, not double-counted. |
| 7 | Historique | ✅ COMPLET | Same as Module 4 item 10 (Approval Steps timeline). Cross-referenced. |
| 8 | **Interface d'approbation mobile** | ✅ COMPLET (responsive web; no native/PWA) | Same Tailwind responsive-class pattern already credited in Module 2 item 10 (`grid-cols-2 md:grid-cols-4` etc., systematically applied incl. `ApprovalQueuePage.tsx`/`InvoiceDetailPage.tsx`). No dedicated native app or PWA manifest exists (`grep -rln "manifest.json\|PWA" frontend` → no matches) — interpreting "mobile interface" as "responsive web UI usable on mobile", which is the realistic scope for a bachelor project; a separate native/PWA app would be a different requirement entirely and is not implied elsewhere in the matrix. |
| 9 | Notifications d'approbation | ✅ COMPLET | Full notification-event suite: `InvoiceSubmittedEvent`, `InvoiceValidatedEvent`, `InvoiceRejectedEvent`, `BonAPayerEvent`, `InvoicePayedEvent`, `ApprovalDeadlineEvent` — each dispatched to `EmailNotificationListener` (email), `PersistNotificationListener` (in-app, feeds `NotificationDropdown.tsx`/unread-count, Module 2 item 8), and `WebSocketNotificationListener` (real-time push). Comprehensive. |
| 10 | Tracking SLA | ✅ COMPLET | Same as Module 4 item 12 (`BottleneckDTO.isBottleneck`, 3-day SLA). Cross-referenced. |
| 11 | Analytics d'approbation | ✅ COMPLET | `ReportServiceImpl.calculateAverageApprovalDays(stepOrder)` (`:184-200`) — average time-in-step computed separately for L1 (`stepOrder=1`) and L2 (`stepOrder=2`) approvals, combined with bottleneck flagging (item 10) for a complete approval-analytics picture. Exposed via `ReportController` (cross-check Module 11 for the frontend chart). |
| 12 | **Conformité au tableau des départements** | ✅ COMPLET — exact match | `V2__create_departments.sql:16-25` seeds exactly the 9 departments named in the matrix (DRH, DG, FIN, INFO, TERM, COM, QHSSE, INFRA, TECH), with `requires_n2 = TRUE` for **exactly** INFO, INFRA, TECH (Informatique/Infrastructure/Atelier — matching CLAUDE.md §5's "IT et Infrastructure et Atelier ont un Niveau 2") and `FALSE` for the other 6. `V31__fix_finance_approver_and_remove_auditeur.sql` later updates FIN's `n1_role` to `ROLE_DAF` (CFO is FIN's L1 approver per briefing §5.5/§6) — a role-naming refinement, not a structural change to the N1/N2-count table. The "Accounting Assistant as initiator" pattern (CLAUDE.md §5: AA enters all invoices regardless of department) is enforced uniformly — `InvoiceController.createInvoice` is `ROLE_ASSISTANT_COMPTABLE`-gated with no department-specific initiator role. **Full compliance, no findings.** |

### Module 6 coverage: **9/12 ✅ COMPLET, 1/12 ⚠️ PARTIEL, 1/12 ❌ ABSENT** (≈ 75-92% — ⚠️/❌ items are REQ-06/P4-02, both already tracked, no NEW findings from this module)

**Headline finding**: Module 6 is the audit's first module with **zero new findings** — every item is either fully compliant (notably item 12, the department-table compliance check, which the matrix explicitly flagged "à vérifier en priorité" and which matches the spec exactly down to the per-department N1/N2-role names) or a cross-reference to an already-tracked issue (REQ-06 rejection UI, P4-02 delegation UI). The state machine's department-routing foundation is solid.

---

## Module 7 — Payment Tracking

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Dashboard de suivi | ✅ COMPLET | `PaymentsPage.tsx` — "Factures Bon à Payer (en attente de règlement)" table + "Historique des paiements" table, both paginated (`GET /payments`, `GET /invoices?status=BON_A_PAYER`). |
| 2 | Analyse d'âge (aging) | ✅ COMPLET (via Reports) | `ReportServiceImpl.getAgingAnalysis()` (`:408-488`) — buckets `0_30`/`31_60`/`61_90`/`90_plus` days. Frontend usage confirmed in `ReportsPage.tsx` (cross-check Module 11 for full UI verification). Not duplicated on `PaymentsPage.tsx` itself, but the capability exists and is reachable. |
| 3 | Monitoring des échéances | ✅ COMPLET | Same `DeadlineReminderJob` already credited in Module 4 item 11 — covers approval-step deadlines; `BON_A_PAYER`-stage payment deadlines are visible via the "awaiting payment" table (item 1) which lists all such invoices regardless of age. |
| 4 | Statuts (scheduled / processed / paid / overdue) | ✅ COMPLET — modeled via `Invoice.status`, not a separate `Payment.status` | `BON_A_PAYER` = scheduled/awaiting, `PAYE` = paid (set by `recordPayment`, which "Triggers Archive" per `PaymentController.java:36`). "Processed"/"overdue" map to the aging buckets (item 2) and `DeadlineReminderJob` overdue detection. `Payment` entity itself has no separate status field — it's a point-in-time record created only once payment is confirmed, which is the correct design (a `Payment` row existing IS the "paid" signal). |
| 5 | Traitement par lot (batch) | ❌ ABSENT | `PaymentController.recordPayment` (`:34-44`) accepts exactly one `invoiceId` + one `PaymentRequest` — no batch/bulk endpoint, no multi-select UI on `PaymentsPage.tsx`. → folded into **REQ-12** |
| 6 | Enregistrement de confirmation de paiement | ❌ ABSENT — **CRITICAL bug, feature is broken** | `RecordPaymentModal` (`PaymentsPage.tsx:41-145`) submits `paymentMethod` values `BANK_TRANSFER`/`CHECK`/`MOBILE_MONEY`/`CASH` (lines 34-39) to `POST /payments/invoice/{id}`. Backend `PaymentMethod` enum (`PaymentMethod.java:3-7`) is `VIREMENT`/`CHEQUE`/`ESPECES` — **none of the 4 frontend values match any backend enum constant**. `PaymentRequest.paymentMethod` is `@NotNull PaymentMethod` (Java enum type, `PaymentRequest.java:16`) — Jackson will throw `HttpMessageNotReadableException` (HTTP 400) on deserialization for ANY submission. No `@JsonAlias`/custom deserializer/enum-mapping found anywhere in `domain/payment/`. **Every click of "Enregistrer le paiement" fails.** → **REQ-10** (CRITIQUE) |
| 7 | Génération d'avis de versement (remittance advice) | ⚠️ PARTIEL — backend complete, frontend trigger missing | `RemittanceAdviceServiceImpl.generateRemittanceAdvice()` (PDF via iText, `messageSource`-driven i18n) + `GET /payments/{paymentId}/remittance` (pre-signed download URL, `PaymentController.java:65-71`) both fully implemented. `PaymentsPage.tsx:287-292` "Avis" button has **no `onClick`** — `grep -rn "remittance" frontend/src/pages -i` → this is the only match, and it's a dead button. → **REQ-11** |
| 8 | Méthodes de paiement (virement, chèque, mobile money) | ⚠️ PARTIEL — backend has 2/3 (no mobile money), AND frontend/backend enums don't match at all | Backend `PaymentMethod`: `VIREMENT` (✓ virement), `CHEQUE` (✓ chèque), `ESPECES` (cash — not in the matrix's 3-item list, and NOT "mobile money"). **Mobile money does not exist on the backend at all** despite being offered in the frontend dropdown (`MOBILE_MONEY`, `PaymentsPage.tsx:37`) — consistent with item 6's broken enum mapping. Folded into **REQ-10** (the enum mismatch is the root cause for both items 6 and 8 — fixing REQ-10 requires deciding the canonical method list, which should include mobile money per the matrix). |
| 9 | Historique par fournisseur | ✅ COMPLET | `SupplierPaymentHistoryDTO.java` (`domain/report/dto/`) — exists as a report DTO; `PaymentController.listPayments` also supports `departmentCode` filtering (`:54-63`), and the supplier portal (P2-01/P3-02-verified) shows each supplier their own invoice/payment status via `/supplier/invoices`. Per-supplier payment history is reachable both via the supplier's own portal and via admin reporting. |
| 10 | Analyse d'impact cash-flow | ✅ COMPLET (via Reports) | `ReportServiceImpl.getCashFlowProjection(days)` (`:497-541`) — `CashFlowProjectionDTO`/`CashFlowWeekDTO`, weekly breakdown. Frontend usage confirmed in `ReportsPage.tsx` (cross-check Module 11). Same pattern as item 2 — real capability, lives in Reports rather than Payments. |
| 11 | Export rapports | ❌ ABSENT (payment-specific) | No `/payments/export` endpoint and no export button on `PaymentsPage.tsx`. General report export (PDF, via iText) exists for OTHER report types (Module 11) but is not wired for payment/remittance history specifically beyond the single-remittance PDF (item 7). → folded into **REQ-12** |
| 12 | Config des alertes | ❌ ABSENT | No payment-specific alert configuration exists — `grep -rn "alert\|Alert" -i pages/PaymentsPage.tsx` → only the literal substring inside `export default function`. SLA/deadline alerts (Module 4 item 11) are approval-step-scoped, not payment-scoped (e.g., no "alert me if a `BON_A_PAYER` invoice has been unpaid for N days" config). → folded into **REQ-12** |

### Module 7 coverage: **6/12 ✅ COMPLET, 2/12 ⚠️ PARTIEL, 4/12 ❌ ABSENT** (≈ 50-67%)

**Headline finding**: **REQ-10 is the most severe finding of the entire audit so far.** Unlike every prior backend-complete/frontend-absent finding (P4-02, REQ-08, REQ-11 — all "the button doesn't exist"), this is "the button exists, is wired, looks correct, and **always fails**" — a silent, total breakage of the single most business-critical action in the Payment Tracking module (confirming a supplier has been paid, which per `PaymentController.java:36` also triggers archival). Any manual QA pass that clicks through the payment-recording UI would immediately surface this as a 400 error toast — strongly suggesting this code path has **never been executed end-to-end** since the frontend `PAYMENT_METHODS` list was written (it may have been written against an earlier/aspirational version of the backend enum that included `MOBILE_MONEY`, then the backend enum was later finalized as `VIREMENT`/`CHEQUE`/`ESPECES` without updating the frontend). This is the highest-priority Phase 10 correction candidate identified to date.

---

## Module 8 — Supplier Management

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Annuaire fournisseurs (recherche + filtres) | ✅ COMPLET | `SuppliersPage.tsx` — search input + status filter, `GET /api/v1/suppliers?search=...&status=...` (`SupplierController.java:78-87`). |
| 2 | Gestion de profil | ✅ COMPLET | Admin: `SupplierFormPage.tsx`/`SupplierDetailPage.tsx` + `PUT /suppliers/{id}` (`SupplierController.java:64-71`). Self-service: `SupplierProfilePage.tsx` + `PUT /supplier/profile` (item 11 cross-ref). |
| 3 | Détails bancaires (chiffrés — voir T1) | ⚠️ PARTIEL — encrypted at rest ✅, but leaked in plaintext via item 11's endpoint | `Supplier.bankDetails` (`Supplier.java:58-60`) correctly uses `@Convert(EncryptionAttributeConverter.class)` — AES-256 at rest, re-confirms T1. **However**, see item 11 / **REQ-13**: the supplier-facing profile GET returns the raw entity, so the (correctly-encrypted-at-rest) value is serialized as plaintext in the API response. The encryption mechanism itself is sound; the exposure is a separate DTO-layering bug. |
| 4 | Infos fiscales + certificats | ✅ COMPLET | `Supplier.taxId` (unique, `Supplier.java:49-50`) for fiscal info; `SupplierDocumentType.TAX_CERTIFICATE` (`SupplierDocumentType.java:4`) + `POST/GET /suppliers/{id}/documents` (`SupplierController.java:145-175`) for certificate uploads. Modeled as a document-attachment, not a dedicated entity — functionally complete. |
| 5 | Suivi contrats / accords | ✅ COMPLET | `SupplierDocumentType.CONTRACT` (`SupplierDocumentType.java:5`) — same document-repository mechanism as item 4. Contracts are tracked as typed documents per supplier. |
| 6 | Métriques de performance fournisseur | ✅ COMPLET | `GET /suppliers/{id}/performance` (`SupplierController.java:124-144`) + `SupplierPerformanceTask.java` (scheduled task — performance metrics computed proactively, not just on-demand). Exposed in `SupplierDetailPage.tsx` (admin view). |
| 7 | Log de communication | ❌ ABSENT | No `CommunicationLog`/`SupplierNote`/similar entity or endpoint found (`grep -rln "CommunicationLog\|communication_log\|SupplierNote" -i src` → zero matches). No UI for logging calls/emails/notes per supplier. |
| 8 | Catégorisation / segmentation | ❌ ABSENT | No `category`/`segment`/`tier` field on `Supplier` entity, no categorization UI on `SuppliersPage.tsx`/`SupplierFormPage.tsx`. |
| 9 | Repository documentaire par fournisseur | ✅ COMPLET | `SupplierDocument` entity + `GET/POST /suppliers/{id}/documents` (admin) and `GET/POST /supplier/documents` (self-service, `SupplierPortalController.java:207-245`) — `SupplierDocumentsPage.tsx` provides the self-service UI. Cascade + orphan-removal configured (`Supplier.java:88-90`). |
| 10 | Workflow d'onboarding | ✅ COMPLET | `SupplierStatus` lifecycle `PENDING_VERIFICATION → ACTIVE → SUSPENDED` (CLAUDE.md §9), `onboardedBy`/`onboardedAt` tracked on `Supplier` entity, `/activate`/`/suspend` endpoints (`SupplierController.java:89-116`, admin/AA-only per CLAUDE.md §9 "Only ROLE_ADMIN can change supplier status" — **note**: code shows `hasAnyRole('ADMIN','ASSISTANT_COMPTABLE')` for activate/suspend, slightly broader than CLAUDE.md §9's "Only ROLE_ADMIN" — flagging as a minor doc-vs-code discrepancy, not a security issue per se since AA is a trusted internal role, but noted for Phase 11 doc accuracy). |
| 11 | Accès portail self-service | ⚠️ PARTIEL — portal exists and works, but its profile endpoint has a DTO-exposure bug | `SupplierLayout.tsx` + `SupplierDashboardPage.tsx`/`SupplierProfilePage.tsx`/`SupplierInvoicesPage.tsx`/`SupplierDocumentsPage.tsx` — full self-service portal, P2-01/P3-02-verified end-to-end for invoices. **BUT** `GET /supplier/profile` returns `ApiResponse<Supplier>` (raw entity) instead of `SupplierResponse` (the DTO already used by the sibling `PUT /supplier/profile`, line 157) — → **REQ-13** |
| 12 | Export rapports | ❌ ABSENT | No supplier-directory export endpoint (`grep -rn "export" -i SupplierController.java` → no matches). General report export (Module 11) does not include a "supplier list" or "supplier performance" report type (to be confirmed in Module 11). |

### Module 8 coverage: **8/12 ✅ COMPLET, 2/12 ⚠️ PARTIEL, 2/12 ❌ ABSENT** (≈ 67-83%)

**Headline finding**: **REQ-13** is the second security-relevant finding of the audit (after REQ-10's functional break) — a JPA-entity-as-response violation that specifically affects the ENCRYPTED bank-details field, the exact data type CLAUDE.md §3/§9 singles out for protection. The encryption-at-rest mechanism (T1) is correctly implemented; the bug is purely in the controller's response type choice for ONE endpoint, with a ready-made fix already present two lines away (`SupplierResponse`, used by the sibling `PUT` endpoint). Items 7-8 (communication log, categorization) are genuinely absent but represent CRM-adjacent "nice to have" capabilities, lower priority than the core directory/onboarding/documents/performance functionality, which is comprehensive.

---

## Module 9 — Digital Archiving

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Repository avec structure de dossiers | ❌ ABSENT | `ArchivePage.tsx` (full file read) is a flat, paginated table — no folder/tree hierarchy of any kind. Archived invoices are organized only by table columns + filters (item 2), not a navigable folder structure. |
| 2 | Stockage par date / fournisseur / statut | ⚠️ PARTIEL | **By date**: ✅ `from`/`to` date-range filters (`ArchivePage.tsx:39-40`, backend `searchArchived(..., from, to, ...)`). **By status**: ✅ implicitly — only `Invoice.status = ARCHIVE` rows are ever returned by `/invoices/archive` (final BAP state, CLAUDE.md §5). **By fournisseur**: ⚠️ supplier name is searchable via the free-text `keyword` param (`searchArchived`, item 3) but there is no DEDICATED supplier filter/dropdown (contrast with the department `select`, lines 77-83) — a user must know/type the supplier name rather than pick from a list. |
| 3 | Recherche avancée (= T7) | ✅ COMPLET | `GET /invoices/archive` (`InvoiceController.java:193-204`, `InvoiceService.searchArchived` → `invoiceRepository.searchArchived`) — full-text `keyword` + `department` + `from`/`to` date-range, paginated. Re-confirms T7. |
| 4 | Visionneuse avec zoom / rotation | ❌ ABSENT | No PDF/image viewer component found anywhere in `frontend/src` (`grep -rln "zoom\|rotat" -i frontend/src/pages frontend/src/components` → no matches related to document viewing). The Archive page's only document interaction is the dead "Download PDF" button (→ **REQ-15**). |
| 5 | Affichage métadonnées | ✅ COMPLET | The archive table itself displays rich metadata per row: reference, supplier, department, amount/currency, issue date, archived-date (`ArchivePage.tsx:120-127`). `InvoiceDetailPage.tsx` (linked via "view") shows full metadata including documents list. |
| 6 | Versioning des factures | ❌ ABSENT | No `InvoiceVersion`/revision-history entity (`grep -rln "InvoiceVersion\|invoice_version" --include="*.java"` → zero matches). Status-transition history (`InvoiceDetailPage.tsx` approval timeline, Module 4 item 10) tracks WORKFLOW state changes, not DOCUMENT-content versions (e.g., a corrected re-upload after rejection does not preserve the prior file as a "version"). → folded into **REQ-16** |
| 7 | Config de politique de rétention | ❌ ABSENT | `ArchivePage.tsx:175` displays a static "10 ans" (10-year) retention claim with **zero backing implementation** — no `RetentionPolicy` entity, no admin config screen, no scheduled purge job. → **REQ-14** |
| 8 | Contrôles archive / purge (soft-delete uniquement pour le financier) | ✅ COMPLET (by correct absence of purge) | Per CLAUDE.md §3 ("always soft-delete financial records — never hard delete") and CLAUDE.md §9 (`ThreeWayMatchingResult` append-only, `Payment`/`Supplier` soft-delete via `deleted`/`deletedAt`), there is **deliberately no purge mechanism** for financial records — `grep -rln "purge\|Purge" --include="*.java"` → zero matches, which is the CORRECT state per the architecture rules (a "purge" would violate the soft-delete-only mandate). Interpreting this item's parenthetical ("soft-delete uniquement pour le financier") as confirming this is the EXPECTED behavior, not a gap. |
| 9 | Logs d'accès aux documents | ❌ ABSENT | `InvoiceDocumentService`/`InvoiceDocumentController` never call `AuditService` (`grep -n "auditService\|AuditService"` → zero matches in either file) — no audit-log entry is created when a user downloads/views a financial document. The general `AuditLog` infrastructure exists (Module 10) but is not wired to document access. → folded into **REQ-16** |
| 10 | Export | ❌ ABSENT | No export endpoint for the archive list itself (distinct from per-invoice "Download PDF", which is also dead — item 4/REQ-15). `grep -rn "export" -i ArchivePage.tsx` → no matches. |
| 11 | Reporting de conformité | ⚠️ PARTIEL — general compliance reporting exists, not archive-specific | `ReportServiceImpl` produces PDF audit-trail reports (seen in Phase 3, `:332-334` status-transition tables) — general compliance/audit reporting capability exists (full verification in Module 11). No DEDICATED "archive compliance report" (e.g., "X invoices archived this quarter, retention status, access summary") exists, but the underlying audit-trail PDF generation could serve this purpose if pointed at archived invoices. Not raising a new finding — cross-referencing Module 11 for the complete picture. |

### Module 9 coverage: **3/11 ✅ COMPLET, 2/11 ⚠️ PARTIEL, 6/11 ❌ ABSENT** (≈ 27-45%, but item 8's "✅ by correct absence" is a special case — see note below)

**Headline finding**: Module 9 has the LOWEST raw coverage percentage of any module audited so far, but with important nuance: item 8 (the item the matrix itself flags with a caveat — "soft-delete uniquement pour le financier") is ✅ specifically BECAUSE the riskier capability (purge) was correctly NOT built, which is the architecturally-correct outcome per CLAUDE.md §3/§9. The remaining gaps cluster into two groups: (a) **organizational/UX features never built** (folder structure, viewer, versioning, export, dedicated supplier filter) — genuinely missing but lower-risk; and (b) **REQ-14, a misleading integrity/retention claim displayed to users as if it were enforced**, which is the more concerning finding — a compliance-relevant UI string asserting guarantees (SHA-256 verification, 10-year retention) that the system does not actually provide. Core search (item 3/T7) and metadata display (item 5) — the features most likely to be used day-to-day — are solid.

---

## Module 10 — Audit Trail

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Visionneuse de logs complète (= AuditController, lié T7) | ⚠️ PARTIEL — viewer UI exists and is reachable, but the role-specific endpoints it should use are broken | `AdminAuditPage.tsx` (ROLE_ADMIN, via `/audit-logs`) and `FinancialAuditPage.tsx` (ROLE_DAF, via `/audit-logs` with `entityType=HTTP_REQUEST` workaround) both render a paginated, filterable log table — the VIEWER itself exists and re-confirms T7's "audit log viewer" capability at a UI level. **However**, neither page successfully uses its INTENDED dedicated endpoint (`/audit-logs/system` or `/audit-logs/financial`) — both would return zero rows per **REQ-17**. The generic `/audit-logs` endpoint (used by both pages) works and returns data, but without the role-appropriate action-type filtering the dedicated endpoints were built to provide. |
| 2 | Timeline d'actions utilisateur avec filtres | ⚠️ PARTIEL | Both pages offer `username`/`entityType`/`action` filter inputs (`AdminAuditPage.tsx:57-78`, `FinancialAuditPage.tsx:86-104`) and a chronological (`createdAt DESC`, `AuditController.java:67,88,109`) paginated list — the "timeline with filters" UX exists. But per **REQ-18**, the `username` filter is useless against filter-originated rows (≈all of them) since `user_id` is always NULL for those — filtering by user only surfaces the rare `UserService.PROFILE_UPDATE` entries. |
| 3 | Historique de modification de facture | ✅ COMPLET (mechanism), ⚠️ visibility affected by REQ-17 | `AuditLog.oldValue`/`newValue` (JSONB columns, `AuditLog.java:39-45`) are designed to capture before/after state for entity modifications. `AuditServiceImpl.logAction` correctly serializes both via Jackson. The DATA MODEL supports invoice-modification history fully. Whether any service actually calls `logAction` with invoice old/new values (vs. the filter's generic HTTP-level logging) was not found in this audit (only `UserService.PROFILE_UPDATE` and the filter were found as callers) — so the mechanism exists and is correct, but in practice invoice field-level changes are not captured this way; the workflow-status timeline (Module 4 item 10, `ApprovalStep` entities) is the actual source of invoice history shown to users, which IS populated correctly. |
| 4 | Tracking des décisions d'approbation | ✅ COMPLET (via workflow module, not AuditLog) | Re-confirmed from Module 4: `ApprovalStep` entities + `/workflow/steps` endpoint provide a complete, correctly-populated approval-decision history shown in `InvoiceDetailPage.tsx`'s timeline. This is the SAME pattern as item 3 — the "audit trail" requirement for approval decisions is satisfied by the workflow domain's own tables, independent of (and unaffected by) `AuditLog`/REQ-17. |
| 5 | Log des changements de statut de paiement | ✅ COMPLET (via Invoice status, not AuditLog) | `Payment` creation is tied to the `Invoice.status` transition `BON_A_PAYER → PAYE` (CLAUDE.md §5 workflow), which IS tracked via the same `ApprovalStep`/status-history mechanism as item 4. Payment-specific `AuditLog` entries (`action="PAYMENT"`) are never written (per REQ-17), but the STATUS CHANGE itself is captured by the workflow history — functionally, "when was this invoice marked paid" is answerable; "who specifically called the payment-recording endpoint, with what audit metadata" is not. |
| 6 | Enregistrements d'accès aux documents | ❌ ABSENT | Already established as **REQ-16** (Module 9, item 9) — `InvoiceDocumentService`/`Controller` never call `AuditService`. Re-confirmed here, not double-counted. |
| 7 | Activité login/logout | ❌ ABSENT | `"LOGIN"`/`"LOGOUT"` appear ONLY as string literals inside `AuditController.SYSTEM_ACTIONS` (`AuditController.java:42`) — `grep -rn "\"LOGIN\"\|\"LOGOUT\"" src/main/java --include="*.java"` returns only that one line. No authentication-success/failure/logout handler anywhere calls `auditService.logAction(...)` with these (or any) values. The system DECLARES it tracks login/logout (the enum list exists) but NEVER WRITES such an entry — folded into **REQ-17** (the action-value contract is broken for this AND all other declared action types). |
| 8 | Export | ⚠️ PARTIEL — financial only, and only reachable via the broken-filter page | `FinancialAuditPage.tsx:75-82` has a working "Export PDF" button → `reportService.exportCompliancePdf(start, end)` (year-to-date range) — a real, wired export capability for the DAF's financial audit view. **No equivalent export exists on `AdminAuditPage.tsx`** (system/security logs, ADMIN role) — `grep -rn "export" -i AdminAuditPage.tsx` → no matches. Additionally, since the financial page's underlying query is itself the REQ-17 workaround (showing all `HTTP_REQUEST`-typed rows, not true financial-action rows), the EXPORTED PDF's source data has the same fidelity problem as the on-screen table. |
| 9 | Alertes de détection d'anomalie | ❌ ABSENT | `grep -rln "anomal" -i src frontend/src` → zero matches anywhere in the codebase. No threshold-based or pattern-based security alerting exists on top of `AuditLog` data. → **REQ-19** |
| 10 | Affichage conformité période de rétention | ❌ ABSENT | No retention-period display or configuration exists for `audit_logs` specifically (distinct from REQ-14's invoice-document retention claim, Module 9) — `AuditLog` rows have no TTL/retention field, and neither audit page displays any retention-related text. Consistent with the broader absence of retention-policy infrastructure already noted in REQ-14. |
| 11 | Dashboard temps réel | ❌ ABSENT | Neither `AdminAuditPage.tsx` nor `FinancialAuditPage.tsx` uses WebSocket/STOMP or polling-based auto-refresh (`grep -rln "WebSocket\|STOMP\|useInterval\|refetchInterval" -i` on both files → zero matches) — both are static, manually-paginated tables requiring a page reload or filter change to see new entries. → folded into **REQ-19** |
| 12 | **Sous-typage de l'audit** (lié T7 — vérifier que les types d'événements sont bien différenciés, pas tous génériques) | ❌ ABSENT — this is the matrix's own flagged-for-verification item, and verification finds the sub-typing is non-functional | This is **exactly REQ-17**. The DATA MODEL has TWO candidate sub-typing dimensions: `entityType` (filter-classified into `FINANCIAL_ACTION`/`SYSTEM_ACTION`/`HTTP_REQUEST` by `AuditLoggingFilter.classifyAction()`, `AuditLoggingFilter.java:61-71`) and `action` (intended to hold semantic values like `APPROVE`/`LOGIN`/`PAYMENT` per `AuditController`'s two allowlists, but in practice holds `"METHOD URI -> status"` strings for filter-originated rows). **`AuditController` filters on `action`, but the only producer of `action` values for normal traffic writes free-text HTTP-method strings, not the semantic values the filter expects.** The matrix's instruction to "vérifier que les types d'événements sont bien différenciés, pas tous génériques" is answered: **types ARE NOT well-differentiated — `entityType` provides a coarse 3-way split that exists in the DB but is never queried by `AuditController` (which queries `action` instead), and `action` itself is generic (`HTTP_REQUEST`-style) for ~all rows.** This is the root cause underlying items 1, 2, 7, and 8 above. |

### Module 10 coverage: **3/12 ✅ COMPLET, 4/12 ⚠️ PARTIEL, 5/12 ❌ ABSENT** (≈ 25-58% depending on how partial credit is weighted)

**Headline finding**: **REQ-17 is the most severe finding of the entire audit to date** — more severe than REQ-10 (broken payment methods) because: (a) REQ-10 breaks ONE feature with an immediate, visible 400 error that any QA pass would catch; REQ-17 breaks the **audit/compliance subsystem itself** — the system that exists specifically to detect and prove what happened — and does so **silently**, returning empty-but-valid paginated responses (`{content: [], totalElements: 0}`), which looks like "no events yet" rather than "broken filter". (b) It affects BOTH `ROLE_ADMIN` and `ROLE_DAF`, the two highest-trust roles in the system. (c) For a Final Year Project whose subject is an **invoice/financial management system**, an audit trail that cannot actually show "who approved/rejected/paid what, and when" — items 7 (login/logout, REQ-17), and the REQ-18 "every actor is `—`" finding — undermines a core compliance narrative of the entire application, even though the UNDERLYING DATA MODEL (`AuditLog` entity, JSONB old/new values, IP/user-agent capture) is well-designed and would work correctly if the `action`-value contract between `AuditLoggingFilter` (producer) and `AuditController` (consumer) were fixed. Items 3-5 (modification/approval/payment history) are NOT actually broken in user-visible terms — they're served by the workflow module's own tables, a parallel and functioning history mechanism that happens to make the `AuditLog`-based "audit trail" feature's brokenness less immediately obvious to an end user (the approval timeline still works; it's the SEPARATE admin/DAF "Audit Logs" pages that are silently empty/wrong).

---

## Module 11 — Reporting & Analytics

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Dashboard analytics avec KPI cards | ✅ COMPLET | `ReportsPage.tsx:114-125` — 4 KPI cards (total invoices, overdue, avg processing time, rejection rate) from `GET /reports/kpis` → `DashboardKpiDTO` (`ReportController.java:40-45`). `/reports/summary` (`:47-52`) is a documented alias of the same data. |
| 2 | Rapports de temps de traitement | ✅ COMPLET | `DashboardKpiDTO.averageProcessingTimeDays` + per-level breakdown `averageN1ApprovalDays`/`averageN2ApprovalDays`/`averageDafApprovalDays` (`DashboardKpiDTO.java:8,12-14`), computed via `calculateAverageApprovalDays(stepOrder)` (`ReportServiceImpl.java:184-200`, confirmed Phase 3). Displayed on `ReportsPage.tsx:121`. |
| 3 | Analytics de performance fournisseur | ✅ COMPLET | `GET /reports/supplier/{supplierId}/performance` (`ReportController.java:139-144`) → `SupplierPerformanceDTO` (accuracy rate, rejection rate, avg payment time per Module 8 item 6) — re-confirms Module 8 item 6's "✅ COMPLET" verdict from the Reports-module side. |
| 4 | Rapports d'aging | ✅ COMPLET | `GET /reports/aging` (`ReportController.java:109-114`) → `AgingReportDTO` with `0-30/31-60/61-90/90+` buckets (`getAgingAnalysis()`, `ReportServiceImpl.java:408-488`, confirmed Phase 3). Rendered as horizontal bar chart, `ReportsPage.tsx:128-152`. |
| 5 | Analyse du cycle de paiement | ✅ COMPLET (via cash-flow projection, same pattern as Module 7 item 10) | `GET /reports/cash-flow` (`ReportController.java:116-122`) → `CashFlowProjectionDTO`/`CashFlowWeekDTO` (`getCashFlowProjection(days)`, `ReportServiceImpl.java:497-541`, confirmed Phase 3), rendered as a weekly bar chart (`ReportsPage.tsx:188-205`, recharts `BarChart`). This is a forward-looking PROJECTION (invoices due within N days) rather than a backward-looking "average days from invoice receipt to payment" metric — but it directly serves the "cycle de paiement" analytical need (when will cash actually go out). |
| 6 | Identification des goulots d'approbation | ✅ COMPLET | `GET /reports/bottlenecks` (`ReportController.java:132-137`) → `List<BottleneckDTO>`, `calculateApprovalBottlenecks()` (`ReportServiceImpl.java:575-609`, `SLA_DAYS = 3.0`, confirmed Phase 3). `ReportsPage.tsx:155-185` renders a per-approver table, color-coded by `averageDays` (green ≤3, amber ≤5, red >5). |
| 7 | Tendances volume / valeur | ⚠️ PARTIEL — snapshot exists, time-series trend does not | `DashboardKpiDTO.volumeBySupplier` (`:16`, Top-5 suppliers by amount, `findTopSuppliersByAmount`, confirmed Phase 3) and `countByStatus`/`overdueByBucket` provide CURRENT-STATE snapshots of volume/value distribution. **No time-series "trend over time" view exists** (e.g., "invoice volume per month for the last 12 months") — `grep -rn "trend\|Trend" -i ReportsPage.tsx ReportServiceImpl.java` → no matches. The data needed (`Invoice.createdAt`, `Invoice.amount`) is present in the schema; only the aggregation-over-time query and chart are missing. |
| 8 | Comparaison budget vs réel | ❌ ABSENT | No budget-vs-actual comparison exists anywhere in Reports. Compounded by **REQ-20**: `AdminDepartmentFormPage.tsx` collects a per-department `budget` value that the `Department` entity doesn't even have a column for — so even the INPUT data for this comparison is not persisted, let alone compared against actual invoice totals. → folded into **REQ-21** |
| 9 | Constructeur de rapports custom | ❌ ABSENT | No custom-report-builder UI or backend exists (`grep -rln "ReportBuilder\|report.*builder" -i src frontend/src` → zero matches). All reports are FIXED, predefined views (KPIs, aging, bottlenecks, cash-flow) — a user cannot define their own report (choose columns/filters/groupings) and save it. → folded into **REQ-21** |
| 10 | Preview + export (PDF, Excel) | ✅ COMPLET — for export; preview is "render on page" rather than a distinct pre-export preview step | **Export**: `GET /reports/export/excel` (Apache POI, `ReportController.java:61-78`) and THREE PDF export endpoints — `/export/pdf/audit/{id}` (per-invoice audit trail), `/export/pdf/compliance` (date-range compliance summary, used on both `ReportsPage.tsx:241-249` and `FinancialAuditPage.tsx:75-82`/Module 10 item 8) — all iText-based, all wired with working `onClick` handlers (`reportService.ts`, full file read, all 3 export methods correctly construct blob downloads). **Preview**: the on-page charts/tables (aging bars, bottleneck table, cash-flow chart, KPI cards) ARE a form of "preview" of the underlying data before exporting — there is no SEPARATE "preview the PDF/Excel before downloading" step, but the live dashboard arguably satisfies the spirit of "preview" for an FYP-scope system. Not raising this nuance as a new finding. |
| 11 | Config de rapports planifiés | ❌ ABSENT | No `@Scheduled` report-generation job exists for periodic reports (`grep -rn "@Scheduled" -i src/main/java --include="*.java"` was checked in earlier phases — only `DeadlineReminderJob` and `SupplierPerformanceTask` exist, neither generates/sends a "report"). No UI for configuring report frequency/recipients. → folded into **REQ-21** |
| 12 | Gestionnaire de distribution | ❌ ABSENT | No mechanism to email/distribute generated reports to a list of recipients exists. The single "Distribution" string found in the codebase (`DashboardPage.tsx:408`, "Distribution des statuts actuels") is an unrelated chart-section label (status-distribution pie/bar chart), not a report-distribution feature. → folded into **REQ-21** |
| 13 | Générateur de résumé exécutif | ❌ ABSENT | No "executive summary" report type exists (`grep -rln "executive\|résumé" -i src frontend/src` → no relevant matches). The closest analog, `/export/pdf/compliance`, is a compliance-focused audit-trail summary (Module 10), not a business/executive KPI summary (e.g., "this month: X invoices, Y XAF processed, Z% on-time, top issues"). → folded into **REQ-21** |

### Module 11 coverage: **6/13 ✅ COMPLET, 1/13 ⚠️ PARTIEL, 6/13 ❌ ABSENT** (≈ 46-50%)

**Headline finding**: Module 11 shows the clearest split yet between "operational reporting" (items 1-6, 10 — all ✅, all genuinely well-built and using real computed data with correct SLA/aging/bottleneck logic re-confirmed from Phase 3) and "enterprise reporting platform" features (items 8-9, 11-13, all ❌, consolidated into **REQ-21**) that are standard in commercial BI/reporting suites but represent a significant additional scope (report-definition DSL, a job scheduler with email distribution, executive-summary templating) — almost certainly OUT OF SCOPE for a Bachelor's FYP, similar in nature to Module 12's anticipated "hors-scope assumé" status. Unlike Modules 1-10's findings (which were mostly "started but broken" or "exists but not wired"), items 8-9/11-13 here were **never started at all** — there's no dead button, no orphaned config table, no misrouted endpoint to point to. The one item worth a Phase 10 look is **item 7** (trend-over-time) — the underlying data exists and a single new aggregation query + chart would close most of the gap, unlike items 8/9/11-13 which would each require substantial new subsystems. REQ-20 (the `budget` ghost field) is a small, isolated frontend bug independent of the larger REQ-21 scope question.

---

## Module 12 — Integration

> **Scope note**: per `docs/REQUIREMENTS-MATRIX.md`'s explicit instruction for this module, and after
> confirming with the project owner, **9 of the 12 items below are declared "hors-scope assumé"**
> and documented in `docs/SCOPE.md` rather than raised as bugs. The remaining 3 items (8, 9, 11 —
> webhooks/integration-status) ARE raised as a normal finding (**REQ-22**), since a substantial
> backend already exists for them. The table below shows BOTH the scope decision AND the technical
> verdict for completeness.

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Dashboard de config d'intégration | 🔹 HORS-SCOPE (`docs/SCOPE.md`) | No `/integrations` admin dashboard page exists in `frontend/src`. Declared out-of-scope alongside items 2-7/10/12. |
| 2 | Connexion ERP (SAP, Oracle, MS Dynamics) | 🔹 HORS-SCOPE (`docs/SCOPE.md`) | Zero references to any named ERP system anywhere in the codebase. |
| 3 | Intégration système d'approvisionnement | 🔹 HORS-SCOPE (`docs/SCOPE.md`) | The existing Purchase Order module (`PurchaseOrderController`, Module 5/7 cross-refs) is fully INTERNAL — no connector to an external procurement platform. |
| 4 | Connexion logiciel comptable | 🔹 HORS-SCOPE (`docs/SCOPE.md`) | No accounting-software integration of any kind. |
| 5 | Intégration bancaire pour paiements | 🔹 HORS-SCOPE (`docs/SCOPE.md`) | `PaymentController` (Module 7) records payments via manual form entry only — no bank API/file-import integration. |
| 6 | Intégration GED (externe) | 🔹 HORS-SCOPE (`docs/SCOPE.md`) | MinIO (Module 9) is the system's OWN internal document store, not a connector to an external GED platform. |
| 7 | Interface de config API | 🔹 HORS-SCOPE (`docs/SCOPE.md`) | No generic "configure external API credentials" admin screen exists. |
| 8 | Gestion des webhooks | ⚠️ PARTIEL — backend ✅, frontend ❌ | `WebhookController` (`/api/v1/integrations/webhooks`) — register (`POST`, returns secret once), list (`GET`, secret omitted), deactivate (`DELETE`, soft-delete via `isActive`), delivery log (`GET /{id}/deliveries`, paginated). All `@PreAuthorize("hasRole('ADMIN')")`. Re-confirms Phase 3's webhook-delivery findings (P3-04) from the management-API side. **NOT in `docs/SCOPE.md`** — see **REQ-22**. |
| 9 | Monitoring du statut d'intégration | ⚠️ PARTIEL — backend ✅, frontend ❌ | `IntegrationStatusController` (`GET /api/v1/integrations/status`, ADMIN-only) — returns `WebhookStatusResponse` per active webhook: name, URL, subscribed events, `lastResponseStatus`, `lastDeliverySuccess`, `lastDeliveredAt` (via `deliveryRepository.findLatestDeliveryByWebhook`). A real, working "is this integration healthy" endpoint. **NOT in `docs/SCOPE.md`** — folded into **REQ-22**. |
| 10 | Config de planning de sync | 🔹 HORS-SCOPE (`docs/SCOPE.md`) | No external-system sync scheduling exists — the project's only `@Scheduled` jobs (`DeadlineReminderJob`, `SupplierPerformanceTask`, confirmed in earlier modules) are purely internal business-logic jobs, not external-sync jobs. |
| 11 | Log d'erreurs + résolution | ⚠️ PARTIEL — error log ✅ (as part of item 8's delivery log), resolution workflow ❌ | `WebhookDelivery` entity (`responseStatus`, `success`, `attemptCount`, `lastAttemptedAt`) IS an error/delivery log, exposed via item 8's `/{id}/deliveries` endpoint — failed deliveries are visible with their HTTP status and attempt count. **No "resolution" workflow** exists (e.g., marking an error as acknowledged/resolved, manual retry trigger, notes) — the log is read-only. **NOT in `docs/SCOPE.md`** — folded into **REQ-22**. |
| 12 | Interface de test de connexion | 🔹 HORS-SCOPE (`docs/SCOPE.md`) | No "test connection" button/endpoint exists for any integration (would naturally pair with items 2-7's connectors, none of which exist). |

### Module 12 coverage: **9/12 🔹 hors-scope assumé** (`docs/SCOPE.md`), **3/12 ⚠️ PARTIEL** (backend-complete/frontend-absent, REQ-22) — **0/12 counted as ❌ ABSENT bugs**

**Headline finding**: Module 12 is the audit's first module where the scope-exclusion mechanism (anticipated by the matrix itself) was actually exercised. The 9 excluded items cleanly fit the "named third-party enterprise system connector" pattern the matrix's note describes — genuinely beyond a Bachelor FYP's reasonable scope, and excluding them does not weaken the audit (no evidence any of them were ever attempted). The 3 NOT-excluded items (8/9/11, all → **REQ-22**) are a SEVENTH instance of this audit's recurring backend-complete/frontend-absent pattern, but notably the MOST COMPLETE backend of any such instance so far — `WebhookController` + `IntegrationStatusController` together implement registration, secret management, soft-delete, paginated delivery history, AND health monitoring, all correctly role-gated and matching CLAUDE.md §9's webhook rules exactly (HMAC-SHA256, 3-retry backoff, append-only log, admin-only). If a Phase 10 UI were ever built for this, very little additional backend work would be needed — it is, in effect, a "headless admin feature" the project owner may not even know is fully usable via direct API calls today.

---

## Module 13 — User & Access Management

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Console de gestion utilisateurs | ✅ COMPLET | `AdminUsersPage.tsx` (`/admin/users`, ROLE_ADMIN) — paginated table (`size=100`) listing every user with name/email, username, role badges, and an active/inactive dot. `EditUserModal` (`:36-125`) supports inline edit of first/last name + email, plus activate/deactivate and unlock-account actions. Backed by `UserController` (`GET /users`, `PUT /users/{id}`, `PATCH /users/{id}/activate`, `POST /users/{id}/unlock` — all `@PreAuthorize("hasRole('ADMIN')")` at class level). |
| 2 | Attribution rôles / permissions | ✅ COMPLET | `AdminUserFormPage.tsx` (`/admin/users/new`) — `ROLE_OPTIONS` dropdown (14 roles, `:12-27`) with conditional department picker for the 11 department-scoped approver roles (`DEPT_REQUIRED_ROLES`, `:30-35`). `UserController.assignRoles` (`PUT /{id}/roles`, `AssignRoleRequest`) is the backend for changing an EXISTING user's role (replaces current roles). Role assignment is a single-select dropdown, not a free-text field — matches PROB pattern of constrained input. |
| 3 | Contrôle d'accès niveau département/équipe | ✅ COMPLET | Department-scoped roles (`ROLE_VALIDATEUR_N1_*`/`N2_*`) are tied to a specific `departmentId` at user-creation time (`AdminUserFormPage.tsx` conditional department `<select>`, `:213-240`, populated from `GET /departments`). `Department.java` carries `n1Role`/`n2Role` fields that the BAP workflow (CLAUDE.md §5) uses to route approvals to the correct department's approvers — re-confirms the department-routing logic verified in earlier phases. |
| 4 | Classification de sensibilité des données | ❌ ABSENT | → **REQ-23** |
| 5 | Monitoring d'activité | ⚠️ PARTIEL — cross-reference to Module 10, not a new gap | "User activity monitoring" is exactly what `AuditLog`/`AdminAuditPage.tsx` are FOR (per-user filter, `:57-78`) — but per **REQ-17/REQ-18**, the dedicated filtering is broken and the `user_id` is NULL on nearly all rows, so per-user activity monitoring does not actually work in practice. Not re-raised as a new finding — same root cause as Module 10. |
| 6 | Gestion du statut de compte | ✅ COMPLET | Three account-status mechanisms, all wired end-to-end: (a) **activate/deactivate** — `EditUserModal`'s toggle (`:98-101`) → `PATCH /users/{id}/activate?active={bool}` → `UserService.activateUser`; (b) **unlock** — `EditUserModal`'s unlock button (`:102-105`) → `POST /users/{id}/unlock` → `UserService.unlockUser` (resets failed-login counter + lock, per CLAUDE.md §9's "lock after 5 consecutive failed OTP attempts; only ROLE_ADMIN can unlock"); (c) the active/inactive status dot in the main table (`AdminUsersPage.tsx:189-191`) gives at-a-glance visibility. |
| 7 | Import/export en masse | ❌ ABSENT | → **REQ-23** |
| 8 | Workflow de demande d'accès | ❌ ABSENT | → **REQ-23** |
| 9 | Éditeur de matrice de permissions | ❌ ABSENT | → **REQ-23** |
| 10 | Vue de gestion des sessions (= T6 ActiveSession) | ✅ COMPLET | `SecuritySettingsPage.tsx`'s "Sessions actives" section (`:135-181`) — `GET /api/v1/admin/sessions` (`AdminSessionController`, ROLE_ADMIN) lists every active `ActiveSession` (username, IP, created/expires timestamps); a per-row "Révoquer" button → `DELETE /api/v1/admin/sessions/user/{userId}` → `sessionRepository.revokeAllForUser`. Full create/list/revoke lifecycle works — re-confirms T6's `ActiveSession` feature is reachable and functional from the UI, closing the loop on the backend-only verification done in T6. |
| 11 | Visionneuse d'audit trail utilisateur | ⚠️ PARTIEL — duplicate of Module 10, not re-raised | Same viewer (`AdminAuditPage.tsx`) and same `username` filter input referenced in item 5 — subject to the identical **REQ-17/REQ-18** brokenness (dedicated endpoint returns zero rows; `user_id` NULL on filter-originated entries). Counted under Module 10's findings, not duplicated here. |
| 12 | Config de menu basée sur le rôle | ✅ COMPLET | `Sidebar.tsx` uses `RoleGuard`/`PageRoleGuard` (`components/auth/RoleGuard.tsx`) throughout — each nav section is wrapped in `<RoleGuard allowedRoles={[...]} fallback={null}>` (9 distinct role-gated sections, `Sidebar.tsx:106-177`), so the menu a user sees is dynamically built from their roles. `RoleGuard.test.tsx` exists. Matches the PROB-002/PROB-004 patterns already codified in `CLAUDE.md §13` (separate route guards per role category, `fallback={null}` for nav items). |

### Module 13 coverage: **6/12 ✅ COMPLET, 2/12 ⚠️ PARTIEL (both = Module 10 duplicates, not double-counted), 4/12 ❌ ABSENT** (≈ 50-67%)

**Headline finding**: Module 13 is the audit's strongest showing for "core" functionality — every item that the BAP workflow and day-to-day admin operations actually depend on (user CRUD, role/department assignment, account activation/unlock, session management, role-based menus) is ✅ COMPLET and independently re-confirms earlier findings (T6 ActiveSession, PROB-002/004 routing patterns). The 4 ❌ items (data-sensitivity classification, bulk import/export, access-request workflow, permission-matrix editor) form a coherent "enterprise IAM" tier absent end-to-end — genuinely missing, but not contradicted by any PRD/WORKFLOW requirement (**REQ-23**, low severity). Item 10 (T6 ActiveSession) also closes the loop on **REQ-02** from the OPPOSITE direction: REQ-02 (raised in Module 1) flagged `SecuritySettingsPage.tsx`'s security-POLICY form as simulation-only — but that SAME page's "Active Sessions" sub-section (`:135-181`, this module's item 10) is fully real and working. REQ-02 is therefore now FULLY EVIDENCED: one page, two halves, one real (session list/revoke) and one fake (policy toggles, with the code's own comment admitting "simulation only in this version") — confirming `SecuritySettingsPage.tsx` is a "frontend-complete/backend-absent + self-admitted fake" for its policy half specifically, the inverse of this audit's six recurring "backend-complete/frontend-absent" findings (P4-02, REQ-08, REQ-11, REQ-15, REQ-16, REQ-22).

---

## Module 14 — Security & Compliance

| # | Item | Verdict | Proof |
|---|---|---|---|
| 1 | Matrice de permissions par rôle | ❌ ABSENT | Duplicate of Module 13 item 9 — folded into **REQ-23**. No visual role×permission matrix exists; role assignment is a single-select dropdown (`AdminUserFormPage.tsx`). |
| 2 | Indicateurs de statut de chiffrement | ❌ ABSENT | `grep -rln "encrypt.*status\|chiffrement.*statut" -i frontend/src` → zero matches. AES-256 encryption of `bankDetails`/MFA secrets (T1, `EncryptionAttributeConverter`) is real and correctly implemented at the data layer, but no admin UI surfaces "which fields are encrypted" or "encryption health/key status" — a user/admin has no way to SEE that this protection exists. → **REQ-24** |
| 3 | Paramètres 2FA / MFA | ⚠️ PARTIEL — user-level setup works; admin-level policy does not | **User-level**: ✅ COMPLET — `ProfilePage.tsx`'s MFA section (QR setup, OTP confirm, status badge — verified P4-04) is fully functional, and `MfaSetupEnforcementFilter.requiresMandatoryMfa()` correctly enforces MFA for `ROLE_ADMIN`/`ROLE_DAF`/`ROLE_ASSISTANT_COMPTABLE`/`ROLE_VALIDATEUR_N1_*`/`ROLE_VALIDATEUR_N2_*` exactly per CLAUDE.md §9 — **resolves the outstanding REQ-MFA-01 follow-up with no bug**: `ROLE_ASSISTANT_COMPTABLE` IS present (`MfaSetupEnforcementFilter.java:79`). **Admin-level**: ❌ — the "MFA required" toggle on `SecuritySettingsPage.tsx` (item 1 of its form) is part of **REQ-02**'s fake/simulated config — toggling it does nothing; the actual mandatory-role list is hardcoded in `MfaSetupEnforcementFilter.requiresMandatoryMfa()`, not configurable. |
| 4 | Monitoring d'activité de login | ⚠️ PARTIEL — cross-reference to Module 10, not a new gap | Login events are exactly the kind of thing `AuditLog`/`SYSTEM_ACTIONS` (`"LOGIN"`, `"LOGOUT"`) were designed to capture (`AuditController.java` SYSTEM_ACTIONS list) — but per **REQ-17**, no `AuditLog` row can ever have `action="LOGIN"`, since `AuditLoggingFilter` never writes that value. Login-activity monitoring is therefore structurally non-functional, same root cause as Module 10. Not re-raised as a new finding. |
| 5 | Statut de backup des données | ❌ ABSENT | `grep -rln "backup\|sauvegarde" -i src frontend/src` → zero matches anywhere in the codebase (excluding `node_modules`/build artifacts). No backup job, no backup-status entity, no admin UI. → **REQ-24** |
| 6 | Config de politique de rétention | ❌ ABSENT — duplicate of Module 9, not re-raised | Identical gap to **REQ-14** (Module 9): `ArchivePage.tsx` displays a static "10 ans" retention claim with zero backing `RetentionPolicy` entity/config/job. Counted under REQ-14, not duplicated here. |
| 7 | Tracking d'acceptation de politique de confidentialité | ❌ ABSENT | `grep -rln "privacy.*polic\|politique.*confidential\|consent" -i src frontend/src` → zero matches. No privacy-policy version, no acceptance timestamp/flag on `User`, no consent-capture UI (e.g., at registration or supplier self-registration). → **REQ-24** |
| 8 | Reporting d'incident de sécurité | ❌ ABSENT | `grep -rln "incident" -i src frontend/src` → zero matches. No security-incident model, no report-an-incident form, no incident-tracking workflow. → **REQ-24** |
| 9 | Checklist de conformité (SOX, IFRS, régulations locales) | ❌ ABSENT | `grep -rln "SOX\|IFRS\|checklist.*conform\|conformit.*checklist" -i src frontend/src` → zero matches. The system enforces SOME compliance-relevant BEHAVIORS (soft-delete per CLAUDE.md §3, append-only matching/webhook logs per §9, AES-256 encryption per T1, MFA per §9) — but nothing PRESENTS these as a checklist against named frameworks (SOX, IFRS, or local Gabonese regulations). → **REQ-24** |
| 10 | Outils de préparation d'audit | ⚠️ PARTIEL — underlying export capability exists, dedicated "audit prep" framing does not | `ReportController`'s `/export/pdf/audit/{id}` and `/export/pdf/compliance` endpoints (verified Module 11, `reportService.exportAuditPdf`/`exportCompliancePdf`, both wired to working buttons on `FinancialAuditPage.tsx`/`ReportsPage.tsx`) ARE genuinely useful "audit preparation" artifacts — a per-invoice audit-trail PDF and a compliance summary PDF. What's missing is any DEDICATED "audit prep" workspace (e.g., a checklist of evidence to gather, bulk-export-by-date-range for an external auditor). Given the underlying exports work, this is the closest item in Module 14 to being satisfied — not folded into REQ-24 (which covers items with NO existing capability at all), but noted as the one item where Phase 10 effort (if prioritized) would build on something real rather than starting from zero. |
| 11 | Calendrier de conformité | ❌ ABSENT | `grep -rln "compliance.*calendar\|calendrier.*conformit" -i src frontend/src` → zero matches. No recurring-deadline/compliance-calendar feature (e.g., "VAT filing due in 12 days", "annual audit window opens"). → **REQ-24** |
| 12 | Dashboard de santé sécurité | ❌ ABSENT | `grep -rln "security.*dashboard\|sant[ée].*s[ée]curit\|security.*health" -i src frontend/src` → zero matches. `SecuritySettingsPage.tsx` is a CONFIGURATION form (and per REQ-02, a non-functional one) — not a health/status DASHBOARD (no MFA-adoption rate, no failed-login trend, no encryption coverage, no webhook-delivery health rolled up here, even though `IntegrationStatusController`/`DashboardKpiDTO.webhookDeliverySuccessRate` from Module 12/11 could feed exactly such a widget). → **REQ-24** |

### Module 14 coverage: **0/12 ✅ COMPLET, 4/12 ⚠️ PARTIEL (3 = duplicates of REQ-17/REQ-MFA resolution, 1 = partial-credit on existing exports), 8/12 ❌ ABSENT** (≈ 0-33%)

**Headline finding**: Module 14 has the LOWEST coverage of the entire audit — but the picture is more nuanced than "0% built". Three of its four ⚠️ items are NOT new gaps: item 3 (2FA settings) resolves the long-outstanding **REQ-MFA-01** cleanly (✅, `ROLE_ASSISTANT_COMPTABLE` correctly enforced), and items 4/6 simply restate **REQ-17** and **REQ-14** from the user's "security & compliance" vantage point rather than the "audit trail"/"archiving" vantage point used in Modules 9-10. The 8 genuinely-❌ items, plus item 2 and the "framing" half of item 12, cluster into a single coherent absence: **there is no compliance/security META-LAYER** — nothing that takes the REAL, working, individually-correct controls already in the system (AES-256 encryption, soft-delete, append-only logs, MFA enforcement, webhook health monitoring, PDF audit exports) and presents them AS compliance posture, backup status, incident response, or policy tracking. Every one of these 8 items (→ **REQ-24**) would be a genuinely NEW feature (no existing backend to wire up), unlike the recurring "backend-complete/frontend-absent" pattern seen in Modules 9/11/12/13 — this is the audit's first module where the gap is "feature never conceived", not "feature half-built". Given a Bachelor FYP's scope, several of these (SOX/IFRS checklists, compliance calendar, incident reporting) arguably belong in `docs/SCOPE.md` alongside Module 12's exclusions — flagging for Phase 8/9 triage rather than pre-judging here.

---

**Phase 6 complete — all 14 modules audited.**

*(This document is being built incrementally across Phase 6. Remaining modules — User & Access Management, Security & Compliance — follow below as each is completed.)*
